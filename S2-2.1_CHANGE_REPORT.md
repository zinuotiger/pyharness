# S2-2.1_CHANGE_REPORT.md — `policy.updated` 事件注册实施报告

> **阶段**：S2-2.1（注册 `policy.updated`；**未进入 S2-3**、未接入 `ctx.governance`）
> **日期**：2026-09-14 ｜ **基线 HEAD**：`327a87b`
> **依据**：[S2-2.1_REGISTRATION_DESIGN.md](S2-2.1_REGISTRATION_DESIGN.md)（X-1~X-4 已裁定）· [ADR-020-policy-event-boundary.md](ADR-020-policy-event-boundary.md)
> **性质**：S2-2.1 实施。**未 commit**、未 push、**未接装配**。

---

## 0. 结论

# S2-2.1 = PASS

**改动 4 文件（+74 / −14）**；`EVENT_TYPES 73→74`、`SYNC_TYPES 11→12`；**全量回归 1468 collected / 1466 passed / 2 skipped / 0 failed**（较 S2-1 基线 1466/1464 **+2，零回归**）。

**边界全过**：`engine.py` / `scope.py` / `desktop/sessions.py` / `tools_guard.py` / `governance/policy.py` **五个禁止面全部未改动**；`scope.updated` 的 `op="tighten"` **语义未变**。

---

## 1. 变更清单

| 文件 | 增删 | 内容 |
|---|---|---|
| `pyharness/events/payload.py` | **+20 / −0** | 新增 `PolicyUpdatedPayload`（六字段，紧邻 `ScopeUpdatedPayload`） |
| `pyharness/events/vocab.py` | **+8 / −2** | ① payload 具名 import 名单加 `PolicyUpdatedPayload`；② `_CORE_EVENT_TYPES` 尾部注册 `("policy.updated", PolicyUpdatedPayload, False)`；③ **`SYNC_TYPES` 加入 `"policy.updated"`**；④ 词表规模注释 `70 → 74`、扩展项 `13 → 14`（R4） |
| `tests/unit/test_events.py` | **+6 / −4** | `EXPECTED_ALL` +`"policy.updated"`；`73 → 74` 两处；`test_vocab_full_64` → **`test_vocab_full`**（原名与实不符）+ docstring 更正 |
| `tests/unit/test_governance_policy.py` | **+40 / −8** | ① T6b 由"未注册降级"改为 **"session 未接线降级"**；② **新增 T6c**：注册后 `emit` **真落盘**（六字段 + `trace.config_ref` + 强同步）；③ **新增 T10**：`PolicyUpdatedPayload` 字段集 **恰等于** `POLICY_UPDATED_FIELDS`（**X-3 的防回归守卫**） |

**合计**：4 文件，**+74 / −14**。

---

## 2. 裁定落地对照

| 裁定 | 落地 | 证据 |
|---|---|---|
| **X-1** `config_ref` 留 `Envelope.trace`，不入 payload | payload 模型**无** `config_ref`；`emit_updated`（已提交，未改）继续走 `trace` | **T6c** 断言 `evs[0].trace == {"config_ref": "security.guards.disabled"}` 且 payload 无该键 |
| **X-2** 不新增 `policy_refs` | payload 六字段，无 `policy_refs` | **T10** 字段集断言；ADR-020 未变 |
| **X-3** 保留 `added` 与 `reason` | 两字段均在模型中（`reason` 必填 `min_length=1`） | **T10**；**T6c** 断言 `added: []`/`reason: "guard-disabled"` |
| **X-4** 允许范围加 2 个测试文件 | `test_events.py`（必需）+ `test_governance_policy.py`（建议）均已改 | `git diff --name-only` = 恰好这 4 个文件 |
| 只增加 `policy.updated` | 词表 +1（无其他新类型） | `EVENT_TYPES` 73 → 74；`TRANSIENT_TYPES` 仍 3 项（未动） |
| `op={add,enable,disable}` 保持 | 未改 `POLICY_OPS`；payload `op` 为 `str`（值域校验在治理层） | 运行时 `POLICY_OPS = {'add','disable','enable'}`；**T6a/T6b** 仍锁定 |
| `scope.updated` 保持 `op=tighten` | `scope.py` **未改动** | `grep 'op": "tighten"' scope.py` = 2 处（`tighten`/`note_tighten`） |

---

## 3. 关键设计取舍（与 X-2/X-3 的裁定一致）

| 取舍 | 理由 |
|---|---|
| `op: str`（**非** `Literal`） | 用 `Literal` 需在 `events` 层**复制** `POLICY_OPS`（第二真源）或让 `events` **反向 import `governance`**（违 ADR-018:308）；且最近邻先例 `ScopeUpdatedPayload.op` 同为 `str`。值域由治理层 `emit_updated` 校验（S2-1 T6 锁定） |
| `reason` **必填** | INV-G6「可审计追踪」的审计内容本身（谁在何因下放宽/启用）；设为可选会让"无理由的治理动作"合法化 |
| `added` 可选（`default_factory=list`） | `op=add/enable` 时为空列表；与 `ScopeUpdatedPayload.added` 同型 |
| 注册行放 E 组尾部 | 与 `config.updated` 相邻；`TRANSIENT_TYPES` 不动（`policy.updated` 是**持久**事件） |

---

## 4. 测试

### 4.1 事件相关测试（先跑）

```
tests/unit/test_events.py · tests/unit/test_governance_policy.py · tests/invariants · tests/unit/test_persistence.py
→ 全绿
```

覆盖：词表 74 名与 `EXPECTED_ALL` 集合相等（**锁死变更**）· 强同步族成员资格 · 瞬态清单未变 · `SessionStore.SYNC_TYPES == vocab.SYNC_TYPES` · INV 骨架。

### 4.2 全量回归

| 指标 | S2-1 基线（`327a87b`） | **S2-2.1 后** | 变化 |
|---|---|---|---|
| collected | 1466 | **1468** | **+2** |
| passed | 1464 | **1466** | **+2** |
| skipped | 2 | 2 | — |
| failed / error | 0 / 0 | **0 / 0** | — |

**+2 来源**：`test_governance_policy.py` 由 14 例 → 16 例（T6b 拆分 + T6c 新增 = +1；T10 新增 = +1）。
**留证**：`tmp/baseline/junit_s2_2.xml` · `tmp/baseline/pytest_s2_2.log`（`tmp/` 已忽略）。

### 4.3 运行时事实（实测）

| 项 | 值 |
|---|---|
| `len(EVENT_TYPES)` | **74**（73 → 74） |
| `len(SYNC_TYPES)` | **12**（11 → 12） |
| `is_registered("policy.updated")` | **True** |
| `"policy.updated" in SYNC_TYPES` | **True** |
| `payload_model_for("policy.updated")` | `PolicyUpdatedPayload` |
| `TRANSIENT_TYPES` 规模 | **3**（未变） |

---

## 5. 风险与残留

| # | 项 | 等级 | 说明 |
|---|---|---|---|
| **R1** | **2 处硬编码 sync 副本仍不一致**（`engine.py:641` `_SYNC`、`desktop/sessions.py:151` 内联 tuple） | 中 | 本步**禁止**改这两处 → 词表已含 `policy.updated` 而两副本未含。**非持久性缺口**：`session.append`（`session.py:321`）对 `SYNC_TYPES` 成员**独立 `_flush`**，`persistence.py:280` 直接复用词表 → 该事件仍被强同步落盘；两副本只影响其传给 `store.append` 的 `sync=` 提示位。**须在 S2-4 收敛** |
| **R2** | `reason` 空串会被 payload 拒（`min_length=1`），抛 EVT-100 | 低 | 与 INV-G6 一致；`emit_updated` 的默认值非空，无实际触发路径 |
| **R3** | 词表规模注释的内部算术仍不闭合（`57 + 14 = 71 ≠ 74`） | 低（文档） | **既有**不一致（改前 `57 + 13 = 70 ≠ 73`）。本步只把**头部计数**校正为 74 并把 `policy.updated` 记入扩展项，**未重写该句的记账结构**（避免引入新的不准确）。残留登记，建议随 S6 的文档一致性校验统一处理 |
| **R4** | 治理层仍未接线 | — | `ctx.governance` 未挂载（S2-3），`emit_updated` 的调用方尚未存在 → **本步对生产行为零影响** |

---

## 6. 边界确认

| 禁止项 | 状态 |
|---|---|
| 未进入 S2-3（装配切换） | ✅ |
| 未修改 `engine.py` | ✅ |
| 未修改 `scope.py` | ✅ |
| 未修改 `desktop/sessions.py` | ✅ |
| 未修改 `tools_guard.py` | ✅ |
| 未修改 `governance/policy.py` | ✅ |
| 未创建新的事件类型（只加 `policy.updated`） | ✅（`EVENT_TYPES` +1） |
| 未接入 `ctx.governance` | ✅ |
| 未创建 Decision / Receipt / Evidence / Audit | ✅ |
| 未实现 `authorize()` | ✅ |
| 未修改 ADR-018 / `ARCHITECTURE_DECISION_RECORD.md` / ADR-020 | ✅ |
| **未 commit / 未 push** | ✅ |

**本步产物**：4 个文件改动 + [S2-2.1_REGISTRATION_DESIGN.md](S2-2.1_REGISTRATION_DESIGN.md) + 本报告。

**等待人工 checkpoint。** 下一步按计划为 **S2-3**（装配切换：`engine.py` → `PolicyEngine.from_config(...)` + 挂 `ctx.governance` + 一致性测试）——这是 S2 唯一的高风险步。
