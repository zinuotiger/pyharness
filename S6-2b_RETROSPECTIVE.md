# S6-2b_RETROSPECTIVE.md — S6-2b 阶段回顾（INV-01 ~ INV-05）

> **阶段**：S6-2b（不变量测试面）**已冻结**（锚点 **`a0c0917`**）
> **日期**：2026-09-15 ｜ `main...origin/main [ahead 43]` ｜ worktree **clean**
> **性质**：**回顾与反思**。本文件**不修改任何代码 / 测试 / Registry / Event Schema**；**未进入 INV-06**；**未 commit、未 push**（生成后等授权）。
> **实测规模**（`4f05c95` S6-1 Freeze → `a0c0917`）：**24 文件、+5851 / −4** —— 其中 **测试代码 +1073** 行 · **生产代码 +15** 行 · **文档 +4763** 行。

---

## 1. INV-01 ~ INV-05 的设计目标

这五条不变量不是"测试覆盖率指标"，而是**把架构承诺变成可证伪命题**。每条都回答一个"如果它坏了，系统会以什么方式静默失效"的问题。

| INV | 设计目标（要钉死的架构承诺） | 它防的失效模式 | 为什么必须是"不变量"而非"功能测试" |
|---|---|---|---|
| **INV-01** 日志只追加 / 历史必由日志派生 | 事件日志是**唯一真源**；一切历史（消息/统计/UI/索引）都是**派生**，可整体丢弃重建 | 模块自维护内存历史 ⇒ **模型所见 ≠ 日志**；崩溃后"记忆"与日志分叉，且**无人察觉** | 它是"可审计可回放"叙事的**物理前提**；一旦有第二写口，审计即失信 |
| **INV-02** 无绕过 agent-loop 直调 llm | 全系统 `llm.chat` 的**唯一合法调用方 = agent-loop** | 任一模块直调模型做"小任务" ⇒ **无闸调用**（烧钱）且输出**绕过护栏段** | 三闸（轮数/预算/取消）只在循环内；绕过循环 = 绕过全部闸 |
| **INV-03** rebuild 与缓存一致 | 派生缓存**可随时整体丢弃重建**，且与增量结果逐事件一致 | 审批被拒后缓存仍留旧上下文 ⇒ **下轮基于过期事实行动**；崩溃恢复后半新半旧 | 它是 INV-01"唯一真源"的**可执行形式**：不能重建的派生即等于第二真源 |
| **INV-04** 无 guard 事件即非法执行（含单调拒绝 / 无 bypass） | `guard` 是**唯一且不可绕过**的判定点；每次执行前必有求值事实；拒绝**不可翻回** | ① 插件绕 `execute` 直调低层 API ⇒ **副作用已发生但无拦截记录**（抵赖成立）；② 存在任一放行点 ⇒ **安全强度 = 最松一环** | 安全属性只能用**结构断言**证明（"不存在某 API"），行为测试无法穷举绕过路径 |
| **INV-05** 拒绝后零副作用 | 任意拒绝后 **Provider 调用计数 = 0**、无 `tool.result`；拒绝事实**强同步可证** | `reject` 后仍执行 ⇒ **日志说拒、文件其实被删** —— 审计与真实世界分叉 | 单调性的**全部价值**押在"拦了且没执行"可证之上 |

**共同的设计意图**：这些不变量**大多不是"功能对不对"，而是"结构上可能不可能"**（无第二写口 / 无 bypass API / 无绕过路径 / 无第二真源）。这正是它们必须用**静态扫描 + 结构断言**验证的原因。

---

## 2. 验证方法与工程价值

### 2.1 判定口径（本阶段最有价值的单项决策）

> **"若该子性质被破坏，对应测试是否必然失败？"** —— **不因"测试存在"自动判 `covered`**。

这条口径迫使每次标注都回答一个**可操作的问题**，而不是"有没有测试"。它直接产出了三类诚实结论：

- INV-01 的 2 项 `partial`（不是"有测试就算过"）
- INV-04 的 A4/A5 从 `partial` 升为 `covered`（因为补齐了 4 分支与双闸扫描）
- INV-05 的 G-3 从 `partial` **更正为 `covered`**（因为发现证据其实已存在）

### 2.2 四重证据法（对每条关键性质）

| 层次 | 手段 |
|---|---|
| **静态** | AST 扫描（**非 grep**）+ 文件级白名单**逐条写明理由** + **双向校验**（未登记 / 过期）+ `SyntaxError` **响亮失败** |
| **行为** | **真实装配优先**：真实 `SessionLog` / `SessionStore`（文件真源）/ `EventBus` / `GovernanceContext` / `ApprovalProvider`；**替身只用于注入边界**，不 mock 内部 |
| **反证（mutation）** | 每条关键性质配 **1 行级专属 mutation**：**执行 → 验证 RED → 立即还原 → 核验无残留**（`grep -c MUTATION` = 0、`git status` 干净） |
| **全量回归** | 每阶段跑全量并核对 **collected / failed / skipped / schema** 四项，逐字比对基线 |

**本阶段实际执行 mutation 14 次**，并**三次拒绝执行"不自然的 mutation"**（M-A7 跨函数生命周期移动、M-6 同步/异步跨层注入、T-3 专属需人为引入非确定性），均**书面说明理由**而非强凑 —— 这是"验证的诚实性"本身。

### 2.3 两项工程方法沉淀

1. **判据自检**：每个静态/字节级判据都配一条"**该判据真能识别违约**"的自检用例（在 `tmp` 合成输入上验证假阴/假阳）。⇒ 判据本身也是被测试的对象。
2. **失败可诊断性设计**：断言消息**点名被违反的子性质**；"前置条件失败"与"不变量违约"措辞**显式区分**；`OSError`/`PyHError`（环境/实现）与 `AssertionError`（性质）**异常类型不同**。⇒ 失败时能分清**实现缺陷 / 测试假设错误 / 环境问题**。

### 2.4 工程价值（可核验的产出）

| 价值 | 证据 |
|---|---|
| **发现并修复 1 条真实 P0 违约** | INV-02：`auto_title` 越过 Agent Loop 直调 `llm.chat`（`engine.py:805-806` 生产接线）⇒ 根因是 F042 指定的 `mini` 出口**缺失** ⇒ 裁定 + 最小修复（**生产代码仅 +15 行**）+ 四重验证 + 反证 |
| **两条规范范围裁定** | **F-1**（`tool.error` 分执行前/执行后失效）· **F-2**（拒绝事实**按来源分列**：guard 链 ⇒ `guard.rejected`；审批 ⇒ `approval.*`）—— 均**拒绝创建统一事件**，避免语义被摊薄 |
| **消灭编号歧义** | S6-1 的 4 个语义族清理 + Registry 建立；本阶段所有用例**只以 Registry 为语义来源** |
| **建立可回归的结构防线** | INV-01 静态写路径扫描 · INV-04 双闸扫描（`lookup_provider` + `.handle(`）· INV-02 出口类别边界 |
| **诚实暴露 3 处自身错误** | ① INV-05 的 G-3 误判（审计只查了 executor 侧）· ② INV-04 T-2 初版假阴性（executor 用裸可调用而非 `.handle(`）· ③ 总结的用例合计数（38 → 31）。**全部留痕并更正**，未隐藏 |

---

## 3. 保留设计与过度工程风险

### 3.1 值得**保留**的设计（已证明有效）

| # | 设计 | 为什么值得保留 |
|---|---|---|
| **K-1** | **单一测试文件逐条扩展**（`test_inv_core.py`） | 编号化用例集中 ⇒ 一处即可通读全部核心不变量；避免"第二套 INV 测试" |
| **K-2** | **真实装配优先，替身只用于注入边界** | 本阶段两次关键发现（T-2 假阴性、G-3 误判）都源于对"真实路径形态"的追问；mock 内部会掩盖它们 |
| **K-3** | **静态扫描三件套**（AST + 白名单附理由 + 双向校验） | 让"结构承诺"可回归；双向校验同时防漏登记与防白名单过期 |
| **K-4** | **判据自检** | 防止"判据写错但测试全绿"这一类最隐蔽的假绿 |
| **K-5** | **mutation 纪律（专属 + 1 行级 + 立即还原）** | 把"这条测试有没有鉴别力"从主观判断变成可执行证据 |
| **K-6** | **`Status` 与 `Coverage Gap` 分离记账**（Registry 设计） | 避免"因为测试存在就自动判 covered"；本阶段正是靠它抓到 INV-01 的 2 项 partial |
| **K-7** | **定义-实现范围不一致有固定处置范式**：先裁定口径 → 走 Registry §6 变更记录 → 再落测试 | F-1 / F-2 均按此走；既有历史留痕（不原地改写），又让定义与实现对齐 |

### 3.2 **过度工程风险**（诚实评估，含建议）

| # | 风险 | 实测证据 | 评估 |
|---|---|---|---|
| **OE-1** | **文档 / 代码比严重失衡** | 本阶段 **文档 +4763 行 vs 测试代码 +1073 行 ≈ 4.4 : 1**（生产代码仅 +15 行）。18 份阶段文档共 4704 行 | ⚠️ **真实风险**。项目整体文档/代码本就约 1:1，"先出计划、再做、再自审、再改写总结"的**每步三份文书**把比例推到 4.4:1。**信息重复度高**：同一组数字（23 函数 / 31 用例 / 14 mutation / 1702 passed）在 COVERAGE_AUDIT、CHANGE_REPORT、FINAL_REVIEW、SUMMARY 中**各出现一次** |
| **OE-2** | **同一事实多处维护 ⇒ 出错面放大** | 用例合计数在 4 处出现，**4 处同时错成 38**；修正需 4 次编辑 | ⚠️ 与 OE-1 同源。建议：**单一数字源**（一处定义，他处引用） |
| **OE-3** | **测试夹具的轻微重复** | 本阶段自建 `_Sess` / `_Prov` / `_inv05_scope` / `_FakeApproval`，与 `tests/unit/test_tools_executor.py` 的同类夹具形态相近 | ✅ **可接受**。选择"不跨测试模块 import"换取了**低耦合**；代价是约 60~80 行重复。若未来出现第 3 处，应上提到 `conftest.py` |
| **OE-4** | **参数化矩阵的边际收益递减** | INV-05 T-1 的 5 类来源中，scope-hidden / guard / critical **三者最终都汇入同一条 `_append_rejected`** | ✅ **仍值得保留**。三者的**触发机制不同**（scope 前置 / guard `match` / danger 分级），覆盖的是**不同分支的入口**，非重复执行同一路径 |
| **OE-5** | **白名单的维护负担** | INV-01 写路径白名单 7 条 · INV-04 双闸白名单 2 条 | ✅ **可控**。双向校验会把"过期条目"变成 RED，不会静默腐烂 |

> **前缀说明**：**`OE-*`（Over Engineering）为本 Retrospective 专用前缀**，与 `S6-2b-4_CHANGE_REPORT.md` §9 登记的 **Technical Debt `O-1`**（`tools_executor._reject` 死代码）**无关**，两者**不得互相引用**。

**建议（供 INV-06 起采纳）**：
1. **数字单一源**：阶段核心数字只在 `*_CHANGE_REPORT.md` 定义一次，其余文档**引用**而非复述；
2. **合并 FINAL_REVIEW 与 CHANGE_REPORT 的重复段**（如 INV-05 的覆盖矩阵在两者中都完整出现）；
3. **报告模板瘦身**：保留"四重证据 / mutation 表 / 残余表"三张核心表，其余表格按需出现。

> **关于 Residual 事项的边界**：Residual 状态**以 `S6-2b_FINAL_SUMMARY.md` §7 为单一来源**，本 Retrospective **不重复维护状态账本**（这正是 **OE-2** 所警示的"同一事实多处维护"的直接应用）。

---

## 4. 对后续 Agent Runtime 架构演进的影响

| # | 影响 | 说明 |
|---|---|---|
| **A-1** | **"出口类别"取代"例外名单"成为 LLM 接入纪律** | INV-02 的修复不是加例外，而是确立 **对话出口**（`chat`/`chat_stream`，唯一合法调用方 = agent-loop）与 **系统工具出口**（`mini`/`summarize`/`json_chat`）的**类别边界** ⇒ 后续新增任何 utility LLM 调用只需落进类别，**无需改不变量** |
| **A-2** | **拒绝了"统一 rejection event"** | F-2 明确：guard 链与审批链**各自留痕**（`guard.rejected` / `approval.*`），**不引入统一事件**。这保住了"规则级 vs 治理级"的分层，避免用一个大事件把两类语义摊平 |
| **A-3** | **暴露了"三闸绑定在循环而非出口"的结构问题（KF-B）** | 实测：`llm.py` 全链**无任何准入判定**；三闸只在 `agent_loop.py:197-199`。⇒ 任何非循环 LLM 调用（`summarize` / `json_chat` / `mini`）天然不过闸。**这是 runtime 演进必须回答的设计问题**（而非某个函数的 bug） |
| **A-4** | **"执行面唯一性"有了静态验证范式** | INV-04 的双闸（获取闸 `lookup_provider` + 调用闸 `.handle(`）给出了"新增 seam 时如何守住唯一执行入口"的可复制做法 ⇒ 未来扩展 capability 层时可直接沿用 |
| **A-5** | **Registry 成为可演进的定义源** | 本阶段首次**实际修改** Registry（`5cd66c9`，仅 INV-05 定义一处 + §6.1 变更记录），并证明该流程可用：**定义可以演进，但历史不可磨灭** ⇒ S6 后续（INV-06~09）与 S7 有了既定的定义维护路径 |
| **A-6** | **S7 `TraceabilityMatrix` 的直接前驱** | 本阶段的"双份存档（`raw_args`/`args`）· 因果链（`decision.issued` ↔ `guard.evaluated` 经 `call_id`）· 证据可重放"三条，正是 S7 追溯矩阵要消费的原料；INV-06（args 一致性）是其最后一块拼图 |

---

## 5. 可用于作品集与面试的技术亮点

> 选取标准：**可验证、可复现、有数字、含判断**（而非"我用了 X 框架"）。

### 5.1 十个可讲亮点

| # | 亮点 | 一句话 | 可展示的证据 |
|---|---|---|---|
| **HL-1** | **把架构承诺变成可证伪命题** | 9 条核心不变量，其中 5 条已编号化为 **23 个测试函数 / 31 条执行用例**，每条标注 `covered / partial / gap / deferred` | `tests/invariants/test_inv_core.py`（1073 行）+ Registry 状态表 |
| **HL-2** | **用"必然失败"口径替代"有没有测试"** | 判定口径 = "若该性质被破坏，对应测试是否必然失败" ⇒ 主动标出 INV-01 的 2 项 partial，**拒绝自我美化** | 各 `*_FINAL_REVIEW.md` 的逐子性质表 |
| **HL-3** | **发现并修复一条真实 P0 违约（含完整证据链）** | `auto_title` 越过 Agent Loop 直调 `llm.chat`；根因是规格指定的 `mini` 出口**在实现中缺失** ⇒ 修复**只用 +15 行生产代码** | `S6-2a-P0-F_*`；`docs/KEY-FINDINGS.md` 附录 A（KF-A `CLOSED`） |
| **HL-4** | **"类别式边界"而非"例外名单"** | 修复方案被刻意设计为**新增出口类别**（可静态校验），而不是给某个调用方开例外 ⇒ **不扩大不变量，而是消除歧义** | `test_inv02_*`（3 静态 + 2 行为） |
| **HL-5** | **14 次 mutation 验证 + 3 次"拒绝执行不自然的 mutation"** | 每条关键性质配 1 行级专属 mutation（RED 后立即还原）；**对代价过高/需人为引入非确定性的，书面说明理由而不强凑** | 各 `*_CHANGE_REPORT.md` 的 mutation 表 |
| **HL-6** | **两次规范范围裁定（F-1 / F-2）** | 面对"实现与字面不符"，判定为**定义范围问题**而非缺陷，并给出**按来源/按阶段分列**的精确口径 | `S6-2b-3/4_COVERAGE_AUDIT` · Registry §6.1 变更记录 |
| **HL-7** | **静态扫描的白名单工程** | AST（非 grep）+ 文件级白名单**逐条写明理由** + **双向校验**（防漏登记 + 防白名单过期）+ `SyntaxError` 响亮失败 | `test_inv01_no_second_message_history_write_path` · `test_inv04_provider_*` |
| **HL-8** | **判据自检（测试的测试）** | 每个静态/字节级判据都配"该判据真能识别违约"的自检用例，在 `tmp` 合成输入上验证假阴/假阳 | `test_inv01_append_only_predicate_detects_rewrite` · `test_inv01_write_scan_detects_rogue_writer` |
| **HL-9** | **系统性地清理"历史编号漂移"** | 实测全库 `INV-0x` 812 次、**4 个语义族**冲突 22 处；建立 Canonical Registry + `Legacy Mapping`（**Registry §2 中的 L-1~L-10**），**保留全部历史语义** | `docs/INVARIANT_REGISTRY.md` · `S6-1_INV_CLASSIFICATION.md` |
| **HL-10** | **公开记录自己的 3 处错误并更正** | ① G-3 误判（只查了 executor 侧）② T-2 初版假阴性（executor 用裸可调用）③ 合计数 38→31。**均在文档中留痕、加 Revision，而非静默改动** | `S6-2b-4_COVERAGE_AUDIT.md` 文首 `Revision / Correction` · 本文件 §2.4 |

### 5.2 三个"面试深挖"准备

| 追问 | 回答要点 |
|---|---|
| **"你怎么知道这些不变量真的没被破坏？"** | ① **四重证据**（静态扫描 / 真实装配行为 / mutation 反证 / 全量回归）；② **判据自检**排除假绿；③ 对无法自然反证的性质（如确定性），**明说限制**（仅进程内）而不外推 |
| **"发现 bug 后你怎么保证没改错？"** | 以 KF-A 为例：**先裁定**（是"出口选择"违约，不是"功能不该存在"）→ **再定最小边界**（不引入例外、不夹带 F042 其他偏离）→ **修复** → **四重验证 + 反证自检** → **全量回归 + schema 冻结值不变**（`77/14/3`）→ **在 KEY-FINDINGS 留痕**（含精确的 CLOSED 边界声明） |
| **"这套不变量体系对工程有什么实际价值？"** | 它把"可审计 / 可回放 / 越权必拦"从**口号**变成**可执行断言 + 可回归防线**；并且**真实抓到了一条生产违约**（INV-02）—— 不是纸面制度 |

### 5.3 一句话总结（作品集版）

> **把 9 条架构不变量中的 5 条（INV-01~05）转成 31 条可执行断言，用"必然失败"口径逐条证伪，以 14 次 mutation 反证测试自身的鉴别力；在此过程中真实发现并修复了一条 P0 生产违约（LLM 出口误用），并把两次"定义与实现不符"的争议沉淀为可演进的规范口径。**

---

## 6. 边界声明

**本文件性质**：**回顾与反思**（只读整理）。
**未做**：❌ 修改代码 / 测试 / Registry / Event Schema · ❌ 处理 KF-B / FC-8 · ❌ 进入 INV-06 · ❌ commit · ❌ push。

**worktree**：`?? S6-2b_RETROSPECTIVE.md`（仅本文件）；**HEAD 仍 `a0c0917`**，ahead 43。

---

**S6-2b Retrospective 结束。未进入 INV-06，等待授权。**
