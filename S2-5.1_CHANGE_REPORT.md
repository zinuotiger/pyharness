# S2-5.1_CHANGE_REPORT.md — `scope.updated` 面确认 + 注释一致性修复（实施报告）

> **阶段**：S2-5.1（按 [S2-5.1_STAGED_DIFF_PLAN.md](S2-5.1_STAGED_DIFF_PLAN.md) 落码；X-events = **Option α**）
> **日期**：2026-09-14 ｜ **基线 HEAD**：`806a82e`
> **性质**：**未 commit**。**零行为变更**——仅注释 / docstring / 文档文本订正。

---

## 0. 结论

# S2-5.1 = PASS

**改动 15 文件（+122 / −28）**；全量回归 **1485 collected / 1483 passed / 2 skipped / 0 failed**（较 S2-4.1 基线 1481/1479 **+4**，即 T-1~T-4）。

### 两项被要求的明确声明

> **① `events/*` 仅文本变更** —— 涉及 `events/{vocab,payload,__init__}.py` 三个文件，**全部为注释与模块 docstring**；`_CORE_EVENT_TYPES` / `SYNC_TYPES` / `TRANSIENT_TYPES` / payload 模型 / `__all__` 再导出 **逐字未动**。
> **② 行为面零变化** —— **最强证据**：对全部 **9 个被改的源码 `.py`** 做"**剥离 docstring 后的 AST 逐字比对**"，结果 **9/9 `SAME`**（唯一 `DIFF` 是 `tests/unit/test_events.py`，因**新增 4 个测试函数**，属预期）。

---

## 1. 修改文件清单与 diff summary

```
 CODE-MATRIX.md                   |  1 +          ← B-15 加注(不改数)
 README.md                        |  2 +-         ← B-13 (73→74 + 测试数)
 docs/EVENT-SCHEMA.md             |  4 +-         ← A-2 勘误 + B-14
 docs/specs/README.md             |  2 +          ← C-2 加注
 docs/specs/events.py.md          |  2 +          ← C-2 加注
 pyharness/bus/event_bus.py       |  6 +--        ← B-5/B-6/B-7(注释)
 pyharness/cli.py                 |  2 +-         ← B-8(注释)
 pyharness/core/approval.py       |  4 +-         ← B-9/B-10(注释)
 pyharness/core/scope.py          |  9 ++--       ← A-1 勘误(注释)
 pyharness/core/tools_guard.py    |  4 +-         ← B-11/B-12(注释)
 pyharness/core/tools_registry.py |  2 +-         ← **B-16(守卫抓出的人工遗漏)**
 pyharness/events/__init__.py     |  4 +-         ← B-3/B-4(docstring)
 pyharness/events/payload.py      |  2 +-         ← B-2(docstring)
 pyharness/events/vocab.py        |  8 ++--       ← B-1(注释 + 算术 14→17)
 tests/unit/test_events.py        | 98 ++++++...  ← T-1~T-4 新增 + 头 docstring 订正
 15 files changed, 122 insertions(+), 28 deletions(-)
```

**分类**：源码注释/docstring **10 文件**（其中 `events/` 3 个）· 活文档 **2**（README / EVENT-SCHEMA）· 历史快照加注 **1**（CODE-MATRIX）· 契约加注 **2**（specs）· 测试 **1**（含新增 4 用例）。

---

## 2. 逐项落地对表

| 项 | 位置 | 处置 | 内容 |
|---|---|---|---|
| **A-1** | `core/scope.py`（偏离 4） | **勘误** | 原"**尚未入 events 词表**（57 锁定类型之外）、会 **EVT-102 拒写**"→ 改为"**已入词表（现 74 型），正常落盘**；原立论已失效（历史阶段门）"。**保留** fire-and-forget 说明 |
| **A-2** | `docs/EVENT-SCHEMA.md` §3.6 | **勘误** | 原"4 型**尚未入词表**（73 型）→ 一律 EVT-102 拒写；版本 73→77"→ 改为"**`policy.updated` 已于 S2-2.1 注册**（73→**74**，并入 `SYNC_TYPES`，见 ADR-020）；**其余 3 型仍预登记**；版本目标 **74→77**" |
| **B-1** | `events/vocab.py:178` | **改 + 算术** | 头部 74 保留；"57 A-E 分组**权威**"补标 **[基线引用,非当前计数]**；"词表外扩展 **14 项**"→ **17 项**（74−57=17，**闭合算术**） |
| **B-2** | `events/payload.py:4` | **改** | "57 事件词表" → "**74 事件词表（72 payload 模型）**" |
| **B-3/B-4** | `events/__init__.py:3,10` | **改** | 57 → **74**（`:10` 补"72 payload 模型"） |
| **B-5/B-6/B-7** | `bus/event_bus.py:106,114,143` | **改** | 57 → **74** |
| **B-8** | `cli.py:619` | **改** | 57 → **74** |
| **B-9/B-10** | `core/approval.py:38,78` | **改数字** | 57 → **74**；两处**陈述本身正确**（`extra="forbid"` 锁定语义、`trust_*` 确未注册）→ 只改数字 |
| **B-11/B-12** | `core/tools_guard.py:30,38` | **改数字** | 57 → **74**；`guard.disabled` **确未注册** → 陈述正确，只改数字 |
| **B-13** | `README.md:4` | **改** | 事件词表 73 → **74**；测试数 `1,400 passed / 2 skipped` → `1,481 collected / 1,479 passed / 2 skipped`，并标注"**快照 2026-09-14；现行值见 `docs/baseline/`**"（防再次腐化） |
| **B-14** | `docs/EVENT-SCHEMA.md:101` | **改** | 57 → **74**；F 组状态更新（`policy.updated` **已注册**、余 3 型预登记） |
| **B-15** | `CODE-MATRIX.md:37` | **加注（不改数）** | 原"64 类型"**保留**；追加"后续演进(2026-09-14)：已增至 **74 型**（强同步 12）；本文件为 2026-09-07 历史快照，现行值见 `docs/baseline/BASELINE_REPORT.md`" |
| **B-16** | `core/tools_registry.py:86` | **改数字**（**守卫抓出**） | `TOOL_REGISTERED` 注释"**57 词表外**"→ **74 词表外**。经实测 `tool.registered` **确未注册**→ 陈述正确，只改数字。**此项不在我人工扫描的清单里**，是 T-4 守卫跑出来的 |
| **C-2** | `docs/specs/events.py.md:6` · `docs/specs/README.md:24` | **加注** | "57 个事件负载"**保留**（基线引用），各追加"注(2026-09-14)：57 为基线；后续已演进至现行 `EVENT_TYPES = 74`，见 `docs/EVENT-SCHEMA.md` §3 与 `docs/baseline/`" |
| **附加** | `tests/unit/test_events.py` 头 docstring | **订正** | "64 名全量核对:57 词表…" → "**74 名**（基线 57 + 扩展 17）"；并修正**已失效的测试名引用**（`test_vocab_full_64` 已在 S2-2.1 更名为 `test_vocab_full`） |

---

## 3. 白名单记录（Y-5 要求"必须记录原因"）

| 白名单模式 | 放行原因 | 落入位置 |
|---|---|---|
| `57` + "**A-E 分组 / 权威**" | **基线引用**——"§3 的 57 A-E 分组"是 `PARAMETER-ANCHOR` 的**基线名**，非当前计数 | `events/vocab.py:178` |
| 数字后接"**类型已锁定**" | 描述的是 payload `extra="forbid"` 的**锁定语义**（陈述正确）→ 只改数字 | `approval.py:38` · `tools_guard.py:30` |
| "**未入词表**"且实测为真 | 陈述**当前仍正确**（`approval.trust_*` · `guard.disabled` · `tool.registered` 确未注册）→ 只改数字 | `approval.py:78` · `tools_guard.py:38` · `tools_registry.py:86` |

**守卫实现**（`test_s25_no_stale_vocab_count_in_source`）：用 `tokenize` 扫 `pyharness/**/*.py` 的 **COMMENT** token，正则 `(?<![\w-])(\d{2,3})\s*(?:个)?\s*(?:事件|词表|核心类型)` 命中后要求数字 ∈ `{74}`；命中点 ±60 字符含白名单词（基线 / A-E / 权威 / PARAMETER-ANCHOR）则放行。**白名单原因已写入测试源码注释**。

> **不做全局替换**（Y-5）：全部 15 处均为**逐处判定**；`57` 在基线语境下**保留**，仅在当前计数语境下订正。

---

## 4. 测试结果

| 项 | 命令 / 内容 | 结果 |
|---|---|---|
| 相关测试 | `pytest tests/unit/test_events.py tests/unit/test_engine.py tests/unit/test_persistence.py` | **全绿** |
| **全量** | `pytest`（可运行集） | **1485 / 1483 passed / 2 skipped / 0 failed**，exit 0 |
| 新增用例 | **T-1** `scope.updated`/`budget.paused` **确已注册**（封 P-1 根因）· **T-2** `SYNC_TYPES`=12 / `TRANSIENT`=3 · **T-3** ADR-020 Q1 分工冻结（通道差异 + `op` 值域互斥 + 字段面各守其域）· **T-4** 注释扫描守卫 + 白名单 | **4 新增**（较基线 +4） |
| 留证 | `tmp/baseline/junit_s2_5.xml` · `tmp/baseline/pytest_s2_5.log` | — |

*2 条 skip = 既有平台相关（`test_spill`），与本次无关。*

---

## 5. 边界确认

| 项 | 状态 | 证据 |
|---|---|---|
| **行为面零变化** | ✅ | **9 个源码 `.py` 剥离 docstring 后 AST 逐字相同**（脚本核验，9/9 SAME） |
| `events/*` 仅文本变更 | ✅ | 3 个 `events/` 文件仅动注释/docstring；`_CORE_EVENT_TYPES`/`SYNC_TYPES`/`TRANSIENT_TYPES`/payload 模型/再导出 **未动** |
| 未改任何 Python 逻辑 | ✅ | AST 比对 + 全量回归绿 |
| 未改 `_CORE_EVENT_TYPES`/`SYNC_TYPES`/`TRANSIENT_TYPES`/payload 字段/dataclass/import-export | ✅ | 同上 |
| 未改 `persistence.py` / `session.py` **（任何内容）** | ✅ | 两者**根本未出现在改动清单** |
| `bus/event_bus.py` **仅动 3 处注释**（B-5~B-7） | ⚠️ 见下注 | 该文件**在改动清单内**，但 AST 剥离 docstring 后 `SAME`（已证零代码变化） |
| 未改 `governance/*` / 任何 ADR / `ARCHITECTURE_DECISION_RECORD.md` | ✅ | 改动清单无这些路径 |
| `docs/baseline/*` 完全不改 | ✅ | 未出现在改动清单（Y-4） |
| `CODE-MATRIX.md` / `docs/specs/*` 原数字未改 | ✅ | 仅追加注记（Y-2/Y-3） |
| **未 commit** | ✅ | 工作区改动待 checkpoint |

> **注**：B-5~B-7 所属文件为 `pyharness/bus/event_bus.py` —— 它**在改动清单内但仅动 3 处注释**（AST 剥离 docstring 后 SAME，已证）。你执行要求 4 的"不修改 event_bus.py"与 Y-5/Y-6 的 Tier B 冲突**与此同源**（我按 Option α 处理：**禁改限定为逻辑，允许注释**）。

---

## 6. Tier C：本步**未做**的事项（按裁定）

| 位置 | 处置 | 依据 |
|---|---|---|
| `docs/baseline/*`（4 篇） | **完全不改** | Y-4（基线不可变） |
| `CODE-MATRIX.md` 原数字 | **不改数**，仅加注 | Y-3 |
| `docs/specs/*` 原数字 | **保留 57**，仅加注 | Y-2 |
| `docs/specs/events.py.md:81,286` | **未逐处加注**（由文件头的那条注统一覆盖） | 避免同文件三处重复注记 |

---

## 7. 额外发现（值得记录）

**守卫抓到我人工扫描的一处遗漏**：`pyharness/core/tools_registry.py:86` 的 `TOOL_REGISTERED` 注释含"**57 词表外**"。我此前的 grep 模式（`57 词表` 前后文不同）**漏掉了它**——T-4 守卫第一次运行即报出。这说明**文本扫描守卫对"人工清单"有实际增量价值**（而非形式主义），也印证 Y-5 选择"全局扫描 + 白名单"是对的。

---

## 8. 是否影响 ADR

**不影响，无需新建 ADR。** ADR-020 的 Q1 事件边界**未受影响**（本步只订正与它不符的注释；代码早已符合 Q1）；`ARCHITECTURE_DECISION_RECORD.md` 与 ADR-013~020 **逐字未动**。

**建议在 S2-6 报告中登记**：`scope.py` 偏离 4 的"尚未入词表"立论**已失效并完成勘误**（属**偏离登记**范畴，非 ADR 范畴）。

---

## 9. 下一步（待授权）

| 步 | 内容 |
|---|---|
| **S2-6** | S2 收敛报告（M2 判据 ①②③ 汇总 + S2 全程偏离登记 + F-SYNC-1 待办）→ 然后 checkpoint |
| （另立） | `F-SYNC-1` durability / reliability 评估（含 T5 marker 红测试） |

**等待人工 checkpoint。**
