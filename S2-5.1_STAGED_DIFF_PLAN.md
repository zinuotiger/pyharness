# S2-5.1_STAGED_DIFF_PLAN.md — 注释一致性修复的 staged diff 计划

> **阶段**：S2-5.1（**只出计划，不落码**）
> **日期**：2026-09-14 ｜ **基线 HEAD**：`806a82e`（工作区 clean）
> **依据**：[S2-5_SCOPE_UPDATED_AND_COMMENT_CONSISTENCY_DESIGN.md](S2-5_SCOPE_UPDATED_AND_COMMENT_CONSISTENCY_DESIGN.md) · Y-2~Y-6 裁定
> **性质**：**未修改任何文件**。按你的执行顺序：步骤 1（读目标文件）已完成 → 本文件为**步骤 2**。

---

## 0. ⚠️ 落码前必须先裁定的一处范围冲突

你批准了 **Y-5/Y-6**（含 Tier B 的源码注释与活文档订正），但**执行要求 4** 把 **`events/*`** 列为禁改。二者**直接冲突**——Tier B 中有 **4 处落在 `events/` 内**：

| 计划项 | 文件（均在 `events/` 内） | 性质 |
|---|---|---|
| #3 | `pyharness/events/vocab.py:178-180` | **注释块**（`_CORE_EVENT_TYPES` 上方，非注册内容） |
| #4 | `pyharness/events/payload.py:4` | **模块 docstring**（非模型定义） |
| #5 #6 | `pyharness/events/__init__.py:3,10` | **模块 docstring**（非再导出内容） |

**四处的共同点**：**全是注释/docstring 文本，不含任何语句、注册行、字段或再导出改动** → **零行为变更**。

**旁证（判定"执行要求 4 可能是模板残留"）**：该条同时禁改 `persistence.py` / `session.py` / `event_bus.py` —— 这三者是 **S2-4/F-SYNC-1 相关**文件，**与 S2-5 的目标（注释一致性）毫无关系**（本计划本来就不碰它们）。即该清单疑似沿用上一阶段的模板。

**请二选一**：
- **Option α（建议）**：把 `events/*` 的禁改**限定为"不得改任何语句 / 注册表内容 / payload 字段 / 再导出"**；**允许纯注释/docstring 订正**（即上表 4 处）→ 本计划全量执行；
- **Option β**：严格禁改 `events/*` → 上表 4 处**本步不做**，登记为**待办**（S2-6 或另立文档阶段处理）。

> 其余项（`scope.py` · `bus/event_bus.py` · `cli.py` · `approval.py` · `tools_guard.py` · `README.md` · `docs/EVENT-SCHEMA.md` · `CODE-MATRIX.md` · `docs/specs/*` · `tests/unit/test_events.py`）**均在禁改清单之外，无歧义**。

---

## 1. 逐项计划（Y-5 要求：逐处判断 + 白名单记录原因）

> 判定图例：**改** = 订正为实测值 ／ **保留+加注** = 数字是基线引用，保留但补说明 ／ **加注** = 不改原文，追加演进说明 ／ **不改** = 完全不动

| # | 位置 | 现文（摘） | 判定 | 理由 / 白名单 |
|---|---|---|---|---|
| **A-1** | `pyharness/core/scope.py:41-44`（偏离 4） | "budget.paused/scope.updated **尚未入 events 词表**（57 锁定类型之外…真实 SessionLog.append 会 EVT-102 拒写）" | **改（勘误）** | **事实错误**：两者**均已注册**（实测 `is_registered` 皆 True）。改为"**已入词表（现 74 型），正常落盘**；原立论失效，注明勘误日期"。**保留** `:37-40` 的 fire-and-forget 说明（那部分仍正确） |
| **A-2** | `docs/EVENT-SCHEMA.md:481`（§3.6 状态行） | "本组 4 型**尚未入词表**（实测 73 型）→ 一律 **EVT-102 拒写**；升词表版本（**73→77**）" | **改（勘误）** | **与 S2-2.1 交付矛盾**：`policy.updated` **已注册**（74 型，并入 `SYNC_TYPES`）。改为：4 型中 **1 型已注册**（`policy.updated`，见 ADR-020），**其余 3 型仍预登记**（`decision.issued`/`receipt.emitted`/`evidence.archived`）→ 实现前仍 `EVT-102`；版本目标改为"**74→77（余 3 型实现时）**" |
| **B-1** | `pyharness/events/vocab.py:178-180` | "**74** 事件类型…§3 的 **57** A-E 分组权威 + 词表外扩展 **14 项**…基线 57" | **改（算术）+ 保留基线 57** | 头部 74 ✓；**算术不闭合**（57+14=71≠74）→ 扩展项改 **17 项**（74−57=17）。<br>**白名单**：`57` 在"§3 的 57 A-E 分组**权威**"处 = **基线引用**（该分组是定义的基线名）→ **保留**，仅补"基线 57 + 扩展 17 = 74" |
| **B-2** | `pyharness/events/payload.py:4` | "**57 事件词表** + llm.retry" | **改** | 当前计数声明 → "**74 事件词表（72 payload 模型）**"。<br>（模型 72 ≠ 类型 74：3 个 approval 结果共用一个模型） |
| **B-3** | `pyharness/events/__init__.py:3` | "**57 事件**负载词表注册" | **改** | 当前计数声明 → 74 |
| **B-4** | `pyharness/events/__init__.py:10` | "payload.py — **57 事件**各自 payload 负载模型" | **改** | 当前计数声明 → "74 事件 / 72 模型" |
| **B-5** | `pyharness/bus/event_bus.py:106` | "不进 **57** 词表 EVENT_TYPES" | **改** | 当前计数声明 → 74 |
| **B-6** | `pyharness/bus/event_bus.py:114` | "其余 **57** 词表类型" | **改** | 当前计数声明 → 74 |
| **B-7** | `pyharness/bus/event_bus.py:143` | "词表 **57 核心类型**" | **改** | 当前计数声明 → 74（**注**：此句原意或指"核心词表"，但 74 含扩展，须写清"现 74 型"） |
| **B-8** | `pyharness/cli.py:619` | "词表 **57 类型**逐类型订阅" | **改** | 当前计数声明 → 74 |
| **B-9** | `pyharness/core/approval.py:38` | "（**57 类型已锁定**，实测超字段 append → EVT-100）" | **改数字** | 该句**陈述正确**（payload `extra="forbid"` 超字段即拒），仅"57"漂移 → 74 |
| **B-10** | `pyharness/core/approval.py:78` | "trust_* 未入 **57** 词表" | **改数字** | **陈述正确**（`approval.trust_*` **确未注册**）→ 仅数字改 74 |
| **B-11** | `pyharness/core/tools_guard.py:30` | "**57 类型已锁定**；实测：超字段 append → EVT-100" | **改数字** | 陈述正确，仅数字漂移 → 74 |
| **B-12** | `pyharness/core/tools_guard.py:38` | "guard.disabled 未入 **57** 词表" | **改数字** | **陈述正确**（`guard.disabled` **确未注册**）→ 仅数字改 74 |
| **B-13** | `README.md:4`（状态行） | "**1,400 passed / 2 skipped**，事件词表 **73 型**" | **改** | Y-6"更新当前状态描述"：词表 **73→74**；测试数 **1,400→1481 collected / 1479 passed / 2 skipped**，并标注"**快照 2026-09-14；现行值见 `docs/baseline/`**"（避免再次腐化） |
| **B-14** | `docs/EVENT-SCHEMA.md:101`（§3 阅读约定） | "词表 **57 个事件名**按 A-F 分组（F 组为治理层**预登记**…）" | **改** | 当前计数声明 → 74；**F 组状态更新**：`policy.updated` **已激活**、余 3 型仍预登记（与 A-2 一致） |
| **B-15** | `CODE-MATRIX.md:37` | "事件词表 **64 类型**（57 核心 + plan/schedule 扩展）" | **加注（不改数）** | **Y-3**：保留历史快照语义（该文件标"终版 2026-09-07"）→ 原文不动，**追加一行**"**后续演进**：截至 2026-09-14 为 **74 型**（见 `docs/baseline/BASELINE_REPORT.md`）" |
| **C-1** | `docs/baseline/*`（4 篇） | 多处"73 型 / 强同步 11" | **完全不改** | **Y-4**：带 `0cba75d` 基线标识的**时点记录**，**基线不可变** |
| **C-2** | `docs/specs/events.py.md:6,81,286` · `docs/specs/README.md:24` | "**57 个事件负载**词表注册" | **保留 57 + 加注** | **Y-2**：保持历史引用；**追加说明**"基线 57，后续版本已演进至现行 `EVENT_TYPES = 74`（见 `docs/EVENT-SCHEMA.md` §3 与 `docs/baseline/`）" |

### 1.1 白名单记录（Y-5 要求"必须记录原因"）

| 白名单模式 | 允许保留的原因 | 出现位置 |
|---|---|---|
| `57` + "**A-E 分组权威 / 基线**" | **基线引用**——"§3 的 57 A-E 分组"是**定义的基线名**（`vocab.py:178` 与 specs 均以此立论），不是当前计数 | `events/vocab.py:178` · `docs/specs/**` |
| 数字后紧跟"**类型已锁定**" | 该短语描述的是 **payload 模型 `extra="forbid"` 的锁定语义**（陈述正确），数字只是附带 → **只改数字** | `approval.py:38` · `tools_guard.py:30` |
| "**未入词表**" 且经实测为真 | 陈述**当前仍正确**（`approval.trust_*`、`guard.disabled` 确未注册）→ **只改数字** | `approval.py:78` · `tools_guard.py:38` |

> **不做全局替换**（Y-5 明令）：上表每一行都单独判定；`57`/`64`/`73` 在**基线语境**下保留，仅在**当前计数语境**下订正。

---

## 2. 修改文件清单与预估 diff

| 文件 | 项 | 预估 | 类别 |
|---|---|---|---|
| `pyharness/core/scope.py` | A-1 | **+5 / −4** | 源码注释 |
| `pyharness/events/vocab.py` ⚠️ | B-1 | **+2 / −2** | 源码注释（**Option α 才做**） |
| `pyharness/events/payload.py` ⚠️ | B-2 | **+1 / −1** | docstring（同上） |
| `pyharness/events/__init__.py` ⚠️ | B-3/B-4 | **+2 / −2** | docstring（同上） |
| `pyharness/bus/event_bus.py` | B-5~B-7 | **+3 / −3** | 源码注释 |
| `pyharness/cli.py` | B-8 | **+1 / −1** | 源码注释 |
| `pyharness/core/approval.py` | B-9/B-10 | **+2 / −2** | 源码注释 |
| `pyharness/core/tools_guard.py` | B-11/B-12 | **+2 / −2** | 源码注释 |
| `README.md` | B-13 | **+1 / −1** | 活文档 |
| `docs/EVENT-SCHEMA.md` | A-2/B-14 | **+10 / −7** | 活文档 |
| `CODE-MATRIX.md` | B-15 | **+2 / −0** | 历史快照加注 |
| `docs/specs/events.py.md` | C-2 | **+3 / −0** | 契约加注 |
| `docs/specs/README.md` | C-2 | **+1 / −0** | 契约加注 |
| `tests/unit/test_events.py` | T-1~T-3 | **+≈50 / −0** | 新增测试 |
| **合计** | | **≈ +85 / −25** | **无任何语句改动** |

**预估 `git diff --stat` 形态**（Option α）：
```
 README.md                               |   2 +-
 CODE-MATRIX.md                          |   2 +
 docs/EVENT-SCHEMA.md                    |  17 +++++----
 docs/specs/README.md                    |   1 +
 docs/specs/events.py.md                 |   3 +
 pyharness/bus/event_bus.py              |   6 +++---
 pyharness/cli.py                        |   2 +-
 pyharness/core/approval.py              |   4 ++--
 pyharness/core/scope.py                 |   9 ++++----
 pyharness/core/tools_guard.py           |   4 ++--
 pyharness/events/__init__.py            |   4 ++--      ← Option α 专属
 pyharness/events/payload.py             |   2 +-         ← Option α 专属
 pyharness/events/vocab.py               |   4 ++--      ← Option α 专属
 tests/unit/test_events.py               |  50 ++++++++++++++++++++++
 ≈14 files changed, ≈85 insertions(+), ≈25 deletions(-)
```

**Option β** 则少 3 个文件（`events/vocab.py` · `events/payload.py` · `events/__init__.py`），合计约 **+80 / −20**，且 4 项登记为待办。

---

## 3. 不修改的内容（边界）

| 禁改项 | 状态 |
|---|---|
| **任何代码逻辑**（语句 / 控制流 / 常量） | ✅ 本计划**只动注释与 docstring 文本** |
| `events/*` 的**注册表内容 / payload 字段 / 再导出** | ✅ 不动（Option α 下仅动注释文本） |
| `governance/*` | ✅ 不动 |
| 任何 ADR / `ARCHITECTURE_DECISION_RECORD.md` | ✅ 不动 |
| `persistence.py` / `session.py` / `bus/event_bus.py` **逻辑** | ✅ `event_bus.py` 仅动 3 处**注释**（B-5~B-7），不动逻辑；另两者**完全不碰** |
| `docs/baseline/*` | ✅ **完全不改**（Y-4） |
| `CODE-MATRIX.md` 的**原数字** | ✅ **不改数**，仅加注（Y-3） |
| `docs/specs/*` 的**原数字** | ✅ **不改数**，仅加注（Y-2） |

---

## 4. 文档一致性测试（新增，落 `tests/unit/test_events.py`）

| # | 用例 | 断言 |
|---|---|---|
| **T-1** | `test_scope_events_are_registered` | `is_registered("scope.updated")` 与 `is_registered("budget.paused")` **均为 True**（**封 P-1 的根因**：任何注释再写"尚未入词表"前，事实层先被钉死） |
| **T-2** | `test_vocab_channel_counts` | `len(SYNC_TYPES) == 12` 且 `len(TRANSIENT_TYPES) == 3`（`len(EVENT_TYPES) == 74` 已由既有 `test_vocab_full` 覆盖） |
| **T-3** | `test_scope_policy_event_boundary_frozen` | ① `"scope.updated" not in SYNC_TYPES`；② `"policy.updated" in SYNC_TYPES`；③ 两者 `op` 值域互斥（`ScopeUpdatedPayload` 只接受 `tighten` 语义 / `POLICY_OPS == {add,enable,disable}`）——**冻结 ADR-020 Q1 分工** |
| **T-4** | `test_no_stale_vocab_count_in_source` | **注释扫描守卫**（Y-5 的"全局扫描 + 白名单"）：扫描 `pyharness/**/*.py` 的注释与 docstring，命中形如"**词表 N 类型** / **N 事件词表** / **N 个事件**"且 **N ∉ {74}** → 失败并列出位置。**白名单**（见 §1.1）：`57` 后接 "A-E 分组 / 基线 / 权威" 者放行，并在测试内**注释记录该白名单理由** |

**验收标准**：
1. T-1~T-4 全绿；
2. 全量回归绿（基线 `1481 / 1479 passed / 2 skipped / 0 failed`）；
3. `git diff` **不含任何非注释/非 docstring/非 `.md`** 的行改动（逐文件人工核验）。

> **T-4 的降级预案**（Y-5 已预留）：若白名单仍误报，降级为"**仅断言 Tier A 两处（`scope.py` 与 `EVENT-SCHEMA.md` §3.6）已改为正确表述**"，放弃全局扫描。

---

## 5. 待你裁定（1 项）

| # | 事项 | 选项 |
|---|---|---|
| **X-events** | `events/*` 的 4 处**纯注释/docstring**订正是否放行？ | **Option α（建议）**：限定"禁改语句/注册内容/字段/再导出"，**允许注释订正** → 全量执行<br>**Option β**：严格禁改 `events/*` → 4 处登记待办 |

**裁定后即执行 S2-5.1 落码（含 T-1~T-4 测试），随后跑相关测试 + 全量回归，出 CHANGE_REPORT。**
