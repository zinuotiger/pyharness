# ADR-019：S1 范围裁定——确认 S1 实施范围为 Runtime Integrity Repair 四项，剩余项按依赖关系分流

> **本文件是 ADR-019 的正式文本。**
> **编号核验（2026-09-14 实测）**：`docs/ADD.md` 索引表实际登记 **12 条**（ADR-001~012，末条在 `ADD.md:308`）；全库扫描无 `ADR-013`~`ADR-029` 的任何其他占用。`ADR-013~018` 由 [ARCHITECTURE_DECISION_RECORD.md](ARCHITECTURE_DECISION_RECORD.md) **预留但尚未回填注册表**。故**本 ADR 取号 019**（013–018 为预留块，不得占用）。
> **登记状态**：**待登记**——本 ADR 与其 013~018 一并在 **C-2**（`ADD.md` §2 索引 + §4 正文 + 头部条数）回填。**在回填前，本文件是唯一权威文本**；回填后以 `ADD.md` 为准（避免形成并行注册表）。
> **状态**：已接受（人工裁定的记录） | **日期**：2026-09-14 | **落点**：S1↔S2 边界 | **原则映射**：G-6（文档即契约）/ G-7（范围即架构）
> **前置**：[S1_EXIT_REVIEW.md](S1_EXIT_REVIEW.md)（发现范围歧义）· [S1_SCOPE_ADJUDICATION.md](S1_SCOPE_ADJUDICATION.md)（本次裁定的分析报告）

---

## 背景

`ARCHITECTURE_DECISION_RECORD.md` §5.2 的 **S1 行**（`:411`）定义 S1 为「Phase 0 接线收口」，包含 **9 个工作项**与 **5 条出口判据**：

> 工作项：`W1–W5 五项修复 + build_runner_components 按职责拆分为 5 个子函数 + 5 处重复落盘适配器收敛 + 两套 repair 收敛 + 死代码清理（含 _invoke、死态依赖）`
> 出口判据：`① g1 validator 生效；② guards.disabled 生效；③ 预算单信号；④ 全量回归绿；⑤ AgentLoop 无 resume + hasattr 负断言`

而 2026-09-14 的 S1 实施任务书定义为 **4 项**：S1-01（`from_config` 接线）、S1-02（`paused`/`resume` 死态）、S1-03（F026 连败主链）、S1-04（close 状态一致性）——即 §5.2 的 **W1/W2/W4/W5**。

S1 Exit Review 判定为 **CONDITIONAL PASS**，其 **F-2** 指出：**存在两个"S1 既定任务"口径，范围不同**（9 工作项 vs 4；出口判据 5 条含③），冻结记录与实际执行脱节——违反 G-6。**本 ADR 即对该歧义的正式裁定**。

**歧义成因（三源）**：

1. **§5.2 的 S1 行把两类不同性质的工作混在一格**：W1–W5 是**行为修复**（有测试可证），而后 4 项是**结构性收敛/重构**（拆分、去重、删死代码）——后者的验收方式与前者不同，却共用同一格与同一套判据，为"看似做了 vs 实际做了"留出空间。
2. **任务书在实施时收窄了范围，但未同步修订冻结记录**——记录保持原样（正确，因其声明 FROZEN），于是记录与现实分叉。
3. **未走 ADR 只增不改的机制**：本应在收窄的同时新建 ADR 取代，实际未建，导致分叉无人记录（即 F-2 的实质）。

---

## 决策

**裁定 1：S1 的实施范围确认为 S1-01~04 四项**（即 §5.2 的 W1/W2/W4/W5）。S1-01~04 **构成当前 S1 的完整范围**，理由见「决策依据」。

**裁定 2：§5.2 S1 行的剩余 5 项移出 S1，按依赖关系分流（销往何处见下表），不并入本 S1、不启动 S1.5 代码修复。**

| 剩余项 | 是否 S2 硬前置 | 影响 Governance 正确性 | 可安全延后 | 承接阶段 | 需单独 ADR |
|---|---|---|---|---|---|
| **W3 预算单信号** | 否 | 间接（S5 审计读 run 终态；S2 不读） | 是 | **S1.5**（运行完整性子集） | **建议是**（改终态语义与错误码契约，触 ADR-011） |
| **`build_runner_components` 拆 5 子函数** | 否 | 否（装配可达性，非语义） | 是 | **S2 内顺手做**（S2 必改该函数挂 `ctx.governance`）；若独立则走 CND-07 | 否 |
| **落盘适配器收敛** | 否（非硬前置） | **是**（见下方「关键依赖」） | **是（移出 S1）** | **S2 内必修的收纳动作**（成本≈4 行） | 否（ADR-018 的"单一真源"纪律已覆盖） |
| **两套 repair 收敛** | 否 | 间接（repair 后日志一致性） | 是 | **S1.5 或 S6 清理**（须先对齐 `docs/specs/persistence.py.md`） | 否（若删测试专用实现） |
| **`_invoke` 死代码清理** | 否 | 否 | 是 | **S2 或 S6**（**须先处理 spec 引用**，见下） | **建议是**（`specs/tools_executor.py.md` 记载其存在，删除=改规格） |

**裁定 3：§5.2 S1 行的出口判据 ③（预算单信号）随 W3 一并移出 S1 出口，改由 S1.5 承担。** 此后 S1 的出口判据为 ①、②、④、⑤ 四条——**均已满足**（见「后果」）。

**裁定 4（本裁决的附加纠正）**：§5.2 S1 行工作项中「死代码清理（含 `_invoke`）」的表述**本身不准确**：`_invoke` 并非纯死代码，而是被 `docs/specs/tools_executor.py.md:75`（伪代码 `self._invoke`）、`:180`（函数表）记载的**规格契约残留**——实现改走 `_run_provider` 后它才成为零调用点。故其"清理"的实质是**规格漂移的处置**（更新 spec 或记录偏离），不是无害删除。**本纠正不修改任何既有 ADR 文本，仅记录该工作项的表述缺陷。**

**裁定 5：本 ADR 为增量记录，不修改既有冻结 ADR 的历史事实。**
- `ADR-001~012`（`docs/ADD.md`）与 `ADR-013~018`（`ARCHITECTURE_DECISION_RECORD.md` §2）的文本、结论、编号**全部保持原貌**；
- 本 ADR **不编辑** `ARCHITECTURE_DECISION_RECORD.md` 的任何内容（该文件 §0.1 声明 FROZEN）；对 §5.2 **S1 行**的取代关系**仅在本 ADR 中声明**，即"读 §5.2 S1 行时应并读本 ADR"；
- 依据 `docs/ADD.md:35`「ADR 只增不改，若未来推翻某决策，新建一条 ADR 记录取代关系，旧条目保留原貌」——本 ADR 即该"取代记录"，而非对旧记录的修改。

### 决策依据（为什么四项构成当前 S1）

**S1-01~04 的共同性质 = 行为修复（behavior repair）**，四者共享同一判据形态：**存在一个可复现的运行时缺陷 → 修复 → 有行为级测试证明**。

| 项 | 缺陷（可复现） | 性质 |
|---|---|---|
| S1-01 | `engine` 直构 `GuardChain` 绕过 `from_config` → g1 g-schema 恒 allow、`guards.disabled` 永不生效 | 行为修复（安全防线空转） |
| S1-02 | `LoopState` 三死态 + `agent.py` 调不存在的 `loop.resume()` | 行为修复（死代码 + 活体缺陷） |
| S1-03 | F026 连败计数只在批量入口生效 → 主链失效 | 行为修复（规格未落地） |
| S1-04 | `_closed` 先置位后校验 → close 不可重入 | 行为修复（契约不成立） |

而移出的 5 项**不共享该性质**：拆分与收敛是**结构重构**（行为不变，验收靠"等价性"而非"缺陷复现"）、`_invoke` 清理是**规格处置**、W3 是**契约变更**（ADR-011 域）。把它们与四项混作一个"S1"是本歧义的根因。

**独立验证结论**：本次裁定**不是**顺着"倾向缩小"的结论走的。对 5 项逐项做了代码依赖核验（见 [S1_SCOPE_ADJUDICATION.md](S1_SCOPE_ADJUDICATION.md) §2），结论是**没有任何一项构成 S2 的硬前置**（唯一与 S2 强相关的"落盘适配器"亦可在 S2 内以 ≈4 行解决）——**故收窄 S1 不产生依赖缺口**，裁定成立。

---

## 后果

**收益**：
1. **S1 闭环可判定**：范围 = 4 项行为修复，出口判据 = ①、②、④、⑤ 四条，**全部满足**（① `test_guard_from_config_injects_schema_validator`；② `test_guard_from_config_applies_cfg_disabled`；④ 全量 1450 passed / 0 failed；⑤ `test_agent_loop_has_no_resume_api` + `test_loop_state_space_is_reachable_only`）→ S1 门禁由 CONDITIONAL PASS **可重评为 PASS**（另需 C-2/C-3 清其余条件）。
2. **G-6 恢复**：范围歧义以 ADR 形式被裁定并记录，冻结记录与现实的差异**有了正式承接**，不再是无记录的漂移。
3. **不产生依赖缺口**：5 项均非 S2 硬前置（逐项已验证），故收窄不欠债。
4. **成本最优**：把"结构性收敛"推到它们**本来就要发生的阶段**（S2 改装配函数时顺手挂 `ctx.governance`；S2 加强同步事件时顺手收敛 sync 清单），避免为重构单独开一轮。

**代价与必须承担的后果**：
1. **S1 不再覆盖"结构性收敛"**：`build_runner_components` 将持续为 ~185 行单函数直到 S2；5 处落盘订阅适配器持续重复直到 S2/S6。
2. **W3 的两套预算信号在 S1.5 前继续并存**：`reason` 可能被判为 `error` 而非 `budget`（仅重试路径可达），且 `PyHError("BUDGET-EXHAUSTED")` 为**未登记码直构**（违 ADR-011 登记纪律，既有先例同 `BUSY`）。
3. **落盘适配器的重复在 S2 前是活跃风险**：S2 若忘记同步 2 处硬编码 sync 清单，其强同步事件在 **engine / desktop 两条主生产路径**上会退化为批量落盘 → 崩溃可能丢决策事件 → 治理审计链断裂。**此项虽非硬前置，但必须在 S2 内显式处理，不得静默略过**。
4. **本 ADR 与 ADR-013~018 一样处于"待登记"状态**：在 C-2 完成前，`docs/ADD.md` 仍只有 12 条 ADR，即 F-3 未解除（本 ADR 不解决 F-3，只是不再新增并行的*冲突*注册表——因为它声明了自己的登记归属）。

---

## 违反它的后果（具体场景）

1. **若把 5 项并入本 S1**：需引入重构类验收（等价性测试/去重验证/规格处置），与四项"缺陷复现"的验收形态混用；一旦为赶时间以"代码已改"代替"行为已证"，就会重演本 ADR 背景中"S1-01~04 被误认为等同 §5.2 S1"的同类错误——**用一次范围扩张换来一次新的验收口径混淆**。
2. **若把 5 项直接删除（既不并入也不分流）**：`_invoke` 将留下"规格记载但实现无此函数"的永久矛盾（G-6 违规）；两套 repair 的语义分歧无人收敛；sync 清单的手写副本在 S2 加事件时静默失配——**最小代价的漏改，换来最难排查的"决策未落盘"**。
3. **若不记录本裁定，仅口头/上下文约定收窄**：`ARCHITECTURE_DECISION_RECORD §0.3` 明令"禁止形成两套并行 ADR 注册表"；无记录的范围变更会使 §5.2 持续被误读为"S1 未完成"，进而**在 S2 阶段被重复要求"补 W3/补拆分"**，或反之被当作"已做过"而漏做——两种都会发生，且都无据可查。
4. **若为避免本 ADR 待登记而直接改写 `ADD.md` 只插入 019**：注册表将出现 `012 → 019` 的**编号断层**（013–018 缺位），比当前"12 条自洽"更糟，且直接违反 §0.3。

---

## 备选方案对比

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| **A. 收窄 S1 为四项 + 剩余 5 项按依赖分流 + 本 ADR 记录**（采纳） | S1 可闭环；无依赖缺口；成本最优；G-6 有正式承接 | S1 不含结构收敛；本 ADR 待登记；S2 须显式处理 sync 收敛 | ✅ **采纳** |
| B. 扩大 S1 覆盖全部 9 工作项 | 一次清干净 | 混用验收口径；重构与修复同期；S2 被推迟；违"不默认扩大" | ❌ |
| C. 直接删除 5 项、不留承接 | 最省事 | `_invoke` 留下规格矛盾；sync 清单在 S2 静默失配；无据可查 | ❌ |
| D. 保持两口径并存、仅口头约定 | 零成本 | 违 G-6/§0.3；§5.2 持续被误读；重复劳动 | ❌ |
| E. 改写 §5.2 S1 行正文 | 记录与现实一致 | 修改 FROZEN 记录，违"不可变历史"；与 ADR 只增不改冲突 | ❌ |

---

## 关联

- `ARCHITECTURE_DECISION_RECORD.md` §5.2 S1 行（**本 ADR 声明取代其对 S1 范围的表述；不修改其文本**）· §5.3（串行纪律）· §0.1（锁定项）· §0.3（禁止并行注册表）
- `docs/ADD.md:35`（ADR 只增不改）· `docs/ADD.md` §2/§4（**本 ADR 的登记目标**）
- `ADR-011`（错误码对外契约——W3 触及）· `ADR-018`（单一真源纪律——落盘适配器收敛触及）· `ADR-013`（治理层三处接缝——`ctx.governance` 装配点触及 `build_runner_components`）
- `REFACTOR_PLAN.md` §4.2/§4.3/§4.4/§5（各剩余项在 REFACTOR_PLAN 中的原始归属）· §2.3（重复代码清单）
- [S1_EXIT_REVIEW.md](S1_EXIT_REVIEW.md)（F-1~F-8）· [S1_SCOPE_ADJUDICATION.md](S1_SCOPE_ADJUDICATION.md)（逐项依赖分析）· [S1_CHANGE_REPORT.md](S1_CHANGE_REPORT.md)
- `docs/specs/tools_executor.py.md:75,180`（`_invoke` 的规格记载——裁定 4 依据）· `docs/specs/persistence.py.md`（repair 规格——收敛前置）
