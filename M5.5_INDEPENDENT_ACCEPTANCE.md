# M5.5 Independent Acceptance Verification Report

> **模式**：M5.5 Independent Acceptance Verification。**严格只读** —— 未改生产代码/测试/
> `LIMITATIONS.md`/`README.md`/方法论文档；未创建永久测试；未修复任何问题；未 commit / 未 push。
> 全部探针写入 `%TEMP%\m55\`（仓库外）。
> **仓库卫生核验**：M5.5 期间对仓库**零写入**（无 `# MUTATION` 残留；tracked 修改数 44 与进入时一致）。

**核心问题**：M5 报告的"已修复/已通过"是否与**当前仓库的真实运行时行为**一致？

---

## 二、Baseline 重锁

| 维度 | M5 报告声称 | M5.5 实测 | 一致? |
|---|---|---|---|
| collected | 2,025 | **2,025** | ✅ |
| failed | 0 | **0** | ✅ |
| errors | 0 | **0** | ✅ |
| skipped | 4 | **4** | ✅ |
| coverage | 79% | **79%**（18322/3791） | ✅ |

**无差异，无需解释。**

---

## 三、M5-1 Channel Contract（九状态 × 层）

| 状态 | 规范解析 | Service | Approval | Governance | 跨层一致 |
|---|---|---|---|---|---|
| **A missing** | fail | FAIL:APR-503 | **FAIL:APR-503** | **FAIL:APR-503** | ✅ |
| **B None** | headless | FAIL:APR-503 | `None`(headless) | `system:system` | ✅ |
| **C `""`** | fail | FAIL:APR-503 | **FAIL:APR-503** | **FAIL:APR-503** | ✅ |
| **D `desktop`** | desktop | `desktop` | `desktop` | `human:desktop` | ✅ |
| **E `acp`** | acp | `acp` | `acp` | `human:acp` | ✅ |
| **F `acp:client-7`** | **acp** | **acp** | **acp** | `human:acp` | ✅ |
| **G invalid** | fail | FAIL:APR-503 | FAIL:APR-503 | FAIL:APR-503 | ✅ |
| **H unknown(非字符串)** | fail | FAIL:APR-503 | FAIL:APR-503 | FAIL:APR-503 | ✅ |
| **I `desktop:`** | fail | FAIL:APR-503 | FAIL:APR-503 | FAIL:APR-503 | ✅ |

**`ALL STATES CONSISTENT: True`** —— 9/9 状态在 Service / Approval / Governance 三层
**同类判定**。`acp:client-7` **未**变成 `desktop` / `None` / `system`。

> 注:B(None)与 D/E/F 的 Service 层差异是**有意的** —— `ApplicationService` 是桌面服务门面,
> 需要明确的交互通道名,故 HEADLESS 被拒;而 Approval/Governance 把 `None` 当合法的
> "无人类通道"。这不是分裂:两者对 `None` 的语义**一致**(= 无人类通道),只是服务门面的
> 前置条件更严。**已在 M5 报告中说明,本轮复核确认该说明与实现一致。**

---

## 四、M5-1 两条装配路径（运行时证据）

| 观测点 | `assemble_real_engine` | `attach_engine_to_ctx` |
|---|---|---|
| `spine.channel` | `acp:client-7` | `acp:client-7` |
| `approval._channel` | **`acp`** | **`acp`** |
| agent ctx `.channel` | `acp:client-7` | `acp:client-7` |
| `_ensure_channel(agent_ctx)` | **`acp`** | **`acp`** |
| `governance.principal_of(agent_ctx)` | `Principal(HUMAN, client-7, acp)` | `Principal(HUMAN, client-7, acp)` |

**`CONSISTENT = true`** —— 非"代码看起来一样",而是**两条路径都实跑并逐字段比对**。

---

## 五、M5-2 Sub-Agent Domain Reachability

默认 `strict` 档下**真实执行** `subagent.spawn`（经 LLM tool_calls → 四关管道）:

```
sandbox_level = strict
can_use("subagent.spawn") = True
tool.call = 1 · tool.result = 1 · subagent.spawned = 1
guard.rejected = []                 ← 无 scope-hidden / GRD-401
child session exists = True (s-sub0001.jsonl)
```

**是否通过放宽 strict 实现？否。** 证据:`exec.shell_run` / `proc.start` 在 strict 下**仍** `can_use=False`
（`tests/invariants/test_inv_domain_classification.py::test_restricted_domains_are_still_restricted`
本轮复跑通过);本次只把 `subagent` 归入 `SELF_DOMAINS`（零外部副作用域），**未动沙箱档位**。

---

## 六、M5-3 Evidence Lifecycle（五终态）

| 终态 | decision.issued | segment.end | evidence.archived | duplicate | claim 中的终态 |
|---|---|---|---|---|---|
| success | 1 | 1 | **1** | **0** | `success` |
| abnormal | 1 | 1 | **1** | **0** | `success` |
| exception | 1 | 1 | **1** | **0** | `error:LLM-310` |
| budget | 1 | 1 | **1** | **0** | `error:CYC-999` |
| **cancellation** | 1 | 1 | **1** | **0** | **`cancelled`** |

**`cancellation` 使用真实 `task_queue.cancel(tid, by="system")`**（非注入 `CancelledError`）：
适配器第 2 轮永挂起 → 首轮工具已执行 → `await q.cancel(tid)` → 断言 `segment.end=1` 且
`evidence.archived=1` 且 claim 含 `cancelled`。

**两处需如实说明的语义（不是缺陷，但报告未写**：
1. **`abnormal` 被记为 `success`** —— `_run_task` 在 `await child` **正常返回**时写 `task.completed`,
   与 agent-loop 的 `reason`（`stall`）无关。故任务级终态是 `success`。**这是分层语义,不是错**:
   `reason` 是循环概念,`task.completed/failed` 是任务队列概念。
2. **`budget` 被记为 `error:CYC-999`** —— `BudgetExhausted` 落入 `_run_task` 的
   `except Exception` 兜底（非 `PyHError` 分支),故持久化记录不含 "budget" 字样。
   **BUDGET 的终态码在此路径上丢失**（agent-loop 侧有 `reason=budget`,但任务队列侧没有）。
   → 见 §C 的观察项。

---

## 七、Evidence Ordering（证据顺序）—— **与预期相反，且该顺序更强**

**实测顺序（5/5 终态一致）**：

```
decision.issued → … → segment.end → evidence.archived
```

即 **`evidence.archived` 的 seq 恒 > `segment.end` 的 seq**（`order(ev>segend)=True` 全部 5 例）。
用户预期的顺序是 `evidence.archived → segment.end`。

**为什么该顺序仍（且更）满足 Segment Terminal Completeness**：

1. 生产者**由 `segment.end` 触发**（`bus.subscribe("segment.end", _evidence_producer(...))`），
   故按构造**不可能**早于段关闭 —— 这比"尽早归档"更强:工件引用的段**在被引用时已经是闭合区间**。
2. 这**正是 N3 的修复**:旧实现（在 `run_for_task` 函数体里归档）恒**早于**段关闭,
   工件引用的是**尚未闭合**的段。现在该情形在构造上不可能出现。
3. 引用可解析性不受影响:`EvidenceRef(kind="segment")` 校验的是 `segment.start` 存在
   （`evidence.py::_resolve`),且归档时 `_task_at` 面对的是已闭合段 ⇒ 闭区间语义正确。
4. `exception` / `budget` / `cancellation` 三个异常终态**同样满足**（上表 5/5）。

**结论**:真实顺序 `… → segment.end → evidence.archived`。它满足不变量,且**严格优于**
"evidence 早于 segment.end"的写法。**M5 报告未显式说明该顺序 —— 属说明缺口,非行为缺陷。**

---

## 八、M5-4 Child Evidence（父/子各自证据链）

真实 `subagent.spawn` 路径（**非直调 API**）:

| 会话 | decision.issued | receipt.emitted | segment.start/end | **evidence.archived** |
|---|---|---|---|---|
| **Parent** | 1 | 1 | 1 / 1 | **1** |
| **Child** | 1 | 1 | **1 / 1** | **1** |

修复前实测（M4.5）:子会话 `segment 0/0`、`evidence 0`。**现已成立。**

---

## 九、`subagent.joined` 的 `notify` 四态

**修复前**:LLM 工具路径上**永不发出**（`bool(args.get("notify", True))` 因 schema 默认 `null` 恒得 `False`）。

| 情况 | 校验后 args | spawned | **joined** | failed |
|---|---|---|---|---|
| `notify` **省略** | `notify: None` | 1 | **1** | 0 |
| `notify = None` | `notify: None` | 1 | **1** | 0 |
| `notify = True` | `notify: True` | 1 | **1** | 0 |
| `notify = False` | `notify: False` | 1 | **0** | 0 |

**结论**:`notify=None` **不再**导致 `joined` 消失。**但需如实说明**:经 pydantic 校验后,
**"省略"与"显式 None"在 args 中不可区分**（都是 `None`）—— 这是 JSON-Schema 默认值为
`null` 的固有结果,不是本轮引入。当前统一语义是"`None`（无论来源）⇒ `True`"。

---

## 十、M5-5 Flush Configuration Wiring

| 配置 (interval, batch) | SessionStore 实际 | 一致? |
|---|---|---|
| (3.0, 7) | (3.0, 7) | ✅ |
| (0.05, 1) | (0.05, 1) | ✅ |
| (10.0, 1024) | (10.0, 1024) | ✅ |
| (1.25, 33) | (1.25, 33) | ✅ |
| **不传参（默认）** | **(0.5, 64)** | ✅ |

**5/5 一致**,含默认不被破坏。

---

## 十一、M5-6 复核：文档一致性

| 声称处 | 内容 | 实测 | 判定 |
|---|---|---|---|
| `LIMITATIONS.md` | `security 49 · acceptance 9 · e2e 40 · invariants 141 · integration 6` | 一致 | ✅ |
| `LIMITATIONS.md` | `2,025 collected / 2,021 passed / 0 failed / 4 skipped` | 一致 | ✅ |
| `LIMITATIONS.md` | `141 用例 / 10 文件` | 一致（实测 10 文件） | ✅ |
| `README.md` | `2,025 collected / 2,021 passed / 0 failed / 4 skipped` | 一致 | ✅ |
| `F2_REMEDIATION_REPORT.md` | `acceptance 9 / e2e 40 / integration 6` | 一致 | ✅ |
| **`F2_REMEDIATION_REPORT.md` §14** | **`tests/invariants    82 passed`** | **实测 141** | ❌ **漂移** |
| `M5_REMEDIATION_REPORT.md` | `security 49 · acceptance 9 · e2e 40 · invariants 141 · unit 1779 · integration 6` | 一致 | ✅ |

**→ 1 处残留漂移（见 §B）。**

---

## 十二 / 十三、N10 验证（只记录，未修复）

### 实测事实

| 问 | 实测 |
|---|---|
| **1. child workspace 实际路径** | `…\tmp<rand>\s-sub0001` |
| **2. parent workspace 实际路径** | `…\tmp<rand>\workspaces` |
| **3. 路径关系** | `child_ws_inside_parent_ws = **false**`；`child_ws_sibling_of_parent_ws = **true**` |
| **4. 目录是否被创建** | `child_ws_exists = **false**`；目录树仅 `sessions/ · spill/ · workspaces/` ⇒ **从未创建** |
| **5. `fs.list_dir` 失败的确切原因** | stderr advice = **「目录不存在或路径不是目录，先 list_dir 父目录侦查」** ⇒ `tool_fs._workspace_root(ctx)` 返回的 `…\s-sub0001` 不存在 ⇒ provider 抛 `TLB-802` |
| 关系链 | **治理判定 `allow` → 关 3 Provider 抛 TLB-802 → `tool.error`（无 `tool.result`）** ⇒ 失败**在执行层**，非治理层 |

**根因（代码级）**：`core/subagent.py:373-375`

```python
base = Path(str(s.policy.workspace_root or "~/.pyharness/workspaces")).expanduser()
pol.workspace_root = str(base.parent / sub_id)      # ← base.parent ⇒ workspaces/ 的**兄弟**
```

两处偏差:① `.parent` 使子根**越出** `workspaces_dir` 树;②**无人创建**该目录。

### 次要观察（非 N10 本体）

`tool.error` 的**载荷 message** 只写笼统的 `"错误[TLB-802]:工具未注册"`，而**真实原因**
（"目录不存在"）只在 **stderr 的 advice** 里。对 LLM 回喂而言，这条消息是**误导性**的
（工具**已注册**，是路径不存在）。→ 记为观察项 **O-2**。

### Sub-Agent Workspace Contract（子代理工作区契约）—— **只分析，不修**

| # | 问题 | 当前实现（实测） | 契约建议（分析结论，未实施） |
|---|---|---|---|
| 1 | **Child workspace 应该在哪里？** | `workspaces/` 的**兄弟目录**（树外） | 应在 **`workspaces_dir` 之内**（如 `workspaces_dir/<sub_id>/`）—— F055 与 `policy.workspace_root` 的前提是"所有会话文件面都在配置的 workspaces 根之下";树外会破坏该前提 |
| 2 | **谁创建？** | **无人** | **装配层**（与 `engine.build_runner_components` 里 `ws_root.mkdir` 同处），在 spawn 时创建；不应由 guard/executor 隐式创建 |
| 3 | **谁拥有？** | 子会话（其 `scope.policy.workspace_root` 指向它） | 子会话独占**写**权;父会话**不应**自动获得子根的写权（当前已如此） |
| 4 | **生命周期？** | 无显式定义（因为未创建） | 与子会话同生灭;建议**保留**以便审计（与"证据保留"一致），清理交显式策略 |
| 5 | **是否允许访问 Parent Workspace？** | **否** —— 子 `workspace_root` 被替换，父根不在其可见面 | 契约化:子**不得**直接写父根（`spec.tools_subset`/`deny_extra` 只收窄，无"父级担保"通道） |
| 6 | **是否允许访问 Parent 之外？** | **否** —— `resolve_in_workspace` 三闸（POL-FS-1/2/3）以 `workspace_root` 为唯一边界 | 保持"单根 + 三闸" |
| 7 | **Filesystem Tool 的 sandbox boundary？** | `tool_fs._workspace_root(ctx)` → `scope.policy.workspace_root`；外加 g3 几何闸（词法/规范化/symlink） | 边界 = **子 workspace_root**;越界 fail-closed `GRD-401` |
| 8 | **多 child 是否必须互相隔离？** | **设计上**按 `sub_id` 分根 ⇒ 隔离;但因未创建而**不可观测** | 必须隔离（同 sub_id 复用同一根即冲突） |
| 9 | **child 结束后 workspace 是否保留？** | 无清理逻辑 ⇒ 事实上"保留"（若曾被创建） | 建议保留;删除须走显式策略（H-1 类操作） |
| 10 | **failure / cancellation 时如何处理？** | 无特殊处理 | 建议与 success 一致（保留）;不得因失败而删父侧数据 |

**结论**：N10 的根因不是"路径写错一个字符"，而是**子代理工作区契约从未被定义过**
（无归属方、无创建方、无生命周期、无清理策略），代码只填了 `workspace_root` 字段而
**没有任何一方承担"供给这个目录"的责任**。修法须先定契约，再定创建点。

---

## 十四、Mutation / Falsification 复核

在**临时副本**上对四个已修不变量各注入一次定向违约（原仓库零写入）:

| 变异 | 目标测试 | 结果 |
|---|---|---|
| **J1** Channel Contract Uniqueness（`_ensure_channel` 回退旧规则） | `tests/invariants/test_inv_channel_contract.py` | **RED — 11 failed** |
| **J2** Registered Tool Is Classified（移除 `subagent` 域） | `tests/invariants/test_inv_domain_classification.py` | **RED — 4 failed** |
| **J3** Segment Terminal Completeness（移除 `segment.end` 订阅） | `tests/e2e/test_evidence_lifecycle_e2e.py` | **RED — 8 failed** |
| **J4** Declared Config Is Reachable（断开 `flush_kwargs_of` 注入） | `tests/invariants/test_inv_config_reachability.py` + `tests/e2e/test_config_wiring_e2e.py` | **RED — 1 failed** |

**4/4 RED** ⇒ 四条不变量的测试**确有鉴别力**（不是"测试数量多所以看起来强"）。

---

## 十五、最终分类

### A. VERIFIED（M5 结论被当前仓库重新证明）

| 项 | M5 报告 | 当前运行时 | 状态 |
|---|---|---|---|
| **M5-1 Channel** | 九状态跨层一致;ACP 不降级;两装配路径一致 | **9/9 一致** · `acp:client-7`→`acp` · `CONSISTENT=true` | **Verified** |
| **M5-2 Domain** | strict 档 `subagent.spawn` 可达;未放宽 strict | `can_use=True` · `tool.result=1` · `spawned=1` · 子会话存在 · `exec/proc` 仍收紧 | **Verified** |
| **M5-3 Evidence** | 五终态各恰一条;取消用真实 cancel | **5/5 `decision=1 / seg.end=1 / evidence=1 / dup=0`**；取消 = 真实 `task_queue.cancel` | **Verified** |
| **M5-4 Child Evidence** | 父/子各有段与证据 | Parent 1/1/1/1 · Child **1/1/1/1** | **Verified** |
| **M5-5 Config** | 配置==运行时;默认不破坏 | **5/5 一致**，含默认 (0.5,64) | **Verified** |
| **M5-6 Docs** | 计数已同步 | 6/7 处一致（1 处残留，见 B） | **Verified（含 1 漂移）** |
| **M5-7 N8** | 保留加注 | 行为未变;注释在位 | **Verified** |
| （附）**N11 notify** | 已修 | 四态语义精确;`None` 不再吞 `joined` | **Verified** |

### B. DRIFT（报告与当前仓库不一致）

| # | 位置 | 报告声称 | 当前实测 | 类型 |
|---|---|---|---|---|
| **D-1** | `F2_REMEDIATION_REPORT.md` §14 | `tests/invariants    82 passed` | **141 passed** | **Documentation Drift**（F2 报告的历史快照值与 M5 后的真实值不符;M5-6 修了 acceptance/e2e 却漏了 invariants） |

**其余声称全部一致**（`LIMITATIONS.md` / `README.md` / `M5_REMEDIATION_REPORT.md`）。

### C. N10 FINDINGS（只记录，未修复）

| # | 发现 | 性质 |
|---|---|---|
| **N10** | 子会话 `workspace_root` = `workspaces/` 的**兄弟目录**且**从未创建** ⇒ 子会话**文件类工具必然失败**（治理 `allow` → 关 3 `TLB-802` → `tool.error`，无 `tool.result`） | **P1 · 功能性空心**（子代理不能做文件类工作） |
| **N10-a** | **根因是"契约缺失"而非"路径笔误"**：`subagent.py:373-375` 只填了 `workspace_root` 字段，**没有任何一方承担"创建/拥有/清理"该目录的责任** | 需先定契约（§十二 十问） |
| （附）**O-2** | `tool.error` 的**载荷 message** 对 N10 类失败是**误导性**的（写"工具未注册"，实为"目录不存在"）；真实原因只在 stderr advice 里 ⇒ 回喂 LLM 的信息质量受损 | P3 · 观察项 |
| （附）**O-3** | `budget` 终态在**任务队列侧**被记为 `error:CYC-999`（`BudgetExhausted` 落入 `except Exception` 兜底）⇒ 持久化记录**丢失 budget 语义**（agent-loop 侧仍有 `reason=budget`） | P3 · 观察项 |
| （附）**O-4** | M5 报告**未说明**真实证据顺序是 `segment.end → evidence.archived`（见 §七） | 说明缺口，非行为缺陷 |

### D. BLOCKERS（真正阻塞 M5 验收的项）

**无。**

判定依据：
- M5 的**每一项验收点**（M5-1 ~ M5-7 + N11）都在当前仓库上**运行时复现通过**；
- N10 **不在** M5 的验收范围内（M5-4 的验收是"父/子各自的**段与证据**",均已成立），
  且其根因与 M5-4 改动的代码路径**无因果**；
- 唯一漂移 D-1 是**文档计数**，不影响任何运行时行为。

---

## 十六、M5 Acceptance Matrix

| 项目 | M5 报告 | 当前运行时 | 状态 |
|---|---|---|---|
| **M5-1 Channel** | 五态冻结 + 六层统一 + 两装配路径一致 | 九状态 9/9 跨层一致;`acp:client-7`→`acp`（非 desktop/None/system）;两路径逐字段相同 | **Verified** |
| **M5-2 Domain** | 补 `subagent` 域 + `RESTRICTED_DOMAINS` + 不变量 | strict 档可达并真实派生;`exec/proc` 仍收紧 | **Verified** |
| **M5-3 Evidence** | 触发点移至 `segment.end`;五终态各一条 | 5/5 `1/1/1`、`dup=0`;取消用**真实 cancel**;顺序 `seg.end → evidence` | **Verified** |
| **M5-4 Child Evidence** | 开段 + child bus 订阅同一生产者 | Parent `1/1/1/1` · Child `1/1/1/1` | **Verified** |
| **M5-5 Config** | 工厂收标量 + 装配层注入（12 点） | 5/5 配置==运行时;默认 0.5/64 未破坏 | **Verified** |
| **M5-6 Docs** | 计数修正 | 6/7 一致;**1 处残留**（F2 §14 invariants 82 vs 141） | **Drift** |
| **M5-7 N8** | 保留 + 加注 | 注释在位;行为未变 | **Verified** |
| **N10** | （M5 列为"只记录不修"） | 复现:子工作区树外 + 未创建 ⇒ 文件类工具必失败;**契约缺失**为根因 | **Not Fixed（按授权）· 记录在 §C** |
| （附）N11 notify | 已修 | 四态语义精确;`None`/省略 ⇒ joined=1;`False` ⇒ 0 | **Verified** |
| GAP-3 原生壳 | Environment Blocked | 无 PySide6 ⇒ 无 UI 运行时证据 | **Blocked** |
| GAP-6 CI | Environment Blocked | 未 push ⇒ 从未在 Actions 执行 | **Blocked** |

---

## 十七、Test Pass ≠ Runtime Proof —— 本轮的区分

| 项 | Test Evidence（测试通过） | Runtime Proof（运行时证明） | 二者一致? |
|---|---|---|---|
| M5-1 | `test_inv_channel_contract.py` 38 passed | **独立探针**实调三层 + 两装配路径逐字段比对 | ✅ 一致 |
| M5-2 | `test_inv_domain_classification.py` 6 passed | **独立探针**真跑 `subagent.spawn` 并读子会话文件 | ✅ 一致 |
| M5-3 | `test_evidence_lifecycle_e2e.py` 9 passed | **独立探针**五终态 + **真实 `task_queue.cancel`** + 顺序 | ✅ 一致 |
| M5-4 | `test_subagent_evidence_e2e.py` 4 passed | **独立探针**读父/子两个 JSONL | ✅ 一致 |
| M5-5 | `test_inv_config_reachability.py` 8 + `test_config_wiring_e2e.py` 3 | **独立探针** 5 组配置值比对 | ✅ 一致 |
| N11 | 同上 | **独立探针**四态 notify | ✅ 一致 |
| **N10** | **无测试断言** | **独立探针复现**（子工作区树外 + 未创建） | ⚠ **测试面空缺** —— 这正是 N10 未被 M5 发现的原因 |

**结论**：M5 的每一项**已声明**结论都有**独立的运行时证据**支撑，二者一致；
唯一"测试面空缺"之处（N10）恰是**未声明为已修**的项 —— 未出现"用测试通过冒充运行时行为"的情况。

---

_文档时间戳：2026-09-20T23:51:12+08:00_
