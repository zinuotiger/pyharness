# M4.5 Pre-Remediation Deep Verification Report（修复前深度验证）

> **模式**：M4.5 Deep Verification。**未修改生产代码 / 测试 / LIMITATIONS / README；未 commit；未 push。**
> 所有探针位于 `%TEMP%\m4-probe\`（仓库外）。
> **复核方式**：本轮**全部结论均来自我本人执行的运行时探针或直接读码**；未使用上一轮的结论作为前提。

---

## Executive Summary（执行摘要）

> **四项优先级全部由 Static Evidence（静态证据）推进到 Runtime Evidence（运行时证据），
> 并在推进过程中新发现一条 P1 级"注册但不可达"缺陷。**
>
> 1. **REOPENED-GAP-08 —— 语义已精确定格**：evidence 产出**当且仅当 `loop.wake` 正常返回**，
>    与终态 reason 无关。五终态实测：`segment.end` **5/5 发出**，`evidence.archived` 仅 **3/5**。
> 2. **N2 —— 比原判断严重两级**：默认 `strict` 档下 `subagent.spawn` **根本不可达**
>    （`subagent` 域不在 `SELF_DOMAINS`）⇒ 子代理能力从 LLM 视角为死代码；
>    且即便放开（`standard`），子会话仍是 **2 决策 / 0 证据 / 0 段**。
> 3. **N5 —— 分层矩阵 + 端到端双证**：`_ensure_channel` 与 `_require_human` 在**同一文件内**
>    对 `"acp:<id>"` 给出**相反**判定；且**两条装配路径给出两种不同的错误结果**。
> 4. **N1 —— 链路断点精确定位到唯一一跳**：`Config → open_store`。运行时实测 3.0/7 → 0.5/64。
>
> **可进入 M5 修复**：四项优先级的运行时证据已充分。**唯一仍缺的运行时证据**见 §12。

---

## 1. REOPENED-GAP-08 深度验证

### 1.A 规则是否仍然正确？

**原规则（我上一轮写下的）**：`段内至少一条 decision.issued ⇒ 归档`。
**实测反例**：段内确有 decision.issued，但 run 抛错 ⇒ 归档数 0。**原规则为假。**

**修正后的规则（本轮定案，五种终态全覆盖）**：

> **规则 R-E1**：一个**已关闭**的任务段，若段内存在**至少一条** `decision.issued`，
> 则**必须存在恰一条** `evidence.archived` 引用该段锚 —— **与任务最终成功、失败、取消、
> 异常退出、部分完成无关**。

**五终态语义正式定义**（每一行均为实测，探针：`probe_g8_states.py`）：

| # | 终态 | 定义（触发条件） | `segment.end` | `evidence.archived` **应然** | `evidence.archived` **实然** |
|---|---|---|---|---|---|
| 1 | **success** | `wake` 正常返回 | 1 | 1（已有 D/R/T） | **1** ✅ |
| 2 | **abnormal completion** | `wake` 正常返回但 `reason != "complete"`（`stall`/`max_turns`） | 1 | 1 | **1** ✅ |
| 3 | **exception** | `wake` 抛 `PyHError`（如 `LLM-310`） | 1 | 1 | **0** ❌ |
| 4 | **budget** | `wake` 抛 `BudgetExhausted` | 1 | 1 | **0** ❌ |
| 5 | **cancellation** | `wake` 抛 `CancelledError` | 1 | 1 | **0** ❌ |
| 6 | **partial completion** | Provider 被掐断、写 `partial` 结果（`tool.result ok=False,truncated=True`） | 1 | 1 | **0** ❌（同上，属异常族） |

**关键实测事实**：`segment.end` 在 **5/5** 情形都发出（由 `task_queue._run_task` 的 `finally` 保证），
而 `evidence.archived` 只在 **3/5** 发出（由 `engine.run_for_task` 的**函数体**触发）。

> **→ 两个"段终态事实"的保证强度不同，这是根因。**

### 1.B 决策后异常时，工件应包含什么？

按 INV-G4/E1（只存引用、不复制内容），建议的**必含引用**与**必含事实**：

| 字段 | 来源 | 是否已有 | 说明 |
|---|---|---|---|
| `task_id` | 段锚 `segment.start.payload.task_id` | ✅ 经 `refs[0].locator="<task_id>:seg"` | 已有 |
| `decision reference` | 段内每条 `decision.issued.payload.decision_id` | ✅ 已收集 | 已有 |
| `receipt reference` | 段内每条 `receipt.emitted.payload.receipt_id` | ✅ 已收集 | 已有 |
| `event range` | 段起止 seq | ⚠ **部分**：`segment` ref 可解析到 `segment.start`，但工件**不含 end seq** | **建议补** |
| `terminal state` | `task.completed` / `task.failed` 的终态码 | ❌ **未包含** | **建议补**（否则无法从工件直接区分"成功归档"与"失败归档"） |
| `failure information` | 异常码 / 取消原因 | ❌ **未包含** | **建议补**（脱敏：只放错误码，不放堆栈） |
| `timestamp` | 归档事件自身的 `Envelope.ts` | ✅ 隐式（事件带 ts） | 已有 |

**设计约束**：INV-G4/E1 禁止复制事件内容 ⇒ 上表新增项必须是**引用或标量**（终态码、异常码），
**不得**复制 payload 原文。

### 1.C `finally` 是否足以解决问题？—— **不足以**

逐条核验（不是假设）：

| 风险 | 核验结论 |
|---|---|
| **重复归档** | ✅ 已被幂等闸拦住（判据取自日志重放，见 `engine.py` 的 `evidence.archived` 扫描）。**不构成阻塞**。 |
| **事件尚未持久化** | ✅ 不成立。生产者读的是**内存事件列表**（`session.events_after(0)`），异常路径上事件已在 `_absorb` 时进入内存。 |
| **archive 失败** | ✅ 不成立。`archive_task_evidence` **自带 try/except 并返回 None**，不会上抛 ⇒ 不会覆盖原始异常。 |
| **原始异常被覆盖** | ✅ 由上一行保证（只要不改动该函数）。 |
| **不完整证据** | ❌ **成立且无法靠 `finally` 解决**：`segment.end` 由 `task_queue._run_task` 在 `run_for_task` **返回之后**才写（实测 `evidence.archived` seq 21 < `segment.end` seq 23）。把生产者放进 `run_for_task` 的 `finally`，它**仍在段关闭之前**运行 ⇒ **N3 依旧存在**。 |
| **shutdown race** | ⚠ **成立**：`finally` 内的 `await` 落在**取消传播路径**上，首个子挂起点即可能再次抛 `CancelledError`，归档被吞。 |
| **取消时 `await` 不安全** | ❌ **成立，且这是决定性理由**：`EvidenceCollector.archive` → `sess.append` 含多个 await（总线分发、强同步 flush）。在已被取消的任务的 `finally` 里 await，**不保证执行完**。 |

> **结论：`finally` 是必要但不充分的。** 正确做法是**把证据生产从"run 函数的收尾"移到
> "段生命周期事件"**（`segment.end`），因为后者由 `task_queue` 的 `finally` 保证，
> 且执行上下文不在被取消的任务里。
>
> **→ 该修正同时消除 N3**（生产者不再早于段关闭）。

### 1.D Sub-Agent 验证 —— 运行时探针结论：**比原判断严重两级**

**D-1（新，P1）：默认档下子代理**根本不可达**。

实测（`probe_n2c.py`，同一注册表、同一工具，仅改 `security.policy.preset`）：

| preset | `sandbox_level` | `scope.can_use("subagent.spawn")` | `tool.result` | `subagent.spawned` | 子会话文件 |
|---|---|---|---|---|---|
| **strict（默认）** | strict | **False** | 0 | 0 | **[]** |
| standard | basic | **True** | 1 | 1 | **["s-sub0001.jsonl"]** |

**根因**（读码确认，`core/scope.py:287`）：
```python
domain = tool_name.split(".", 1)[0]      # "subagent.spawn" -> "subagent"
if domain in SELF_DOMAINS: return True   # SELF_DOMAINS 无 "subagent"
```
`SELF_DOMAINS = {"goal","todo","plan","util","user","skill","session","schedule"}`
—— **`"subagent"` 缺失**，且 `WORKSPACE_DOMAINS`/`ALLOWLIST_DOMAINS` 亦不含 ⇒ strict 档下
`can_use=False` ⇒ `GRD-401 scope-hidden` 终局拒。

> **这是同一类缺陷的第三次发生**。代码注释自陈前两次：
> `session.*`「原缺此域导致索引工具在默认 strict 档被域显隐静默隐藏」、
> `schedule`「同 session 域先例:缺域即被静默隐藏」。

**D-2（P2）：即便放开，子会话仍零证据。**

实测（`probe_n2d.py`，`preset=standard`，真实父 → `subagent.spawn` → 子 → `fs.list_dir`）：

| 会话 | 角色 | `decision.issued` | `receipt.emitted` | `guard.evaluated` | `tool.call` | `segment.start/end` | **`evidence.archived`** |
|---|---|---|---|---|---|---|---|
| parent | 父 | 1 | 1 | 1 | 1 | 1 / 1 | **1** ✅ |
| **s-sub0001** | **子** | **2** | **2** | **2** | 2 | **0 / 0** | **0** ❌ |

**根因**（读码确认）：`core/orchestration.py` 内 `EvidenceCollector` / `archive_task_evidence` /
`evidence` **零命中**。子会话经 `_run_on_session`（`:88-98`）**不开任务段**（无 `open_segment`），
且 `child_session`（`:141-165`）建的 child bus **无 producer 订阅**。

**对上一轮 N2 判断的说明**：上一轮 N2 判定"子会话零证据"，本轮**运行时证实成立**；
但上一轮**漏掉了更前置的 D-1** —— 默认档下子会话**根本不会产生**，故 N2 在默认配置下
是"不可观测"的，这解释了为何上一轮只能给出读码结论。

**完整 Production Call Path 缺口（D-1 + D-2）**：
```
LLM → tool_calls["subagent.spawn"]
   → 关1 lookup ✅ (注册表有)
   → 关2 authorize → GuardChain._evaluate_full
        → scope.can_use("subagent.spawn") → False      ← 【D-1 断点】
        → REJECT / GRD-401 → 终局,Provider 零调用
[若放开]
   → SubagentManager.spawn → EngineSubagentRunner.run_child
        → _run_on_session(新 SessionLog, 无 open_segment)   ← 【D-2 断点 a】
        → 子 AgentLoop 执行工具 → 子会话产生 decision/receipt
        → child_bus 无 evidence producer 订阅              ← 【D-2 断点 b】
        → evidence.archived 永不产生
```

---

## 2. N1 — Flush 配置深度验证

### 2.A 哪个配置对象才是 Runtime Source of Truth？

**`cfg.log.jsonl`**。依据：`config.py:696-699` 以 pydantic 声明并**带校验**
（`flush_interval_s: ge=0.05, le=10`；`flush_batch: ge=1, le=1024`），且 `config.py:75`
的 `DEFAULTS` 与之同源。**声明 + 校验的存在意味着"可调"是设计意图。**

### 2.B 应由谁读取？

**`open_store`（工厂）** —— 它是 `SessionStore` 的**唯一生产构造点**（全库仅 1 处）。
由工厂读 `cfg` 并传给 `SessionStore`，符合项目既有的"装配层注入"纪律。

### 2.C 为什么配置值 ≠ 运行时值？

**链路在唯一一跳断开**：

```
cfg.log.jsonl.flush_interval_s / flush_batch   [config.py:698-699 已声明+校验]
        │
        ✗  断点:open_store(session_id, *, dir=None)  —— 签名里没有 cfg
        │
persistence.py:53-54  _FLUSH_INTERVAL_S / _FLUSH_BATCH  ← 直接读 DEFAULTS 模块常量
        │
persistence.py:327-328  self.flush_batch = flush_batch or _FLUSH_BATCH
                        self.flush_interval_s = flush_interval_s or _FLUSH_INTERVAL_S
        │
engine.py:142  interval = getattr(store, "flush_interval_s", 0.5)   ← 定时器读 store
```

**穷证**：全库（含 tests/scripts）**零处**书写 `flush_interval_s=` / `flush_batch=`。

### 2.D 直接传参会否产生问题？

| 维度 | 评估 |
|---|---|
| **API compatibility** | ⚠ **轻微**。`open_store` 是公开工厂（`persistence.__all__`）。建议**新增关键字参数并保留默认**（`flush_interval_s=None, flush_batch=None`）⇒ 向后兼容，旧调用行为不变。 |
| **test fixture** | ✅ **无影响**。既有测试（如 `tests/e2e/test_persistence_failure_e2e.py`）**直接对 store 实例赋值**，不经 `open_store` 传参 ⇒ 不受影响。 |
| **lifecycle** | ✅ **无影响**。参数只在构造期消费，无资源生命周期含义。 |
| **dependency injection** | ⚠ **这是真正的设计选择**：是否让 `persistence` 依赖 `config`？<br>现状 `persistence` **不 import config**（只 import `DEFAULTS` 常量 dict）。<br>**推荐方案**：`open_store` **不 import config**，改为**接收已解析的标量**；由**装配层**（`engine.build_spine` / `desktop.sessions._open_store`）从 cfg 取值传入。这样保持 `persistence` 对 config 无依赖（与 `INV-08` 依赖方向一致）。 |

### 2.E Runtime Probe（运行时探针）

```
配置想要     : flush_interval_s = 3.0 , flush_batch = 7
SessionStore : flush_interval_s = 0.5 , flush_batch = 64
E_wired      = False
```

**结论：配置当前完全不生效。**（M4 已测，本轮复测一致。）

---

## 3. N5 — Channel Contract（通道契约）深度验证

### 3.1 五种通道状态的正式区分

| # | 状态 | 构造 | 可达性 |
|---|---|---|---|
| 1 | **missing（缺失）** | ctx 无 `channel` 属性 | 可达（未声明通道的装配） |
| 2 | **explicit None（显式 None）** | `ctx.channel = None` | 可达（CLI headless，`cli.py:776`） |
| 3 | **valid（合法）** | `"cli"` / `"desktop"` / `"acp:<id>"` | 可达（三外壳） |
| 4 | **invalid（非法）** | `"hacker"` 等非白名单串 | 可达（若装配层传入任意串） |
| 5 | **unknown（未知）** | `""` 空串 | 当前**不可达**，但代码路径存在 |

### 3.2 分层矩阵（运行时实测，`probe_n5.py`）

| 状态 | Service | Engine `spine.channel` | **Approval `_ensure_channel`** | **Governance `principal_of`** | 链：decision principal |
|---|---|---|---|---|---|
| **MISSING** | `desktop` | `<UNDECLARED>` | **`desktop`**（人类通道！） | **FAIL-CLOSED APR-503** | **FAIL-CLOSED** |
| **NONE** | **`desktop`** ⚠ | `None` | **`desktop`** ⚠ | `system` | `system` |
| **EMPTY `""`** | **`desktop`** ⚠ | `""` | **`desktop`** ⚠ | **`system`** ⚠（静默降级！） | `system` |
| VALID `desktop` | `desktop` | `desktop` | `desktop` | `human:desktop` | `human:desktop` |
| VALID `cli` | `cli` | `cli` | `cli` | `human:cli` | `human:cli` |
| **VALID `acp:c1`** | `acp:c1` | `acp:c1` | **`desktop`** ⚠⚠ | `human:c1:acp` | `human:acp` |
| INVALID `hacker` | `hacker` | `hacker` | **`desktop`** ⚠ | **FAIL-CLOSED APR-503** | FAIL-CLOSED |

**分裂行为确认（用户点名的那一种）**：
**MISSING → Approval `desktop`，但 Governance `APR-503`。** 同一 ctx 状态，
审批层认为"有 desktop 人类通道"，治理层认为"从未声明通道 ⇒ 拒绝"。

### 3.3 矩阵暴露的四个独立症状（同一根因）

| 症状 | 事实 | 证据 |
|---|---|---|
| **N5-a** | MISSING：审批层给 `desktop`（人类），治理层 fail-closed | 矩阵行 1 |
| **N5-b** | **`"acp:<id>"`（合法形态）在审批层不被识别** | 矩阵行 6 + `probe_acp.py` |
| **N5-c** | **`""` 空串在治理层静默降级为 `system`**（`ch or _FRAMEWORK_BY` 的 falsy 短路） | 矩阵行 3 |
| **N5-d** | INVALID：Service 原样透传 `hacker`，Approval 转成 `desktop`，Governance 拒绝 —— 三层三个答案 | 矩阵行 7 |

### 3.4 N5-b 的判定规则冲突（同一文件内两条相反规则）

```
approval.py:611  _ensure_channel : ch in CHANNELS           → "acp:c1" in ("cli","web","acp","desktop") = False
approval.py:784  _require_human  : by.startswith(c + ":")   → "acp:client-7".startswith("acp:")      = True
```

**运行时验证（`probe_acp.py`）**：

| 场景 | `ctx.channel` | `provider._channel` | `_ensure_channel` | `_require_human('acp:client-7')` |
|---|---|---|---|---|
| **ACP（生产形态）** | `acp:client-7` | `acp:client-7` | **`None`（headless）** | **ACCEPTED** |
| CLI | `cli` | `cli` | `cli`（交互） | ACCEPTED |
| Desktop | `desktop` | `desktop` | `desktop`（交互） | ACCEPTED |

### 3.5 端到端功能级复现（N5-b 的真实后果）

`exec.shell_run`（**danger=high**）在 `preset=standard` 下，`channel="acp:c1"`：

```
seq 10  tool.call        exec.shell_run
seq 11  guard.evaluated  need_approval  (g-danger / POL-DGR-1)
seq 12  decision.issued  verdict=approval  (D1)
seq 13  approval.requested  trace={"channel": "desktop", "call_id": "h"}   ← 通道被记为 desktop
```

**两条装配路径给出两种不同的错误结果**：

| 装配路径 | `approval._channel` | `_ensure_channel` | 实际后果 |
|---|---|---|---|
| `assemble_real_engine(channel="acp:c1")`（desktop / desktop_native 路径） | `"desktop"`（**engine.py:628 硬编码**） | `desktop` | **审批请求发到 desktop**（错误的通道，但至少会问人） |
| `attach_engine_to_ctx(channel="acp:c1")`（**真实 ACP 路径**，acp.py:388） | `"acp:c1"` | **`None`** | **headless ⇒ `APR-501` ⇒ ACP 客户端永远收不到审批提示** |

> **后者是功能缺陷**：ACP 下任何 `danger≥high` 工具都会被当作"无通道"直接拒。
> 该缺陷**先于本轮存在**（`attach_engine_to_ctx` 早已把 channel 写入 `_channel`；
> 我的 GAP-11 改动前其为 `None`，改动后为 `"acp:c1"`，**两条路径结果相同 ⇒ 未加重、也未修复**）。

---

## 4. N6 / N7 与 N5 的关系判定

| 项 | 事实 | 与 N5 的根因关系 |
|---|---|---|
| **N6** | `engine.py:628-629` `ApprovalProvider(..., channel="desktop")` 硬编码 | **同一根因**：通道解析散落多处，且**默认值是"人类通道"**（默认即授予交互性） |
| **N7** | `service.py:112` `str(channel or "desktop")` | **同一根因**：falsy 合并把**显式声明**（`None` = headless）改写成**另一种声明**（`desktop`） |
| **N5-c** | `context.py:93` `ch or _FRAMEWORK_BY` | **同一根因的镜像**：falsy 合并把 `""` 改写成 `system` |

**判定：应合并为单一 Finding —— `N5 — Channel Contract Violation（通道契约违反）`**，
其根因是：**不存在唯一的通道解析函数，且"状态 → 语义"的映射在 6 层中被独立实现了 5 次**
（Service 的 or-coalescing、Engine 的硬编码默认、Approval 的精确名匹配 + 回落、
Governance 的 hasattr 哨兵 + or-coalescing）。

**依据根因，不是依据数量。** （用户明确要求按根因判断，本轮正是如此。）

---

## 5. N3 — Evidence Artifact Timing 判定

**实测时机**：工件在 **`run_for_task` 函数体末尾**产生，即
**任务段仍未关闭时**（`evidence.archived` seq 21 < `segment.end` seq 23）。

**判定：N3 与 REOPENED-GAP-08 是同一根因，应合并。**

理由（根因层面，非数量层面）：两者都是**"证据生产的触发点挂在 `run_for_task` 函数体上"**
这一条根因的两个不同表现 ——
- GAP-08 表现：函数体不执行时无证据；
- N3 表现：函数体执行时**早于段关闭**。

**一个修正（把触发点移到 `segment.end`）同时消除两者** ⇒ 合并成立。

---

## 6. N4 — approval.* 缺少 call_id：定性

**判定：deliberate design（有意设计，2 跳联结），不是缺失上下文，也不是历史不一致。**

依据（读码）：
- `ApprovalOutcomePayload`（`payload.py:188-192`）只有 `approval_id / by / ttl_ms`；
  docstring 明写 **"approval_id = requested 的 seq"** —— **这就是设计好的联结键**。
- 链路可一跳还原：`approval.granted.approval_id` → `approval.requested(seq)` → 其 `trace.call_id`。
  消费方 `session.py:256-265` 正是这样做反查的。

**但上一轮的契约表述过宽**：我在 `tests/invariants/test_inv_context_and_paths.py` 写下
"治理面 → 在 trace"，而 `CALL_ID_IN_TRACE` **未收录** `approval.granted/denied/timeout`
⇒ **测试通过，陈述为假**。

**→ 重分类**：从"真实 GAP（P3）"降为 **"契约陈述范围问题 + 契约测试覆盖缺口（P3）"**，
并入 N9 文档/契约一致性家族。

---

## 7. N8 — `require_adapter` 定性

| 问 | 答 | 依据 |
|---|---|---|
| dead API？ | **是**（生产零调用者） | 全库唯一命中 = 定义处 `llm.py:788` + `__all__:1090` + 测试 |
| compatibility API？ | **否** | `docs/` 与根目录 `*.md` **零命中** ⇒ 无对外兼容承诺 |
| future API？ | **无法判定** | 无 TODO/设计文档指向 |
| external API？ | **弱** | 在 `__all__` 中，理论上可被下游 import；但本仓库无下游消费者 |

**建议（本轮不实施）**：**保留并加注**（标 `# 未接线:保留为公开辅助面，无生产调用者`），
**不删除** —— 删除属 H-4（公共导出面变更），收益（少一个函数）小于成本（契约变更）。
若确认要删，须走一次显式契约变更。

---

## 8. N9 — Documentation Drift 精确清单

**代码实际**：`tests/acceptance` = **9** ；`tests/e2e` = **18** ；`tests/security` = **49** ；`tests/integration` = **6**。

| 文件 | 行 | 内容 | 应为 |
|---|---|---|---|
| `LIMITATIONS.md` | 27 | `acceptance 7 · e2e 13 · integration 6` | `acceptance 9 · e2e 18 · integration 6` |
| `LIMITATIONS.md` | 68 | `security 49 · acceptance 7 · e2e 13 · integration 6` | `security 49 · acceptance 9 · e2e 18 · integration 6` |
| `F2_REMEDIATION_REPORT.md` | 419 | `tests/acceptance     7 passed` | `9 passed` |
| `F2_REMEDIATION_REPORT.md` | 420 | `tests/e2e           13 passed` | `18 passed` |
| `F2_REMEDIATION_REPORT.md` | 476 | `acceptance 7 / e2e 13 / integration 6` | `acceptance 9 / e2e 18 / integration 6` |

**共 5 行 / 2 文件。** 另：`LIMITATIONS.md` **L-11**（"EvidenceCollector 生产生产者已建立 …
**已修复**"）缺少限定语 **"仅正常完成路径"** —— 本轮 §1.A 已证其过宽。

**未受影响**：`README.md` 的 1944 计数、`LIMITATIONS.md:45` 的词汇表计数（77/14/3/77）、信封字段数 10 —— 均与实测一致。
`docs/baseline/*` 是 S0 时点快照，**不是漂移**。

---

## 9. Repair Design（修复设计）

### A. Root Cause Matrix（根因矩阵）

| Finding | Root Cause（根因） | Runtime Evidence | Severity | Related |
|---|---|---|---|---|
| **GAP-08R**（含 N3） | **证据生产的触发点挂在 `run_for_task` 函数体**，而段终态由 `task_queue` 的 `finally` 保证 ⇒ 两者保证强度不同、且生产者早于段关闭 | 五终态表：`segment.end` **5/5**，`evidence.archived` **3/5**；seq 21 < 23 | **P2** | N3（合并） |
| **N2-a** | `"subagent"` 域不在 `SELF_DOMAINS` ⇒ 默认 strict 档 `can_use=False` | strict: `can_use=False`/0 子会话；standard: 1 子会话 | **P1** | 是 N2-b 的前置 |
| **N2-b** | `_run_on_session` 不开段 + child bus 无 producer 订阅 | 子会话：2 decision / 0 evidence / 0 segment | **P2** | 依赖 N2-a |
| **N5**（含 N5-a~d、N6、N7） | **不存在唯一的通道解析函数**；6 层各自实现"状态→语义"映射 | 7 状态 × 6 层矩阵；ACP 端到端双路径 | **P2** | N6/N7（合并） |
| **N1** | `open_store` 无 cfg 入参 ⇒ 配置链在**唯一一跳**断开 | 配置 3.0/7 → store 0.5/64 | **P2** | — |
| **N4** | 非缺陷：`approval_id` 是有意的联结键；问题在契约陈述与测试覆盖 | payload docstring | **P3** | 并入 N9 |
| **N8** | 未接线的公开辅助面 | 文档零命中 | **P3** | — |
| **N9** | 手工计数未随代码更新 | 5 行 / 2 文件 | **P3** | N4 并入 |

### B. Consolidated GAP Matrix（合并后的缺口矩阵）

| 编号 | 处置 | 依据 |
|---|---|---|
| **REOPENED-GAP-08** + **N3** | **合并 → `GAP-08R`（Evidence Lifecycle）** | 同一根因（生产者触发点位置）；一个修正同时解决 |
| **N5** + **N6** + **N7** | **合并 → `N5`（Channel Contract Violation）**，症状 N5-a~d | 同一根因（无唯一解析函数）；6 层 5 处独立实现 |
| **N2** | **拆分为 `N2-a`（域可见性，P1）与 `N2-b`（子会话无证据，P2）** | **根因不同**：前者是域分类缺失，后者是段/订阅缺失；且 N2-a 是 N2-b 的前置 |
| **N1** | **保留独立** | 根因是 DI 断链，与其它无交集 |
| **N4** | **关闭为"非缺陷"，降级并入 N9**（契约陈述 + 测试覆盖） | 代码符合设计；问题在描述 |
| **N8** | **保留独立**，建议"加注不删" | 删除属 H-4 |
| **N9** | **保留独立**（文档一致性），吸收 N4 的契约陈述修正 | — |

### C. Remediation Order（修复顺序，按依赖）

> 与用户预期顺序的**唯一调整**：把 **N2-a（域可见性）提到证据工作之前**。
> 理由是它 **P1 且是 N2-b 的前置** —— 不先解除不可达，子代理证据的修复无法被端到端验证。

```
第 1 步  N5  Channel Contract            [P2 · 无前置 · 一处修正消 3 项]
           └ 引入单一 resolve_channel(ctx) -> ChannelState
           └ 六层统一消费；删除 or-coalescing / 硬编码默认 / 精确名匹配

第 2 步  N2-a 域可见性                    [P1 · 无前置 · 一行 + 断言]
           └ SELF_DOMAINS 增补 "subagent"
           └ 新增"域分类全覆盖"不变量

第 3 步  GAP-08R Evidence Lifecycle       [P2 · 依赖第 2 步(为子代理分支铺路)]
           └ 触发点从 run_for_task 函数体 → segment.end 生命周期
           └ 同时消除 N3

第 4 步  N2-b 子代理证据                  [P2 · 依赖第 2+3 步]
           └ child_session 的 bus 订阅同一 producer

第 5 步  N1  Flush 配置接线               [P2 · 独立]
           └ open_store 增可选标量入参；装配层从 cfg 取值注入

第 6 步  N4 + N9  契约陈述与文档一致性     [P3 · 独立]
           └ 改契约测试的 CALL_ID 分类 + 收紧 L-11 限定语；修 5 处计数

第 7 步  N8  死导出加注                    [P3 · 独立]
```

### D. Minimal Patch Scope（最小修改范围）

| # | file | function / class | behavior change | tests | E2E Verification | Negative Verification |
|---|---|---|---|---|---|---|
| **1** | `pyharness/core/approval.py` · `pyharness/core/agent.py`(或新建 `core/channel.py`) | 新增 `resolve_channel(ctx) -> ChannelState`；`_ensure_channel` / `principal_of` 改为消费它 | **收紧**：`missing`→`APR-503`（审批层与治理层一致）；`"acp:<id>"`→识别为 `acp`（前缀匹配统一）；`""`→视为非法而非 `system` | 新增分层矩阵测试（7 状态 × N 层一致性） | 三外壳各跑一次 `danger=high` 工具，断言 `approval.requested.trace.channel` 与 `decision.issued.principal_channel` **一致** | 注入"某层自行 fallback" ⇒ 矩阵测试必须 RED |
|  | `pyharness/engine.py:628` | `ApprovalProvider(..., channel="desktop")` | 去掉**人类通道**默认值 ⇒ 改为"未声明"哨兵 | 同上 | 同上 | — |
|  | `pyharness/application/service.py:112` | `str(channel or "desktop")` | 显式 `None` 保持 `None`（不改写声明） | 同上 | 同上 | — |
| **2** | `pyharness/core/scope.py:105` | `SELF_DOMAINS` | 增补 `"subagent"` | 新增"域分类全覆盖"断言：注册表内每个 `danger != critical` 且无外部副作用的工具，其域必须 ∈ `SELF_DOMAINS ∪ WORKSPACE_DOMAINS ∪ ALLOWLIST_DOMAINS` | 默认 strict 下 `subagent.spawn` 可达 → 产生子会话 | 移除 `"subagent"` ⇒ 断言 RED |
| **3** | `pyharness/engine.py` · `pyharness/core/task_queue.py` | `archive_task_evidence` 的触发点 | 从 `run_for_task` 函数体 → **段生命周期**（订阅 `segment.end`，或由 `TaskQueue` 经注入回调调用） | 五终态表全部断言 `evidence.archived == 1` | 五终态 E2E（success/abnormal/exception/budget/cancel） | 移除订阅 ⇒ 5 中 3 项 RED |
|  | 同上 | 工件字段 | 增补 `terminal state` 与 `failure code`（标量，非内容复制） | 字段存在性断言 | 同上 | — |
| **4** | `pyharness/core/orchestration.py:141-165` | `EngineSubagentRunner.child_session` | child bus 订阅同一 producer | 子会话证据断言 | 父→子→工具→结果 全链，断言子会话 `evidence.archived == 1` | 移除订阅 ⇒ RED |
| **5** | `pyharness/persistence.py:761` | `open_store` | 新增可选标量入参 `flush_interval_s=None, flush_batch=None` → 透传 `SessionStore` | 新增：改 cfg → 断言 store 值随之改变 | 改 cfg 后定时器间隔确实变化（计时观察） | 断开透传 ⇒ RED |
|  | `pyharness/engine.py` · `pyharness/desktop/sessions.py` | 装配层 | 从 `cfg.log.jsonl` 取值传入 `open_store` | 同上 | 同上 | — |
| **6** | `tests/invariants/test_inv_context_and_paths.py` | `CALL_ID_IN_TRACE` | 显式记录 `approval.*` 三类的**例外**（2 跳联结） | 契约测试自证 | — | — |
|  | `LIMITATIONS.md` :27/:68 · `F2_REMEDIATION_REPORT.md` :419/:420/:476 ; `LIMITATIONS.md` L-11 | 计数与限定语 | `acceptance 7→9`、`e2e 13→18`；L-11 加"仅正常完成路径" | — | — | — |
| **7** | `pyharness/core/llm.py:788` | `require_adapter` | 仅**加注**，不删 | — | — | — |

### E. New Invariants（建议新增不变量）

> **编号需与 `docs/INVARIANT_REGISTRY.md` 对齐后再定**（运行时面已用 INV-01~05，
> 注册表面已用 INV-01~09，直接续号会撞号）。以下用 `INV-CAND-x` 占位。

**INV-CAND-1 — Channel Contract Uniqueness（通道契约唯一性）**
- **Property**：给定同一 ctx 状态，**所有层**对"这是不是人类通道、是哪个通道"必须给出**同一判定**。
- **Mechanism**：存在且仅存在一个 `resolve_channel(ctx) -> ChannelState`；
  `missing` 与显式 `None` 必须**可区分**且语义固定（`missing` ⇒ fail-closed；`None` ⇒ headless）。
- **Evidence**：7 状态 × N 层一致性矩阵（运行时）。
- **Negative Test**：让任一层恢复"自行 fallback/coalescing" ⇒ 矩阵断言必须 RED。

**INV-CAND-2 — Segment Terminal Completeness（段终态完备性）**
- **Property**：任一**已关闭**的段，若段内含 ≥1 条 `decision.issued`，则**恰有**一条
  `evidence.archived` 引用该段锚；与终态类型无关。
- **Mechanism**：证据生产的触发点绑定**段生命周期事件**（`segment.end`），
  而非任何函数体的收尾路径。
- **Evidence**：五终态实测表（成功/异常完成/异常/预算/取消）。
- **Negative Test**：把触发点移回 `run_for_task` 函数体 ⇒ 五终态中的 3 项必须 RED。

**INV-CAND-3 — Declared Config Is Reachable（声明即可达）**
- **Property**：任何在 Settings 中**声明并校验**的配置键，必须存在 ≥1 条从该键到运行对象的赋值路径。
- **Mechanism**：静态扫描（对 `pyharness/` 内的 `cfg.*` 叶子键名做出现计数，排除定义处）；
  计数为 0 ⇒ 标记为"死配置"。
- **Evidence**：`flush_interval_s` / `flush_batch` 当前计数 = 0（定义处之外）。
- **Negative Test**：断开 `open_store` 的透传 ⇒ 运行时配置探针 RED。

**INV-CAND-4 — Registered Tool Is Classified（注册即有域归类）**
- **Property**：注册表中每个工具，其名称前缀（域）必须落在
  `SELF_DOMAINS ∪ WORKSPACE_DOMAINS ∪ ALLOWLIST_DOMAINS` 之一，**或**被显式登记为"域外需收紧"。
- **Mechanism**：域分类全覆盖断言（对 `iter_definitions()` 逐项判定）。
- **Evidence**：`subagent.spawn` 当前违反（域 `subagent` 未被分类）。
- **Negative Test**：移除 `"subagent"` ⇒ 断言 RED。

---

## 10. Repository Hygiene（仓库卫生）

- `git status --short -- pyharness/` = **17** —— 与本轮开始时**一致** ⇒ **未触碰任何生产代码**。
- 未改 `tests/`、未改 `LIMITATIONS.md`、未改 `README.md`。
- 未 commit、未 push。
- 全部探针位于 `%TEMP%\m4-probe\`（仓库外）；`%TEMP%\m4-mut\` 变异副本已在 M4 清理。

---

## 11. 观测记录（本轮附带发现，未定性为 GAP）

| 观察 | 说明 |
|---|---|
| `PERS-202 I/O operation on closed file` | 在**故意于审批挂起中调用 `spine.close()`** 的调试脚本里出现 —— 属"关闭时仍有在途任务"的场景，非正常路径。**未进一步定性**，仅记录。 |
| child 会话 `tool.call=2 / tool.result=0` | 子会话内工具调用未产出结果（伴随 `TLB-802 目录不存在`）。可能是我的探针脚本给的 path 不适用于子 workspace 根。**未定性**，不阻塞 N2-b 结论（该结论只依赖 `evidence.archived=0`）。 |
| `TLB-802 tool=session.fts_query` 告警 | 每个引擎探针都会出现；FTS 索引在部分路径未挂载。**既有噪音，非本轮发现**。 |

---

## 12. 最终问题：是否已足够进入 M5 Remediation（第五阶段修复）？

### 结论：**是，可以进入 M5。**

四项优先级的运行时证据均已充分：

| 优先级 | 原证据等级 | 本轮后 | 充分性 |
|---|---|---|---|
| REOPENED-GAP-08 | 单点复现（normal vs wake-raises） | **五终态完整表 + 根因定位（触发点 vs 段生命周期）** | ✅ 充分 |
| N1 | 配置 3.0/7 → 0.5/64 | **链路断点精确到唯一一跳 + DI 四维评估 + 根因** | ✅ 充分 |
| N2 | 仅读码 | **运行时双证：strict 不可达 + standard 下子会话零证据** | ✅ 充分，且**发现更严重的前置缺陷** |
| N5 | 三处读码 | **7×6 分层矩阵 + 同文件规则冲突运行时验证 + 端到端功能级双路径复现** | ✅ 充分 |

其余项（N3 合并判定、N4 重分类、N6/N7 合并判定、N8 定性、N9 影响面）也均已按根因完成判定。

### 仍缺的运行时证据（不阻塞 M5，但应记入 M5 的完成判据）

1. **N5 的真实 ACP 外壳端到端**：本轮用 `attach_engine_to_ctx(channel="acp:c1")` **模拟**了
   真实 ACP 装配（`acp.py:388` 的实际取值路径），但**未驱动真实 ACP JSON-RPC 会话**。
   → M5 应补：`initialize → chat` 真实驱动 ACP，断言 `danger≥high` 工具能走到审批。
2. **N2-b 的父→子证据链**：本轮证明了"子会话零证据"，但**未验证修复后**父会话能通过
   `subagent.joined` 摘要侧看到子证据 —— 这属 M5 的验收点。
3. **取消路径的真实时序**：本轮用 `loop.wake` 注入 `CancelledError` **结构性**模拟取消，
   未做"真实 `task_queue.cancel(tid)` 竞态窗口"测试。→ M5 的负向测试应覆盖。
4. **CI / 原生壳**：GAP-3 / GAP-6 仍为 **Environment Blocked**，与 M5 无关，但 M5 结束时
   不应把它们计入"已闭合"。

### M5 修复顺序（最终）

```
1. N5   Channel Contract        （P2，无前置，一处修正消 3 项）
2. N2-a 域可见性                 （P1，无前置，一行 + 不变量）
3. GAP-08R Evidence Lifecycle   （P2，依赖 2；同时消 N3）
4. N2-b 子代理证据               （P2，依赖 2+3）
5. N1   Flush 配置接线           （P2，独立）
6. N4+N9 契约陈述与文档一致性     （P3，独立）
7. N8  死导出加注                （P3，独立）
```

**M5 门槛（进入前须确认）**：用户须**显式授权**该顺序与最小补丁范围（M2 产物已在上文 §9.D 给出）；
**每项修复须配**：单元 → 运行时 → E2E → 负向 四层证据，并**重跑本轮的四个探针**做 Re-Verification（修复后重新验证）。

---

_文档时间戳：2026-09-20T23:01:56+08:00_
