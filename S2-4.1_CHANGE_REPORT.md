# S2-4.1_CHANGE_REPORT.md — 强同步真源收敛实施报告

> **阶段**：S2-4.1（按 [S2-4_SYNC_CONVERGENCE_DESIGN.md](S2-4_SYNC_CONVERGENCE_DESIGN.md)，裁定 **Y-1(a) 机械派生**）
> **日期**：2026-09-14 ｜ **基线 HEAD**：`0cb841d`
> **性质**：**未 commit**。关闭 ADR-019 **P-3**。**未修 F-SYNC-1**（按裁定，另立 durability 阶段）。

---

## 0. 结论

# S2-4.1 = PASS

**改动 4 文件（+129 / −12）**；全量回归 **1481 collected / 1479 passed / 2 skipped / 0 failed**（较 S2-3.2 基线 1476/1474 **+5，零回归**）。
**两处第二真源已删除**，两适配器统一派生自 `events.vocab.SYNC_TYPES`；**源码层禁止面全部为 0**。

---

## 1. 修改文件列表

| 文件 | 增删 | 内容 |
|---|---|---|
| **`pyharness/engine.py`** | **+9 / −6** | `_record_to` 内**删除** 11 名硬编码 `_SYNC`；改为函数内 `from pyharness.events.vocab import SYNC_TYPES` → `sync=type_ in SYNC_TYPES`；docstring 记明 P-3 收敛与漂移史 |
| **`pyharness/desktop/sessions.py`** | **+4 / −6** | **删除** `:151-155` 的 11 名内联 tuple；`from pyharness.events import EVENT_TYPES, SYNC_TYPES`（`:16`）→ `sync=type_ in SYNC_TYPES`；**sid 过滤保持不变** |
| **`tests/unit/test_engine.py`** | **+32 / −0** | 新增 `test_s24_engine_adapter_derives_sync_from_vocab`：对**全部 74 个词表类型**断言 `sync == (type in SYNC_TYPES)`，并点名 `policy.updated`=True / `scope.updated`=False |
| **`tests/unit/test_persistence.py`** | **+84 / −0** | 新增 4 个用例：① 集合成员资格（`policy.updated` 在 / `scope.updated` 不在）② **desktop 适配器行为断言**（未绑定方法驱动 `_attach_persistence` + 异会话过滤）③ **AST 防回归守卫**（4 个适配器文件不得再出现硬编码强同步清单）④ **派生来源断言**（两文件必须引用 `SYNC_TYPES` 且无 `_SYNC = (`） |

```
 pyharness/desktop/sessions.py  | 10 ++---
 pyharness/engine.py            | 15 +++++---
 tests/unit/test_engine.py      | 32 ++++++++++++++++
 tests/unit/test_persistence.py | 84 ++++++++++++++++++++++++++++++++++++++++++
 4 files changed, 129 insertions(+), 12 deletions(-)
```

---

## 2. 测试结果

| 步骤 | 命令 | 结果 |
|---|---|---|
| 相关测试 | `pytest tests/unit/test_persistence.py tests/unit/test_engine.py tests/unit/test_events.py tests/unit/test_governance_policy.py` | **95 passed** |
| 全量 | `pytest`（可运行集） | **1481 collected / 1479 passed / 2 skipped / 0 failed**，exit 0 |

*2 条 skip = 既有平台相关（`test_spill`），与本次无关。留证：`tmp/baseline/junit_s2_4.xml`*

### 2.1 防回归测试（按你的步骤 5 三项要求）

| 要求 | 落地用例 | 断言 |
|---|---|---|
| **不允许新增硬编码 sync tuple** | `test_s24_no_hardcoded_sync_list_in_adapters` | AST 扫描 `engine.py` / `desktop/sessions.py` / `cli.py` / `core/orchestration.py`：任何"元素全为字符串常量、≥3 个、且**全体 ⊆ SYNC_TYPES**"的 tuple/set/list 即视为第二真源 → 失败 |
| **`policy.updated` 在同步集合中** | `test_s24_sync_set_membership` + 适配器逐类型断言 | `"policy.updated" in SYNC_TYPES`；engine/desktop 适配器对其 `sync=True` |
| **`scope.updated` 不在同步集合中** | 同上 | `"scope.updated" not in SYNC_TYPES`；适配器对其 `sync=False` |

**谓词设计说明**：AST 守卫用"**全体 ⊆ SYNC_TYPES**"而非"含事件名"，以避开 `engine._EVENT_TYPES_OR_ALL()` 的 23 名**词表兜底** tuple（它是 `SYNC_TYPES` 的超集，故不误报）——即守卫只盯"**强同步清单**"，不误伤词表兜底。

---

## 3. ⚠️ 行为变化（如实标注）

你裁定的理由含"**保持 S2 阶段零行为变化原则**"。**这一点需要精确修正**：

| 事件 | 收敛前 | 收敛后 | 是否变化 |
|---|---|---|---|
| 既有 11 个强同步事件 | 适配器 `sync=True` | 适配器 `sync=True`（同值） | **无变化** |
| **`policy.updated`** | 适配器 `sync=False`（不在旧副本里） | 适配器 **`sync=True`**（真源含它） | **✅ 有变化** |
| 其余 62 个非强同步类型 | `False` | `False` | 无变化 |

**该变化的确切后果**：
1. `policy.updated` 的 store 写入由"**入 pending 攒批**（随后被 `session.append` 的 `_flush` 收口）"变为"**适配器内即时 `_flush_pending_all`**"——即与其余 11 个强同步事件**行为一致**。这正是"收敛"的目标。
2. **耐久性不变**：两条路径都在同一次 `append` 调用内落盘（此前我在 S2-2.1 已核验 `session.append:321` 对 `SYNC_TYPES` 成员独立 `_flush`）。
3. **代价（已知且已登记）**：`F-SYNC-1`（适配器内 flush 失败 → 丢弃该行 + 被总线 EVT-103 隔离 → 调用方静默）**随之覆盖到 `policy.updated`**。按你的裁定**本阶段不修**，登记见 §4。

> **⇒ 精确表述**：本次收敛对"既有 11 类"零行为变化，对"`policy.updated`"是**有意的行为对齐**（使它与其余强同步事件同类）。若严格要求"零变化"，应由 **Y-1(b)** 达成（本阶段未采纳）。

---

## 4. `F-SYNC-1` 登记（独立 P1 —— 本阶段**不修**）

| 项 | 内容 |
|---|---|
| **编号** | `F-SYNC-1`（本文档为登记载体） |
| **级别** | **P1**（数据完整性） |
| **现象** | 强同步事件在**适配器内** flush 失败时：该行被 `_flush_pending_all` **丢弃**（不入 `_retry_q`）；异常被总线**隔离为 `EVT-103`**（不外抛）→ `session.append` 步骤 9 面对空 pending 静默通过 → **`append` 返回成功但事件未落盘** |
| **触发条件** | 强同步事件落盘时发生磁盘故障（`OSError`） |
| **影响面** | 全部 **12 个**强同步事件 × **4 条**适配器路径（engine / desktop / cli / orchestration，后两者同样传 `sync=True`）；S2-4.1 后 **`policy.updated` 亦在内** |
| **实证** | 已复现（毒化 `_fh.write` 抛 `OSError` + 真实 `session.append` 路径）：无异常 / 行不在 pending / `retry_q=0` / 文件 0 字节 |
| **留痕（不完全静默）** | 总线会记一条 `EVT-103` 结构化错误日志；但**无错误码上抛、无重试、无恢复路径** |
| **不在既往审计** | `REFACTOR_PLAN` / S1 / S2-1 / S2-2.1 / S2-3 报告均未记录 → **本轮核验新发现** |
| **本阶段动作** | **仅登记**（按裁定）；`persistence.py` / `session.py` / `event_bus.py` **未改** |
| **待办** | ① 后续单独进入 **durability/reliability 评估**；② 建议届时补一条 **marker 红测试**（设计 §E.1 的 T5）复现该缺口，作为修复的验收锚 |
| **登记位置说明** | ⚠️ 你要求"登记为独立 P1 finding"，但 `docs/KEY-FINDINGS.md` **不在本阶段允许修改范围内** → 故登记**落在本报告**；建议在后续允许的阶段（或 S2-6 收敛报告时）转入 `KEY-FINDINGS.md` 长期归档 |

---

## 5. 边界确认

| 项 | 状态 | 证据 |
|---|---|---|
| 允许文件（4） | ✅ 恰为改动集 | `git diff --name-only` = `engine.py` · `desktop/sessions.py` · `tests/unit/test_engine.py` · `tests/unit/test_persistence.py` |
| **禁止：`pyharness/persistence.py`** | ✅ **0 改动** | 定向 `git diff --name-only -- pyharness/persistence.py` → 0（注意：`tests/unit/test_persistence.py` 是**测试**，在允许清单内） |
| **禁止：`session.py`** | ✅ 0 | 同上核验 |
| **禁止：`event_bus.py`** | ✅ 0 | 同上核验 |
| **禁止：`events/*`** | ✅ 0 | 同上核验 |
| **禁止：`governance/*`** | ✅ 0 | 同上核验 |
| **禁止：ADR** | ✅ 0 | `git diff --name-only -- "ADR-*"` → 0 |
| **未顺手修 F-SYNC-1** | ✅ | 未触碰 persistence/session/event_bus |
| **未 commit** | ✅ | 工作区改动待 checkpoint |
| 仓库根无异常产物 | ✅ | 无 `exists.txt` 等 |

---

## 6. S2-4 验收对表

| 判据（设计 §E.3） | 状态 |
|---|---|
| ① `engine` 适配器的 sync 清单 == `SYNC_TYPES` | ✅ 已删除本地清单，改为引用真源；**逐 74 类型**行为断言通过 |
| ② 两适配器对 `policy.updated` 与 11 个既有类型取值**一致**（与真源一致） | ✅ engine + desktop 各有点名断言（`policy.updated`=True、`scope.updated`=False） |
| ③ 全量回归绿 | ✅ 1479 passed / 0 failed |
| ④ AST 守卫通过：源码中无第二份硬编码 sync 清单 | ✅ `test_s24_no_hardcoded_sync_list_in_adapters` |

**另**：`cli.py` / `core/orchestration.py` 经守卫扫描确认**本就派生**，无需改动（设计 §C.3 的预判成立）。

---

## 7. 下一步（待授权）

| 步 | 内容 |
|---|---|
| **S2-5** | `scope.updated` 面确认（Q1 分工）+ 对齐过期注释（`scope.py:41` 称"尚未入词表"实际已注册；`vocab.py:176` 的记账算术不闭合） |
| **S2-6** | S2 收敛报告（判据 ①②③ 汇总）+ 提交 checkpoint |
| （另立） | `F-SYNC-1` 的 durability/reliability 评估阶段 |

**等待人工 checkpoint。**
