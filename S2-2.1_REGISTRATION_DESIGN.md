# S2-2.1_REGISTRATION_DESIGN.md — `policy.updated` 注册变更设计

> **阶段**：S2-2.1（**只出设计，不落码**）
> **日期**：2026-09-14 ｜ **基线 HEAD**：`327a87b`（工作区 clean）
> **依据**：[ADR-020-policy-event-boundary.md](ADR-020-policy-event-boundary.md) · [S2-1_CHANGE_REPORT.md](S2-1_CHANGE_REPORT.md) · [S2-1_EXIT_REVIEW.md](S2-1_EXIT_REVIEW.md)
> **性质**：**未修改任何文件**。本文件按你的要求先输出：文件列表 / 当前事件注册位置 / payload 结构 / 风险点。

---

## 0. 结论摘要

**设计已完成，但落码前需裁定 3 项，且存在 1 项 P0 阻塞（与你的文件范围限定冲突）。**

| # | 类型 | 内容 |
|---|---|---|
| **X-1** | **与你给的字段清单冲突** | 你把 **`config_ref`** 列入 payload，而 **ADR-020 Δ-6 明确"`config_ref` 走 `Envelope.trace`，**不修改 payload schema**"**（该 ADR 已提交，`327a87b`） |
| **X-2** | **新增字段** | **`policy_refs`** 不在 ADR-020 / `POLICY_UPDATED_FIELDS` / 冻结设计 §2.3 中——属**新增字段，需决策** |
| **X-3** | **遗漏字段** | 你的清单**缺 `added` 与 `reason`**；而**已提交的 `emit_updated` 会构造这两个字段**，且 `reason` 是它的**必填参数**、又是 **INV-G6 要求的审计内容** |
| **X-4** | **P0 阻塞（文件范围）** | 词表被 `tests/unit/test_events.py` **锁死为恰好 73 名**（3 条断言）→ **不改该文件，S2-2 必然让测试变红**；但它**不在你允许修改的 2 个文件内**。同理，若采纳 X-1/X-2/X-3，还需改 `governance/policy.py`，**同样不在允许清单内** |

> **你的指令内部亦不一致**：你写"字段必须与 ADR-020 / `POLICY_UPDATED_FIELDS` 一致"，但列出的字段集**与两者都不同**。故必须由你裁定以哪边为准。

---

## 1. 前置事实：当前事件注册位置（**已实测定位，不假设**）

| 事项 | 实际位置 | 现况 |
|---|---|---|
| **事件注册表** | `pyharness/events/vocab.py:180` `_CORE_EVENT_TYPES: tuple[tuple[str, type[BaseModel], bool], ...]` | 元素 = `(type, payload_model, transient)`；**追加点 = `vocab.py:265-267`**（E 组尾部，`("config.updated", ConfigUpdatedPayload, True)` 之后） |
| **公开词表别名** | `vocab.py:143-144` `EVENT_TYPES = _EVENT_REGISTRY` | 由 `_install_core_vocabulary()`（`vocab.py:272`）遍历注册表装配 —— **无需手工改** |
| **payload 具名 import 名单** | `vocab.py:18` 起 `from pyharness.events.payload import (...)` | **显式名单**（非 `*`）→ 新模型**必须加名**，否则注册行 `NameError` |
| **强同步清单** | **`vocab.py:96-101`** `SYNC_TYPES = frozenset({...})` | **11 项**（`user.message` / `guard.rejected` / `approval.{requested,granted,denied,timeout}` / `session.{finished,recovered}` / `segment.start` / `fork.created` / `context.compacted`） |
| 瞬态清单 | `vocab.py:104` `TRANSIENT_TYPES` | **不含** `policy.updated`（它是持久事件，**不加**） |
| payload 基类 | `payload.py:18-19` `_PayloadBase(BaseModel)`，`ConfigDict(extra="forbid")` | 严格模式：**多余字段 → EVT-100 拒写** |
| payload 导出 | `payload.py` 末尾 `__all__ = [name for name in globals() if name.endswith("Payload") ...]` | **自动收集**——新模型无需改 `__all__` |
| 参照模型 | `payload.py:561-566` `ScopeUpdatedPayload` | `op: str = Field(min_length=1)` + `added: list[str] = Field(default_factory=list)` + `reason: str = Field(min_length=1)` |
| 词表规模注释 | `vocab.py:176-178`（写"70 事件类型"）· `payload.py:5`（写"57 事件词表"） | 均**与实际不符**（实测 73）——既有文档漂移 |

### 1.1 已提交的消费方（**关键约束**）

`governance/policy.py` 的 `emit_updated()`（**已在 Commit 1 `e23484a` 提交**）构造：

```python
payload = {"policy_id": …, "version": …, "fingerprint": …,
           "op": op,
           "added": sorted(policy.disabled) if op == "disable" else [],
           "reason": str(reason)}
trace   = {"config_ref": str(config_ref)} if config_ref else None
await session.append(EVENT_POLICY_UPDATED, payload, actor="system",
                     sync=True, trace=trace)
```

且 `POLICY_UPDATED_FIELDS = ("policy_id","version","fingerprint","op","added","reason")`（同文件常量，S2-1 声明为"S2-2 的 payload 模型须与之一致"）。

**⇒ 这是 X-1/X-2/X-3 的判据来源：payload 模型的字段集必须能容纳 `added`/`reason`，且 `config_ref` 不在 payload。**

---

## 2. P0 阻塞：词表被测试锁死（X-4 的实证）

`tests/unit/test_events.py` 用**显式清单**锁死词表：

| 行 | 内容 |
|---|---|
| `:62` | `assert len(EXPECTED_ALL) == 73, "测试词表清单必须恰为 73 名"` |
| `:79` | `assert len(EVENT_TYPES) == 73` |
| `:80` | `assert set(EVENT_TYPES) == set(EXPECTED_ALL)` |
| `:77` | 函数名 `test_vocab_full_64`（名与实不符：docstring 写 73） |

**⇒ 注册 `policy.updated` 后，这 3 条断言必然失败**（`EVENT_TYPES` 变 74，且 `EXPECTED_ALL` 不含新名）。

**这不是"可以顺手改测试"的问题，而是范围冲突**：你在 S2-2 明确"允许修改：① `payload.py` ② `vocab.py`"，并同时要求"保持向后兼容 / 测试通过"。二者**在本项目不可兼得**——因为该项目**刻意**把词表在测试里显式枚举（该断言本身就是"词表变更必须显式登记"的守卫）。

**⇒ 必须把 `tests/unit/test_events.py` 加入允许范围**（改动很小：清单 +1 名、`73`→`74` 两处、可选改函数名）。

---

## 3. 推荐设计（**Option A：ADR-020 一致**）

> 依据：ADR-020 是**已提交且经人工确认**的架构决策；Option A **零 ADR 变更、零已提交代码变更**。

### 3.1 payload 结构（`pyharness/events/payload.py` 追加）

```python
class PolicyUpdatedPayload(_PayloadBase):
    """policy.updated:治理层策略集变化(ADR-020)。

    op ∈ {add, enable, disable}(tighten 归 scope.updated,禁止两事件表达同一
    变化);config_ref 经 Envelope.trace 携带(Δ-6),**不入本载荷**。
    六字段与 governance.policy.POLICY_UPDATED_FIELDS 一致(S2-1 声明)。
    """
    policy_id: str = Field(min_length=1)
    version: str = Field(min_length=1)
    fingerprint: str = Field(min_length=1)
    op: str = Field(min_length=1)          # 值域校验在治理层(见 §3.3 设计理由)
    added: list[str] = Field(default_factory=list)
    reason: str = Field(min_length=1)
```

**逐字段来源**（与已提交代码严格对齐）：

| 字段 | 生产值 |
|---|---|
| `policy_id` | `Policy.policy_id`（缺省 `"builtin:v1"`） |
| `version` | `Policy.version`（缺省 `"1.0.0"`） |
| `fingerprint` | `Policy.fingerprint`（内容哈希） |
| `op` | `{"add","enable","disable"}`（`POLICY_OPS`） |
| `added` | `op=="disable"` → `sorted(policy.disabled)`；否则 `[]` |
| `reason` | `emit_updated(..., reason=...)`（**必填参数**；`assembly` / `guard-disabled` / `guard-enabled`） |

### 3.2 `vocab.py` 三处改动（**均为只增**）

| # | 位置 | 改动 |
|---|---|---|
| 1 | `vocab.py:18` 具名 import 名单 | 追加 `PolicyUpdatedPayload,`（保持字母序） |
| 2 | `vocab.py:265-267`（E 组尾部） | 追加注册行 `("policy.updated", PolicyUpdatedPayload, False),` + 一行注释（注明 ADR-020） |
| 3 | **`vocab.py:96-101` `SYNC_TYPES`** | 追加 `"policy.updated",`（依据 Q3：强同步） |

**向后兼容性**：`_CORE_EVENT_TYPES` 为**元组追加**，既有行的**次序与内容不变** → **不改变任何已有事件的编号/语义/载荷**；`EVENT_TYPES` 由既有装配函数遍历生成 → 无需改。`TRANSIENT_TYPES` **不动**。

### 3.3 设计理由（两处刻意的取舍）

1. **`op` 用 `str` 而非 `Literal["add","enable","disable"]`**：
   - 用 `Literal` 就需在 `events` 层**重复**治理层的 `POLICY_OPS` → **第二套真源**（违反 ADR-018 的单一真源纪律）；
   - 让 `events.payload` import `governance.policy` 取 `POLICY_OPS` → 形成 **`events` → `governance` 反向依赖**（与 ADR-018:308 方向相反，且 ADR-018 已因 `inputs_digest` 立过"不得引入第二套"的同类纪律）；
   - 既有 `ScopeUpdatedPayload.op` 同为 `str` + `min_length=1` → **与最近邻先例一致**。值域由治理层 `emit_updated` 校验（S2-1 的 T6 已锁定）。
2. **`reason` 为必填**（`min_length=1`）：它是 INV-G6「可审计追踪」的**审计内容本身**（谁在何因下放宽/启用）。设为可选会让"无理由的治理动作"合法化。
3. **`added` 为可选（`default_factory=list`）**：`op=add/enable` 时为空列表；与 `ScopeUpdatedPayload.added` 同型。

### 3.4 文件清单（Option A）

| 文件 | 动作 | 在允许范围？ |
|---|---|---|
| `pyharness/events/payload.py` | 追加 `PolicyUpdatedPayload` | ✅ 允许 |
| `pyharness/events/vocab.py` | ① import 名 ② 注册行 ③ `SYNC_TYPES` | ✅ 允许 |
| `tests/unit/test_events.py` | `EXPECTED_ALL` +`"policy.updated"`；`73`→`74` 两处；**建议**把 `test_vocab_full_64` 更名为 `test_vocab_full` | ❌ **需你加入允许范围**（见 X-4） |
| `tests/unit/test_governance_policy.py` | **可选**：新增"注册后 `emit_updated` 真落盘"用例（把 S2-1 的 T6b 从"降级断言"升级为"落盘断言"） | ❌ 同上（建议纳入） |

**Option A 下 `governance/policy.py` 与 `engine.py` / `scope.py` / `desktop/sessions.py` 均不需改动** ✅。

---

## 4. Option B（**按你给的清单**）所需代价

若坚持 payload = `{policy_id, version, fingerprint, op, config_ref, policy_refs}`：

| 代价 | 说明 |
|---|---|
| **必须改已提交代码** | `governance/policy.py` 的 `emit_updated` 必须改（把 `config_ref` 从 trace 移入 payload、去掉 `added`/`reason` 或另安置、新增 `policy_refs` 生产值）——但**该文件不在你允许修改的清单内**，且它是 **S2-1 的交付物（Commit 1 `e23484a`）** |
| **必须新建 ADR 取代 ADR-020 的 Δ-6** | Δ-6 明文"`config_ref` 走 trace，**不修改 payload schema**"。ADR 只增不改 → 需 **ADR-021** 取代，**不能直接改 ADR-020** |
| **INV-G6 出现缺口** | 丢掉 `reason` ⇒ "放宽的可审计追踪"只剩 op + config_ref，**没有"为什么"**；而 `reason` 目前是 `emit_updated` 的必填参数，去掉它需要同时改签名 |
| **`policy_refs` 无生产者** | 现无任何代码计算"本次策略变化涉及哪些 `policy_refs`"（规则级 refs 由 `RuleDescriptor.policy_refs` 提供，但**变更面** ≠ **规则面**）→ 需先定义语义（是"全策略 refs"还是"受影响规则的 refs"？）再实现 |
| **测试同样变红** | X-4 的 P0 依然存在（`test_events.py` 必须改） |

**评估**：Option B 需要 **1 条新 ADR + 改 1 个已提交源码 + 改签名 + 新增语义未定的字段**，且**削弱 INV-G6 的审计内容**。**不建议**。

---

## 5. 风险点

| # | 风险 | 等级 | 说明与缓解 |
|---|---|---|---|
| **R1** | **`SYNC_TYPES` 加入后，2 处硬编码副本变“不一致”（漂移，非持久性缺口）** | 中 | `SYNC_TYPES` 有 **2 处硬编码副本**：`engine.py:641` 的 `_SYNC`、`desktop/sessions.py:151` 的内联 tuple（ADR-019 §2.3 实测；另 3 处已从 `SYNC_TYPES` 派生）。本步**禁止**改这两个文件 → 加入 `SYNC_TYPES` 后这 2 处副本与新词表**不同步**。<br>**但不构成持久性缺口**：`session.append`（`session.py:321`）对 `env.type in SYNC_TYPES` 的成员**独立调用 `_flush`**，而 `persistence.SessionStore.SYNC_TYPES` 直接复用词表（`persistence.py:280`）→ `policy.updated` 仍会被强同步落盘。两处副本只影响它们传给 `store.append` 的 `sync=` 提示位（即时写 vs 攒批），随后被同一次 append 的 `_flush` 收口。<br>**⇒ 真正的后果是"维护性漂移"**：本步结束后词表与那 2 处副本不一致，须在 **S2-4** 收敛（ADR-019 的 P-3） |
| **R2** | 忘记改 `EXPECTED_ALL` → 3 条断言红 | **P0** | 见 X-4；这是**必然发生**的红，不是"可能" |
| **R3** | 忘记在 `vocab.py:18` 的具名 import 名单加名 → `NameError` | 高 | 该名单是显式的（非 `*`）；落地时须一并加 |
| **R4** | 词表规模注释（`vocab.py:176` 写 70 / `payload.py:5` 写 57）继续漂移 | 低 | 本步可顺手校正为 74（属注释，不改语义）；**建议纳入**（S0 的 R-F 同源） |
| **R5** | `emit_updated` 的 `reason` 若传入空串 | 低 | payload 模型 `reason: min_length=1` 会拒（EVT-100）→ 与 INV-G6 一致；`emit_updated` 默认值非空（`assembly`/`guard-disabled`/`guard-enabled`），无实际触发路径 |
| **R6** | 幂等路径不发事件（`with_rule_disabled` 已禁用 → 返回原对象，不 emit） | — | 设计使然（防刷日志，与 `scope.note_tighten` 幂等同款）；非风险，仅记录 |

---

## 6. 待你裁定（3 项，落码前）

| # | 待裁定 | 我的建议 |
|---|---|---|
| **X-1** | `config_ref` 在 payload 还是 trace？ | **trace**（= ADR-020 Δ-6，已提交；且与 `tools_guard` 的 `call_id`、`approval` 的 `channel` **两处既有先例同款**）。若坚持入 payload → 需 **ADR-021 取代 Δ-6** |
| **X-2** | 是否新增 `policy_refs` 字段？ | **不新增**（不在 ADR-020 / 冻结设计 §2.3；且**语义未定**——"变更面"与"规则面"需先定义）。若确需 → 单独决策 + 新增字段（须可选，保向后兼容） |
| **X-3** | `added` / `reason` 是否保留？ | **保留**（已提交的 `emit_updated` 会构造它们；`reason` 是 INV-G6 的审计内容；`POLICY_UPDATED_FIELDS` 已声明六字段） |
| **X-4** | 是否把 `tests/unit/test_events.py`（必需）与 `tests/unit/test_governance_policy.py`（建议）纳入允许范围？ | **纳入**——否则"测试通过"与"只改 2 文件"不可兼得 |

**⇒ 建议按 Option A 落码，允许范围 = `payload.py` + `vocab.py` + `tests/unit/test_events.py`（必需）+ `tests/unit/test_governance_policy.py`（建议）。**

---

## 7. 边界声明

| 项 | 状态 |
|---|---|
| **未修改任何文件** | ✅ 本步只出设计（本文件为唯一新增） |
| 未进入 S2-3（装配切换） | ✅ |
| 未接入 `ctx.governance` | ✅ |
| 未创建 Decision / Receipt / Evidence / Audit | ✅ |
| 未实现 `authorize()` | ✅ |
| 未修改 `engine.py` / `scope.py` / `desktop/sessions.py` | ✅ |
| 未修改 ADR-018 / `ARCHITECTURE_DECISION_RECORD.md` | ✅ |
| 未修改 ADR-020 | ✅（Option B 才需要 ADR-021 取代） |

**等待你对 X-1 ~ X-4 的裁定后再落码。**
