# S2-4.1_PRECHECK_REVIEW.md — checkpoint 前置复核报告

> **阶段**：S2-4.1 checkpoint 前置复核
> **日期**：2026-09-14 ｜ **基线 HEAD**：`0cb841d`
> **性质**：**只复核与文档修订**——**未修改任何代码**、**未 commit**、**未进入 S2-5**。
> **复核对象**：[S2-4_SYNC_CONVERGENCE_DESIGN.md](S2-4_SYNC_CONVERGENCE_DESIGN.md) · [S2-4.1_CHANGE_REPORT.md](S2-4.1_CHANGE_REPORT.md)

---

## 0. 结论

# 复核 = PASS（发现并修复 2 处文档不一致）

三项指定检查（§1）**全部通过**；另发现 **2 处两份文档间的不一致**（均**只在设计文档一侧**），已按"**取代记录**"体例**加注修复**（不改原建议文字、不动代码）。修复后重新核验：**代码与测试零变动**，相关测试 **95 passed**。

---

## 1. 三项指定检查

### 1.1 Y-1(a) 是否仍描述为"零行为变化"

**✅ 否，未发现此类表述。**

| 文档 | 相关表述 | 判定 |
|---|---|---|
| 设计文档 | 摘要即写"**但收敛不是行为中性的**"；§B.2 标题"**但"机械派生"不是行为中性的（Y-1 待裁定）**" | ✅ 从未声称零行为变化 |
| `S2-4.1_CHANGE_REPORT.md` | §3 标题"**⚠️ 行为变化（如实标注）**"，并**主动修正**了裁定理由中的"保持 S2 零行为变化"一说 | ✅ 表述精确 |

### 1.2 `policy.updated` 由非 SYNC → SYNC 的行为变化是否明确记录

**✅ 已明确记录。** `S2-4.1_CHANGE_REPORT.md` §3 给出三行对照表（既有 11 类**无变化** / **`policy.updated` 有变化** / 其余 62 类无变化），并逐条说明：

1. 写路径由"入 pending 攒批 + session 层 flush"变为"适配器内即时 `_flush_pending_all`"（**与其余 11 类一致** —— 这正是收敛目标）；
2. **耐久性不变**（两条路径都在同一次 `append` 内落盘）；
3. **代价已登记**：`F-SYNC-1` 随之覆盖 `policy.updated`（本阶段不修）。

并给出结论行：**"本次收敛对既有 11 类零行为变化，对 `policy.updated` 是有意的行为对齐；若严格要求零变化，应由 Y-1(b) 达成（本阶段未采纳）"**。

### 1.3 `F-SYNC-1` 是否明确标记为后续 durability 范围、不进入 S2

**✅ 已明确标记（4 处）**：报告 §0 性质行"**未修 F-SYNC-1（按裁定，另立 durability 阶段）**"；§3.3"按你的裁定**本阶段不修**"；§4 专表"**本阶段动作：仅登记**"+"待办① 后续单独进入 durability / reliability 评估"；§7"（另立）`F-SYNC-1` 的 durability/reliability 评估阶段"。**且代码侧已核验**：`persistence.py` / `session.py` / `event_bus.py` **零改动**。

---

## 2. 发现的不一致（2 处，均已修复）

### I-1｜设计文档未记录 Y-1 的**裁定结果**（仍读作"待裁定 / 建议 (b)"）

| 项 | 内容 |
|---|---|
| **位置** | `S2-4_SYNC_CONVERGENCE_DESIGN.md` §B.2 标题（"**Y-1 待裁定**"）· §B.2 末"**建议 Y-1(b)** … **需你裁定**" · §E.4 表"建议 **Y-1(b)**" |
| **问题** | 裁定已**采纳 (a)**（见变更报告），但设计文档未记录 → **单读设计文档的读者会以为 (b) 被推荐且决策未定**，与变更报告冲突 |
| **处置（加注，不改原文）** | ① §B.2 追加"**⚖️ 裁定结果（2026-09-14）**"块：记明**采纳 (a)**、人工给出的四条理由、并**明确"上表建议 (b) 未被采纳"**；② 同块内附**对理由②的精确修正**（"零行为变化"仅对既有 11 类成立；`policy.updated` 属有意对齐）——保持在设计文档内即可自洽；③ §E.4 表下追加"**⚖️ 已裁定：采纳 (a)（非上表建议）**"并说明本表保留原建议作决策前记录 |

### I-2｜设计文档的 **T5 未标注为"未交付"**

| 项 | 内容 |
|---|---|
| **位置** | `S2-4_SYNC_CONVERGENCE_DESIGN.md` §E.1 的 T5 行（`test_F_SYNC_1_silent_loss_hole`，标注"若采纳 Y-1(a)"） |
| **问题** | (a) 已被采纳 → 读者会预期 T5 存在；但**实施步骤清单未含它**，实际**未交付**（变更报告 §4 已说明建议随 durability 阶段补） |
| **处置（加注）** | T5 行追加"**⚠️ 本阶段未交付**（实施时未纳入步骤清单）——建议**随 durability / reliability 阶段**一并补上，作为修复的验收锚"，并指向变更报告 §4 待办② |

### 2.1 附带修正（同一体例）

`§E.4` 的"另建议：把 `F-SYNC-1` 登记进 `KEY-FINDINGS.md`"追加一行：**该建议未在本阶段执行**（`docs/KEY-FINDINGS.md` 不在 S2-4 允许范围），登记载体为变更报告 §4，待后续允许阶段转档。

> **修订纪律**：三处修订**全部为追加**，**未改写任何原建议/原结论文字**（沿用 ADR"只增不改、以取代记录表达变更"的体例）。

---

## 3. 修改后重新核验

| 项 | 结果 |
|---|---|
| **`git diff --stat`（源码/测试）** | **与修订前逐字一致**：`desktop/sessions.py` 10 / `engine.py` 15 / `test_engine.py` 32 / `test_persistence.py` 84 → **+129 / −12** ⇒ **文档修订未触碰代码** |
| **改动文件范围** | 恰为 4 个允许文件（2 源 + 2 测试） |
| **未跟踪文件** | 仅 `S2-4.1_CHANGE_REPORT.md` · `S2-4_SYNC_CONVERGENCE_DESIGN.md`（**S2-4 文档**） |
| **相关测试** | `test_persistence.py` + `test_engine.py` + `test_events.py` + `test_governance_policy.py` → **95 passed** |
| 禁止面（源码层） | `pyharness/persistence.py` · `core/session.py` · `bus/event_bus.py` · `events/**` · `governance/**` · `ADR-*` → **全 0** |

---

## 4. checkpoint 前状态

### 4.1 待提交清单（**建议两段式**）

| Commit | message（建议） | 范围 | 预估 |
|---|---|---|---|
| **1（代码）** | `refactor: converge sync source of truth to vocab.SYNC_TYPES` | `pyharness/engine.py` · `pyharness/desktop/sessions.py` · `tests/unit/test_engine.py` · `tests/unit/test_persistence.py` | +129 / −12 |
| **2（文档）** | `docs: record S2-4 sync convergence design and checkpoint` | `S2-4_SYNC_CONVERGENCE_DESIGN.md` · `S2-4.1_CHANGE_REPORT.md` · 本报告 | 新增 |

> **顺序**：Commit 1 → Commit 2（文档描述代码，单向依赖，同 S1/S2-1/S2-3 惯例）。

### 4.2 工作区状态

```
HEAD = 0cb841d（未前进）
git status --short → 4 M + 3 ??（无异常产物；无 exists.txt）
未 push（main...origin/main [ahead 8]）
```

---

## 5. 边界确认

| 项 | 状态 |
|---|---|
| **未修改代码** | ✅（修订仅 `.md`；`git diff --stat` 与修订前一致） |
| **未 commit** | ✅ |
| **未进入 S2-5** | ✅ |
| **未顺手修 F-SYNC-1** | ✅（`persistence.py`/`session.py`/`event_bus.py` 零改动） |
| 修订仅限 S2-4 相关文档 | ✅（`S2-4_SYNC_CONVERGENCE_DESIGN.md` 加注 3 处；`S2-4.1_CHANGE_REPORT.md` **未改**——其表述本就精确） |

**等待人工 checkpoint 授权。**
