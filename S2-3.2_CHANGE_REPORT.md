# S2-3.2_CHANGE_REPORT.md — 治理层装配切换实施报告

> **阶段**：S2-3.2（按 [S2-3.1_STAGED_DIFF_PLAN.md](S2-3.1_STAGED_DIFF_PLAN.md) 落码）
> **日期**：2026-09-14 ｜ **基线 HEAD**：`a6d097c`（工作区含本次改动）
> **性质**：**未 commit**。治理层进入**装配链**，**未进入运行链**；`authorize()` 未实现。

---

## 0. 结论

# S2-3.2 = PASS

**改动 3 文件（+240 / −7）**；全量回归 **1476 collected / 1474 passed / 2 skipped / 0 failed**（较 S2-2.1 基线 1468/1466 **+8，零回归**）。
**禁止面 10 项全部未触碰**；运行期**零新增一跳**（`tools_executor.py` 未改）；**`spine.guard is spine.governance.policy.chain` 成立（只一条链）**。

**落码过程中捕获并修复 2 个真实缺陷**（§4，其中 1 个被既有测试当场抓住）。

---

## 1. 修改文件列表

| 文件 | 增删 | 内容 |
|---|---|---|
| **`pyharness/engine.py`** | **+63 / −7** | ① 新增 `_build_governance(cfg, *, session, bus, tool_reg) -> (PolicyEngine, guard)`（纯提取子函数，含 `_guard_factory` 形参名适配）；② `build_runner_components` 的 guard 装配改为 `gov_policy, guard = _build_governance(...)`；③ `EngineSpine` 增 `governance: Any = None`；④ `EngineContext` 增 `governance: Any = None`；⑤ spine 构造传 `governance=GovernanceContext(policy=gov_policy)`；⑥ `attach_engine_to_ctx` 增 `ctx.governance = …`；⑦ `assemble_real_engine` 传 `governance=spine.governance` |
| **`pyharness/core/agent.py`** | **+1 / −0** | `create_agent` 增 `ag.ctx.governance = getattr(spine, "governance", None)`（**仅挂载，不改生命周期**） |
| **`tests/unit/test_engine.py`** | **+183 / −0** | 新增 8 个用例（T-A ×2 · T-B · T-C ×2 · T-D ×2 · F-1 守卫）+ 2 个 helper（`_gov_spine` / `_tc`）+ import 扩充 |

---

## 2. `git diff --stat`

```
 pyharness/core/agent.py   |   1 +
 pyharness/engine.py       |  63 ++++++++++++++--
 tests/unit/test_engine.py | 183 +++++++++++++++++++++++++++++++++++++++++++++-
 3 files changed, 240 insertions(+), 7 deletions(-)
```

未跟踪（**仅文档**）：`S2-3.0_ASSEMBLY_DESIGN.md` · `S2-3.1_STAGED_DIFF_PLAN.md`（+ 本报告）

---

## 3. 测试结果

| 步骤 | 命令 | 结果 |
|---|---|---|
| 3 | `pytest tests/unit/test_engine.py` | **15 passed** |
| 5 | `pytest tests/unit/test_events.py tests/unit/test_governance_policy.py tests/unit/test_engine.py` | **全绿** |
| 6 | `pytest`（可运行集） | **1476 collected / 1474 passed / 2 skipped / 0 failed**，exit 0 |

*2 条 skip = 既有平台相关（`test_spill` 的 chmod 0600 / symlink），与本次无关。*
*留证：`tmp/baseline/junit_s2_3_final.xml` · `tmp/baseline/pytest_s2_3_final.log`*

### 3.1 测试覆盖（按你指定的 T-A~T-D）

| 层 | 用例 | 断言要点 |
|---|---|---|
| **T-A** | `test_s23_ta_rules_match_chain` | `[r.rule_id for r in policy.rules] == [g.id for g in guard.chain]`；`enabled_rules()` == `enabled_guard_ids()`；`r.forced == (rule_id in FORCED_GUARDS)`；`r.policy_refs == _RULE_POLICY_REFS[id]`；装配态指纹可算且非空 |
| **T-A** | `test_s23_ta_cfg_disabled_reaches_policy` | `cfg.security.guards.disabled` **同时**进入 `policy.disabled` 与链的 `enabled_guard_ids()` |
| **T-B** | `test_s23_tb_single_chain_and_single_governance` | **`spine.guard is spine.governance.policy.chain`**（只一条链）；`ctx.guard is spine.guard`；`ctx.governance is spine.governance`；二次取 ctx 幂等 |
| **T-C** | `test_s23_tc_attach_path_mounts_same_governance` | `attach_engine_to_ctx`（CLI/ACP 门面）挂载**同一**实例 |
| **T-C** | `test_s23_tc_policy_rules_carry_injected_params` | 直接调**策略规则自己的** `check`：缺 `path` → `("reject","TLB-803")`（证明 `validator` 已到达描述面；若未到达则 g-schema 恒 allow、本例必红） |
| **T-D** | `test_s23_td_guard_behavior_smoke` | basic 档 7 例：g3 `..`逃逸→reject · g4 凭据→reject · g7 已存在→approval · 新建→allow · 合规读→allow · g6 basic 放行但 g2 high→approval · g5 空 allowlist→reject |
| **T-D** | `test_s23_td_strict_scope_precheck` | strict 档：`fs.delete_file`(critical) / `exec.shell_run` / `web.fetch` 均在 scope 前置**终局拒** |
| **F-1** | `test_s23_f1_rule_policy_refs_covers_builtin` | `set(_BUILTIN_IDS) <= set(_RULE_POLICY_REFS)`（封 `describe_rules` 的静默空 refs） |

---

## 4. 落码中捕获的 2 个真实缺陷

### D-1｜形参名不一致（`credential_paths` vs `credentials`）

- **现象**：`TypeError: from_config() got an unexpected keyword argument 'credential_paths'`
- **根因**：治理层契约（S2-1 设计 §3.2）用 **`credential_paths`**；而 **`tools_guard.from_config` 的实际形参是 `credentials`**。`PolicyEngine.from_config` 把 `credential_paths=` 透传给 `chain_factory` → 名不符。
- **修法**：在**装配层**加 `_guard_factory` 适配（映射 `credential_paths → credentials`）。✅ 符合 ADR-018:308——治理层不得 import `tools_guard`，故映射只能落在装配层。
- **语义等同性**：`credentials=None` 时 `from_config` 仍从 `cfg` 取默认清单 → 与 S1 行为一致（既有 S1-01 用例复跑通过即为证）。

### D-2｜**变量遮蔽**（CORE-04）——被既有测试当场抓住

- **现象**：既有 S1 用例 `test_build_spine_components_present` 失败 —— `fs.delete_file`（critical）**变成可见**。
- **根因**：`build_runner_components` 上游已有 **`policy = ScopePolicy()`**（scope 的策略面）。初版写成 `policy, guard = _build_governance(...)` → **遮蔽**该名字 → 紧随其后的 `policy.danger_marks = marks` 与 `_apply_preset(cfg, policy, …)` 全部作用到 **`PolicyEngine`** 上 → **scope 的 danger 分级与权限预设整体失效**。
- **修法**：改名 **`gov_policy`**，并在代码处留下注释警示。
- **副作用（已清理）**：该失败运行把 **`exists.txt` 写到了仓库根**——因为 `policy.workspace_root` 也落在了 PolicyEngine 上，`ScopePolicy.workspace_root` 为空 → `Path("")` = `.`。已删除，并**复验修复后重跑 T-D 不再产生**。
- **教训（值得记忆）**：在 `build_runner_components` 内新增局部变量时，**必须避开 `policy` / `scope` / `guard` 等既有名**。

> 这两个缺陷都**由测试网捕获**（D-2 由 S1 遗留用例、D-1 由新用例），说明既有回归网有效——也印证了 ADR-019 判"装配入口切换是高风险步"的判断。

---

## 5. 是否触碰禁止文件

| 文件 | 状态 |
|---|---|
| `core/tools_executor.py` | ✅ 未改（运行期零新增一跳） |
| `core/tools_guard.py` | ✅ 未改（保持 S2-1 只读描述边界；判定逻辑/执行权不动） |
| `core/scope.py` | ✅ 未改 |
| `core/approval.py` | ✅ 未改 |
| `core/session.py` | ✅ 未改 |
| `pyharness/events/*` | ✅ 未改（词表仍 74、`SYNC_TYPES` 仍 12） |
| `desktop/sessions.py` | ✅ 未改 |
| `governance/policy.py` | ✅ 未改 |
| `governance/context.py` | ✅ 未改（`authorize()` 未实现） |
| 任意 ADR / `ARCHITECTURE_DECISION_RECORD.md` | ✅ 未改 |

**核验**：`git diff --name-only` 仅 3 个文件（§1），禁止面命中 **0**。

---

## 6. 是否满足 S2-3 验收条件

| 约束（你的裁定） | 满足 | 证据 |
|---|---|---|
| **① Governance 进入装配链、不进入运行链** | ✅ | `tools_executor.py` 未改；`ctx.guard.evaluate` 调用点与签名不变；运行期无新增一跳 |
| **② 禁止 `engine → PolicyEngine.authorize() → execution`** | ✅ | `authorize()` 未实现（`governance/context.py` 未改）；`ctx.governance` 当前**零读者** |
| **③ 不允许创建第二条 GuardChain** | ✅ | **T-B**：`spine.guard is spine.governance.policy.chain`；新增 `_build_governance` 返回 `policy.chain` 作 guard，无第二次 `from_config` 直调 |
| **④ 拆分必须是纯结构拆分**（保创建顺序/参数来源/副作用顺序） | ✅ | `_build_governance` 为**纯提取**：调用点仍在原位置，`describe_rules` 与 `chain_factory` 用**同一份 `inj`**；既有 S1 装配用例全部通过（含 D-2 暴露后的复跑） |
| **⑤ `EngineSpine`/`EngineContext` 加字段须兼容**（`… | None = None`） | ✅ | 两处均为 `governance: Any = None`（带默认值）→ 5 处 `build_runner_components` 调用点与既有 fake 构造**无需改** |
| **⑥ `core/agent.py` 只加挂载，不改生命周期** | ✅ | **+1 行**，`getattr` 兜底 |
| **测试 T-A / T-B / T-C / T-D** | ✅ | 见 §3.1（并额外补了 T-C 的"参数同源"层与 F-1 守卫） |

### 6.1 S2 阶段判据（ADR §5.2 S2 行）

| 判据 | 状态 |
|---|---|
| ① 策略指纹随内容变化 | ✅ S2-1 已证（T1/T2）；本步 T-A 复证装配态可算指纹 |
| ② 一致性测试（治理层判定与 `tools_guard` 逐例一致） | ✅ **三层落地**：结构（T-A）+ 参数同源（T-C）+ 行为（T-D）<br>⚠️ **口径说明**：因 `tools_guard` 不改（X-5(a) 否决），`describe_rules` 自建 guard 实例 → **对象同一性不可断言**，故 T-C 以**行为等价**替代 |
| ③ 全量绿 | ✅ 1474 passed / 0 failed |

---

## 7. 下一步（待授权）

| 步 | 内容 |
|---|---|
| **S2-4** | sync 收敛：`engine.py:641` 的 `_SYNC` 与 `desktop/sessions.py:151` 的内联 tuple → 派生自 `SYNC_TYPES`（ADR-019 的 P-3；S2-2.1 遗留的维护性漂移） |
| **S2-5** | `scope.updated` 面确认（Q1 分工）+ 对齐过期注释（`scope.py:41`、`vocab.py:176` 的算术） |
| **S2-6** | 收敛报告 + 提交 checkpoint |

**等待人工 checkpoint。**
