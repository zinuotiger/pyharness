# S2-3.1_STAGED_DIFF_PLAN.md — S2-3 装配切换的 staged diff 计划

> **阶段**：S2-3.1（**只出计划，不落码**）
> **日期**：2026-09-14 ｜ **基线 HEAD**：`a6d097c`（工作区 clean）
> **依据**：[S2-3.0_ASSEMBLY_DESIGN.md](S2-3.0_ASSEMBLY_DESIGN.md)（X-0 已按"装配链"裁定）· [ADR-018](ARCHITECTURE_DECISION_RECORD.md) · [ADR-020](ADR-020-policy-event-boundary.md)
> **性质**：**未写代码、未修改文件、未 commit**。等待人工批准后落码。

---

## 0. 本轮新增的两条硬约束（来自你的裁定）

| 裁定 | 后果（**必须先说清**） |
|---|---|
| **治理层只进装配链** | 运行期**零新增一跳**：`ctx.guard.evaluate()` **调用点与签名都不变**（仍由 `tools_executor.py:449` 调用） |
| **`tools_guard.py` 不改** | **X-5(a) 被否决** → `describe_rules()` 内部自建一套 guard 实例 ⇒ **`policy.rules[i].check is not guard.chain[i].check`**（不同 `_FuncGuard` 对象）。⇒ **一致性断言只能做"结构 + 行为"两层，不能做"对象同一性"**。这直接决定 §3 的测试设计 |

**另：`authorize()` 不实现** —— `GovernanceContext` 不加任何方法（S3 补齐）。运行期**没有任何代码读 `ctx.governance`**（实测零读者）。

---

## 一、实际装配点（已读取确认）

| # | 位置 | 实测内容 | 本步是否动 |
|---|---|---|---|
| 1 | `engine.py:59-104` `class EngineSpine` | dataclass；必填字段 `session…storage`，`persistence` 之后为**带默认值**区（`sys/plan/schedule/jobs/…/budget/fts`） | **动**：默认区加 1 字段 |
| 2 | `engine.py:425-610` `build_runner_components` | **唯一装配漏斗**（被 5 处调用：`engine.py:327/873`、`application/service.py:215/253`、`desktop/app.py:506/553`）；guard 装配在 `:483-486`（`guard = guard_from_config(cfg, session=log_, bus=bus, validator=tool_reg.validate_args, approval_channel=True)`，import 在 `:465`） | **动**：装配改为经 `PolicyEngine` |
| 3 | `engine.py:595-598` `spine = EngineSpine(...)` | 含 `guard=guard, approval=approval,` | **动**：加 `governance=…` |
| 4 | `engine.py:860-902` `attach_engine_to_ctx` | `ctx.*` 赋值在 `:886-900`；`ctx.guard = spine.guard`（`:890`） | **动**：加 1 行 |
| 5 | `engine.py:164-184` `class EngineContext` + `engine.py:833-845` `assemble_real_engine` | 装配束（含 `guard=spine.guard, approval=spine.approval, …`），供桌面消费 | **建议动**（X-8，见 §2.1） |
| 6 | `agent.py:436-492` `create_agent` | ctx 装配：`ag.ctx = Ctx(...)`（`:461`）+ 25 处 `ag.ctx.* =`（`:468-489`）；`ag.ctx.guard` 在 `:471` | **动**：加 1 行 |
| 7 | `core/context.py` | ⚠️ **不存在**（S2-3.0 已指出）；`governance/context.py` 才是 `GovernanceContext` 所在，**本步不动** | 不动 |

**运行期链（S2-3 后仍逐字节相同）**：
`agent_loop.run_turn → ctx.tools.execute → 关2b ctx.guard.evaluate(call, scope)` —— **`tools_executor.py:449` 不改**。

---

## 二、修改文件列表

### 2.1 `pyharness/engine.py`

**修改目的**：把 guard 的构造改由 `PolicyEngine` 经注入的 `chain_factory` 完成，并挂载 `GovernanceContext`；**行为不变**。

| 预计位置 | 改动 |
|---|---|
| `:465-466`（import 段） | 追加 `from pyharness.core.tools_guard import describe_rules as _describe_rules` 与 `from pyharness.governance import GovernanceContext, PolicyEngine` |
| `:483-486` | `guard = guard_from_config(...)` → ① 一份 `inj = dict(validator=tool_reg.validate_args, credential_paths=None, path_exists=None, link_resolver=None, approval_channel=True)`；② `policy = PolicyEngine.from_config(cfg, session=log_, bus=bus, rules=_describe_rules(**inj), chain_factory=guard_from_config, **inj)`；③ `guard = policy.chain` |
| `:96`（`EngineSpine` 默认区） | 追加字段 `governance: Any = None` |
| `:595-598` | `spine = EngineSpine(..., governance=GovernanceContext(policy=policy), ...)` |
| `:890` 邻域 | 追加 `ctx.governance = getattr(spine, "governance", None)` |
| **X-8（建议）** `:164-184` + `:833-845` | `EngineContext` 加 `governance: Any = None` 字段；`assemble_real_engine` 里 `governance=spine.governance` |
| **X-6（可选）** `:425-610` | 拆出 `_build_governance(cfg, log_, bus, tool_reg, *, inj) -> tuple[Any, Any]`（返回 `policy/guard`），把上述装配收敛为 1 个子函数 |

**风险**：
- **R-A（高）**：若写成"先 `guard = guard_from_config(...)`、再 `PolicyEngine.from_config(...)`"→ **两条 `GuardChain`**（`spine.guard` 与 `policy.chain` 不同对象）。**必须** `guard = policy.chain`，禁止保留直调结果。
- **R-B（高）**：注入参数**不同源** → `describe_rules` 与 `chain_factory` 内部两条链的 guard 闭包烘焙了不同参数 → 治理层规则与实链判定**静默分歧**。**必须**用同一份 `inj`。
- **R-C（中）**：`EngineSpine` 新字段**必须带默认值**（否则 `EngineSpine` 必填区错位 → 构造 `TypeError`）。
- **R-D（中）**：`build_runner_components` 是三壳共享漏斗 → 按 CORE-02 全量回归。
- **R-E（低）**：`GovernanceContext(policy=…)` 的四个 `None` 占位字段保持（不得顺手实现）。

### 2.2 `pyharness/core/agent.py`

| 预计位置 | 改动 |
|---|---|
| `:471` 邻域（`ag.ctx.guard = …` 之后） | 追加 `ag.ctx.governance = getattr(spine, "governance", None)  # 治理层单实例(ADR-018;M2 只挂 policy)` |

**修改目的**：把治理层单实例挂到 agent 路径的 `ctx`（三壳的任务执行都经此）。
**风险**：**低**（1 行，`getattr` 兜底 → 未装配时 `None`，与既有 25 处同型）。

### 2.3 `tests/unit/test_engine.py`

> 你写"新增 `tests/unit/test_engine.py`"，但该文件**已存在**（S1 已建，含 `build_spine`/`_cfg`/`_sessions_dir` 模式）→ 本步是**追加用例**，不是新建文件。

| 追加用例 | 内容 |
|---|---|
| `test_governance_assembly_single_instance`（T-B） | `spine.guard is spine.governance.policy.chain`（**只一条链**，封 R-A）；`ctx.governance is spine.governance`；同会话二次 `_agent_ctx_of` → 同一 `GovernanceContext`；`attach_engine_to_ctx` 路径亦挂载 |
| `test_governance_rules_match_chain`（T-A） | `[r.rule_id for r in policy.rules] == [g.id for g in spine.guard.chain]`；`enabled_rules()` == `enabled_guard_ids()`；`r.forced == (r.rule_id in FORCED_GUARDS)`；`policy.rules[i].policy_refs == _RULE_POLICY_REFS[g.id]` |
| `test_governance_behavior_unchanged`（T-C） | 11 例语料（S2-3.0 §5.2）经 `spine.guard.evaluate` 断言**冻结期望值**（reject/approval/allow 与 policy_ref）——**"S2-3 前后行为一致"的可执行形式** |
| `test_rule_policy_refs_covers_builtin`（F-1） | `set(_BUILTIN_IDS) <= set(_RULE_POLICY_REFS)` |

**风险**：**低**；但注意 **T-A 不能断言 `check` 对象同一**（X-5(a) 已否决），只能断言结构 + 行为。

---

## 三、禁止修改文件（逐条确认）

| 文件 | 状态 | 依据 |
|---|---|---|
| `core/tools_executor.py` | **不改** ✅ | 四关结构/顺序冻结；`ctx.guard.evaluate` 调用点（`:449`）保持 |
| `core/tools_guard.py` | **不改** ✅ | 保持 S2-1 的 read-only descriptor 边界；判定逻辑与执行权不动 |
| `core/scope.py` | **不改** ✅ | 策略/预算面不动；`scope.updated` 仍 `op=tighten` |
| `core/approval.py` | **不改** ✅ | 审批契约不动（S4 才动 `_grant_slots`） |
| `core/session.py` | **不改** ✅ | `append` 契约与五步校验链不动 |
| `pyharness/events/*` | **不改** ✅ | 词表 74 型、`SYNC_TYPES` 12 项已定（S2-2.1） |
| `desktop/sessions.py` | **不改** ✅ | 走 `build_runner_components` 漏斗 → 自动获得 |
| `governance/policy.py` | **不改** ✅ | S2-1 已交付 `from_config/resolve/current/fingerprint/registry/chain` |
| `governance/context.py` | **不改** ✅ | M2 形状冻结；`authorize()` 不声明（Δ-5） |
| `pyharness/core/agent.py` | **仅加 1 行**（挂载面） | 唯一例外（装配点），非"执行权/契约"改动 |
| 任何 ADR / `ARCHITECTURE_DECISION_RECORD.md` | **不改** ✅ | 冻结 |

---

## 四、测试设计（三层 + 1 守卫）

> **口径提醒**：M2 的 `PolicyEngine` **无 `evaluate()`**（决策属 S3）→ "判定一致"必须落为**规则集同一性 + 行为等价**，而非"两个 evaluate 对比"。

### T-A｜规则集同一性（结构）
```
policy.rules 的 rule_id 序列  ==  guard.chain 的 id 序列        （含求值序）
policy.enabled_rules() 的 ids ==  guard.enabled_guard_ids()
policy.rules[i].forced        ==  (rule_id in FORCED_GUARDS)
policy.rules[i].policy_refs   ==  _RULE_POLICY_REFS[rule_id]
policy.disabled 的 ids        ==  cfg.security.guards.disabled
```
**不可断言**：`check`/`match` 对象同一（X-5(a) 否决策略下必然不同对象）。

### T-B｜`ctx.governance` 单实例（装配面）
```
spine.guard is spine.governance.policy.chain          ← 封 R-A（只一条链）
ctx.governance is spine.governance                    ← create_agent 路径
ctx2.governance is ctx.governance                     ← 二次 _agent_ctx_of 幂等
light_ctx.governance is spine.governance              ← attach_engine_to_ctx 路径
len([o for o in (spine.guard,) if o is spine.governance.policy.chain]) == 1
```

### T-C｜行为一致（`ctx.guard.evaluate()` 前后等价）
**可执行形式** = 对 11 例语料断言**冻结期望**（语料见 S2-3.0 §5.2）：

| 语料 | 期望 |
|---|---|
| `fs.read_file` 缺 `path` | `reject` / `TLB-803`（g-schema） |
| `fs.list_dir` + `../../outside` | `reject` / `POL-FS-2` |
| `fs.read_file` + `credentials.yaml` | `reject` / `POL-CRED-1` |
| `fs.list_dir` + 绝对越界 | `reject` / `POL-FS-1` |
| `web.fetch` + 未列域名 | `reject` / `POL-NET-1` |
| `exec.subprocess`（strict） | `reject` / `POL-EXEC-1` |
| `exec.subprocess` + `shell=True`（basic） | `reject` / `POL-EXEC-1` |
| `fs.write_file` 目标已存在 | `approval` / `POL-OVW-1` |
| `fs.delete_file`（critical） | `reject` / `POL-DGR-1` |
| 合规 `fs.read_file` | `allow` |
| `can_use` 为假 | `reject` / `GRD-401` |

> 补充一层：**该链就是治理层持有的链**（T-B 已断言 `spine.guard is policy.chain`）→ T-C 的"行为一致"由此**传递成立**，无需第二套 oracle。

### 守卫｜F-1
`set(_BUILTIN_IDS) <= set(_RULE_POLICY_REFS)` —— 封 `describe_rules` 的 `.get(g.id, ())` 静默默认。

---

## 五、git staged diff 预测

> **口径**：以下为**预估**（按改动行数估算，实际以 `git diff --numstat` 为准）。本轮为**两段式**（同 S1/S2-1 惯例）；若你选单 commit，合并两段即可。

| 文件 | 预估 | 说明 |
|---|---|---|
| `pyharness/engine.py` | **+20 / −5**（未拆）<br>**+28 / −8**（拆 `_build_governance`） | import 2 行 + `inj`/`policy`/`guard` ≈ 9 行（替换原 4 行）· `EngineSpine` 字段 1 行 · `spine(...)` 入参 1 行 · `attach_engine_to_ctx` 1 行 · **X-8** `EngineContext` 字段 + 赋值 2 行 |
| `pyharness/core/agent.py` | **+1 / −0** | 挂载 1 行 |
| `tests/unit/test_engine.py` | **+≈75 / −0** | 4 个用例（T-A/T-B/T-C/F-1）+ 语料表 ~35 行 + 断言 ~40 行 |
| **合计（代码段）** | **≈ +96 / −5** ~ **+104 / −8** | 视 X-6 / X-8 而定 |
| **文档段（第二 commit）** | 新增 1~2 个 `.md` | `S2-3_CHANGE_REPORT.md`（+ 若拆则 `S2-3.1` 计划文档已在工作区） |

**预估 `git diff --stat` 形态**：
```
 pyharness/core/agent.py        |   1 +
 pyharness/engine.py            |  20 +++++----
 tests/unit/test_engine.py      |  75 +++++++++++++++++++++++
 3 files changed, ≈96 insertions(+), ≈5 deletions(-)
```

---

## 六、超范围风险检查

| # | 风险 | 判定 | 缓解 |
|---|---|---|---|
| **O-1** | 顺手改 `tools_executor.py` 让运行期向治理层问询 | ❌ **超范围（= S3）** | 运行期**零改动**；`authorize()` 不实现 |
| **O-2** | 给 `GovernanceContext` 加 `authorize()` 或其他方法 | ❌ **超范围（Δ-5）** | `governance/context.py` 不改 |
| **O-3** | 给 `governance/policy.py` 加求值方法 | ❌ **超范围（S3）** | 该文件不改 |
| **O-4** | 为实现一致性去改 `tools_guard.describe_rules` 签名（X-5(a)） | ❌ **已被你否决** | `tools_guard.py` 不改；一致性退为**结构 + 行为** |
| **O-5** | 顺手改 `scope.py` / `events/*` / `approval.py` / `session.py` | ❌ 超出 | 明确不改（§三） |
| **O-6** | 顺手实现 `Decision`/`Receipt`/`Evidence`/`Audit` | ❌ 超范围（S3~S5） | 不创建 |
| **O-7** | 因"拆函数"而顺带重排装配顺序 | ⚠️ **实质风险** | 拆分**不得改变装配顺序/副作用**（纯提取）；若顺序变 → 回归面不可控 |
| **O-8** | `EngineContext`（X-8）漏加 → 桌面 shell 拿到 `ctx.guard` 但无 `ctx.governance` | ⚠️ 装配面不一致 | 建议纳入 X-8；至少 T-B 覆盖 `assemble_real_engine` 路径 |

**结论**：本计划**无超范围项**；O-7/O-8 是**必须显式处理**的两处细节。

---

## 七、待你批准/裁定

| # | 事项 | 建议 |
|---|---|---|
| **X-6** | 是否顺带拆 `build_runner_components` | **拆**（本函数本步必改；ADR-019 判"S2 内顺手"） |
| **X-8** | 是否同时把 `governance` 加入 `EngineContext`（`assemble_real_engine` 面） | **纳入**（否则桌面 shell 的装配面缺 `ctx.governance`） |
| **X-9** | 提交粒度 | **两段式**（代码 / 文档），同 S1/S2-1 惯例 |

**批准后落码（S2-3.2），随后按 CORE-02 跑全量回归。**
