# S2-3.0_ASSEMBLY_DESIGN.md — Governance Layer 进入运行链的设计评审

> **阶段**：S2-3.0（**只出设计，不落码**）
> **日期**：2026-09-14 ｜ **基线 HEAD**：`a6d097c`（工作区 clean）
> **依据**：[ADR-013](ARCHITECTURE_DECISION_RECORD.md)（治理层独立成层 · 接缝②）· [ADR-018](ARCHITECTURE_DECISION_RECORD.md)（目录/接口冻结）· [ADR-020](ADR-020-policy-event-boundary.md)· [S2-1_EXIT_REVIEW.md](S2-1_EXIT_REVIEW.md) · [S2-2.1_CHANGE_REPORT.md](S2-2.1_CHANGE_REPORT.md)
> **性质**：**未创建代码、未修改文件、未 commit**。等待人工批准后落码。

---

## 0. 结论摘要：**一处必须先纠正的架构偏差**

**你给的目标链图**：

```
request → engine → GovernanceContext → PolicyEngine → tools_guard → execution
```

**问题**：这条链把 `PolicyEngine` 放在**运行期的执行路径上**——而实测与 ADR 均表明 **S2-3 阶段它不在链上**。理由：

1. **今日的 `tools_guard` 不是由 `engine` 调用的**。`guard.evaluate` 的**唯一调用方是 `tools_executor` 关 2b**（`tools_executor.py:449`）。`engine` 只负责**装配**，运行期从不介入（`git grep` 实证：全库无 `engine.` 的运行期调用）。
2. **ADR-013 接缝② 明确**：治理层与现有代码的新增接缝只有 3 处，其中②是"**`ctx.governance` 单实例装配点**"——即**装配期挂载**，不是运行期调用。
3. **G-4 / INV-G5**：治理层**只授权不执行**；若 `PolicyEngine` 在链上向 `tools_guard` 下传调用，它就在**编排执行**——正是 ADR-018 的"违反后果④"（治理层获得执行编排视角 → 为"治理层开始执行"打开缺口）。
4. **S2 判据 ③"全量绿"** = 零行为变更。在链上插入一次调用即改变行为。

**⇒ 修正后的目标链**（S2-3 = M2 边界）：

```
【装配期】engine.build_runner_components
    ├─ PolicyEngine.from_config(rules=describe_rules(**inj),
    │                           chain_factory=guard_from_config, **inj)
    │        └─ 内部经 chain_factory 构造 GuardChain（执行对象）
    ├─ spine.guard      = policy_engine.chain     ← 与今日同型，executor 消费面不变
    └─ spine.governance = GovernanceContext(policy=policy_engine)
              └─ create_agent → ctx.guard / ctx.governance       ← 单实例挂载

【运行期】与今日**逐字节相同**（无新增一跳）
    … → agent_loop.run_turn → ctx.tools.execute(call, ctx)
        → 关2b ctx.guard.evaluate(call, scope)   ← 仍由 tools_executor 调用
```

**即：S2-3 让治理层"进入装配链"，而非"进入运行链"。** 让运行期向治理层问询（`authorize()`）是 **S3**（关 2 出口由枚举升格为决策对象）——本步**不做**。

> 若你确实希望在 S2-3 就让运行期经过 `PolicyEngine`，那**超出 M2 边界**：需同时做 S3 的关 2 升格 + `authorize()` 实现，且会破坏"零行为变更"判据。**建议按修正链执行。**

---

## 1. 当前执行链分析（实测）

### 1.1 运行期主链（与治理层今天的关系）

```
外壳 submit
 └─ core/agent.py::Agent.submit → session.append("user.message", sync=True)
     └─ core/task_queue.py::TaskQueue.submit → 泵单例 → runner.run_for_task
         └─ engine.py:680 make_runner.run_for_task
             └─ engine.py:747 _agent_ctx_of  ← ★ create_agent 在此（ctx 装配）
             └─ core/agent_loop.py::wake → run_turn
                 ├─ ctx.llm.chat / chat_stream
                 └─ core/tools_executor.py::execute(call, ctx)     ← ★ 四关管道（唯一强制点）
                     ├─ 关1a registry.lookup            (tools_registry)
                     ├─ 关1b registry.validate_args     (tools_registry)
                     ├─ 关2a ctx.scope.can_use          (core/scope.py)
                     ├─ 关2b ctx.guard.evaluate         ← ★★ tools_guard 的**唯一运行期调用方**
                     ├─ 关2.5 ctx.approval.request      (core/approval.py)
                     ├─ 关3  Provider（线程池/超时/取消）
                     └─ 关4  finalize（输出校验/spill/tool.result）
```

**关键事实**：

| 事实 | 证据 |
|---|---|
| `guard.evaluate` 唯一调用方是 `tools_executor` 关 2b | `tools_executor.py:449` |
| `engine` 在**运行期**从不被调用（只被装配与外壳调用） | 全库无 `engine.<runtime>` 的调用点 |
| `ctx.guard` 由 `create_agent` 从 `spine.guard` 挂载 | `core/agent.py:471` |
| `ctx` 装配点在 `create_agent` | `core/agent.py:461`（`ag.ctx = Ctx(...)`）+ `:471-489`（25 处 `ag.ctx.* =`） |
| 轻量壳 ctx 另一个装配点 | `engine.py:886-893`（`attach_engine_to_ctx` 的 `ctx.*` 赋值） |
| 治理层当前**无任何读者** | `grep 'ctx.governance'` 全库**零命中** |

### 1.2 装配期漏斗（决定"改一处 = 三壳同时获得"）

`build_runner_components` 是**唯一装配漏斗**，被 5 处调用：

| 调用点 | 覆盖 |
|---|---|
| `engine.py:327`（`build_spine`） | CLI / ACP / `assemble_real_engine` |
| `engine.py:873`（`attach_engine_to_ctx`） | CLI（`cli.py:578`）· ACP（`acp.py:379`） |
| `application/service.py:215` · `:253` | 桌面业务门面 |
| `desktop/app.py:506` · `:553` | Web 桌面 |
| （`desktop_native/app.py:30` 经 `assemble_desktop_ctx` → `cli.assemble_ctx`，再由上列路径补线） | Qt 桌面 |

**⇒ 治理装配写进 `build_runner_components`，三壳与全部入口自动获得，`desktop/sessions.py` 无需改动。**

---

## 2. `engine.py` 修改方案

### 2.1 行为契约（六条不变式，落码须逐条守住）

| # | 契约 | 守法 |
|---|---|---|
| **C1** | **不改 g1–g7 判定逻辑** | 不触碰 `tools_guard` 的 `g_*_match/check`、`_resolve_geometry`、`_BUILTIN_IDS`、`build_builtin_chain`、`GuardChain` |
| **C2** | **不改 `tools_guard` 执行权** | 治理层**不调用** `chain.evaluate`；运行期问询是 S3 |
| **C3** | **不改 approve / `session.append` 契约** | 不触碰 `approval.py` / `session.py` / `tools_executor.py` |
| **C4** | **治理层只负责 policy 持有与求值** | S2-3 只**持有** `PolicyEngine`；`resolve()/current()` 已存在但**暂无人调用** |
| **C5** | **`ctx.governance` 单实例** | 一处构造（`build_runner_components`），两处挂载（`create_agent` + `attach_engine_to_ctx`）；**禁止散字段** |
| **C6** | **运行期零新增一跳** | `tools_executor` 的调用面 `ctx.guard` 类型与语义不变 |

### 2.2 具体改动（3 处，均在装配面）

| # | 位置 | 现状 | 改为 |
|---|---|---|---|
| **A** | `engine.py:483` | `guard = guard_from_config(cfg, session=log_, bus=bus, validator=tool_reg.validate_args, approval_channel=True)` | ① 把注入参数提为**一份** `inj`（见 §5.1 的参数同源要求）；② `rules = tools_guard.describe_rules(**inj)`；③ `policy = PolicyEngine.from_config(cfg, session=log_, bus=bus, rules=rules, chain_factory=guard_from_config, **inj)`；④ `guard = policy.chain` |
| **B** | `engine.py:595-598`（`EngineSpine(...)`） | `guard=guard, approval=approval,` | 追加 `governance=GovernanceContext(policy=policy),`（**需给 `EngineSpine` 加一个 `governance` 字段**，与既有 `guard/approval` 同型） |
| **C** | `engine.py:886-893`（`attach_engine_to_ctx`） | `ctx.guard = spine.guard` … | 追加 `ctx.governance = getattr(spine, "governance", None)` |
| **D** | `core/agent.py:471` 邻域（`create_agent`） | `ag.ctx.guard = getattr(spine, "guard", None)` | 追加 `ag.ctx.governance = getattr(spine, "governance", None)` |

> **A 步的关键点**：`guard` 与 `rules` 必须**同源**（同一份注入参数），否则治理层持有的规则集与实际执行的链**在注入面分歧**（例如 `validator` 只注入了一侧）→ 见 §6 的一致性测试会立刻失败。**这是本步最容易出错的地方。**

### 2.3 可选（ADR-019 判"S2 内顺手做"）

`build_runner_components`（~185 行）按职责拆 1~2 个子函数（如 `_build_governance(cfg, log_, bus, tool_reg, inj)`）。**纯结构重构、行为不变**；若不拆，S2-3 的三处改动直接落在原函数内亦可。**建议拆**——本函数本步必然被改，且已加入治理装配点。

---

## 3. 文件影响范围

### 3.1 允许修改

| 文件 | 改动 | 必需性 |
|---|---|---|
| `pyharness/engine.py` | §2.2 的 A/B/C（+ 可选拆分） | **必需** |
| `pyharness/core/agent.py` | §2.2 的 D（`create_agent` 挂 `ctx.governance`，**1 行**） | **必需** |
| `tests/unit/test_engine.py` | 一致性测试（§6）+ F-1 守卫 + `ctx.governance` 挂载断言 | **必需** |
| `tests/unit/test_governance_policy.py` | 若一致性测试放此处（二选一） | 可选 |
| `pyharness/core/tools_guard.py` | **仅当采纳 X-5(a)**：给 `describe_rules` 增**可选** `chain=` 参数（只读派生） | **待裁定** |

### 3.2 禁止修改

`core/tools_executor.py`（四关结构/顺序）· `core/scope.py` · `core/approval.py` · `core/session.py` · `pyharness/events/*` · `pyharness/desktop/sessions.py` · `pyharness/governance/{policy,context}.py` · `tools_guard.py` 的**判定逻辑**（仅为 X-5(a) 加只读参数除外）· 任何 ADR / `ARCHITECTURE_DECISION_RECORD.md`

### 3.3 特别检查（你指定的四项）

| 文件 | 结论 | 依据 |
|---|---|---|
| **`engine.py`** | **必须改**（§2.2 A/B/C） | 装配漏斗所在 |
| **`core/context.py`** | ⚠️ **该文件不存在** | `ls pyharness/core/context.py` → 无。你的清单所指应为 **`pyharness/governance/context.py`**（`GovernanceContext` 类型所在，**本步无需改**），或 **`core/agent.py::create_agent`**（真正的 ctx 装配点，**本步必需改 1 行**） |
| **`tools_guard.py`** | **默认不改**；仅 X-5(a) 时追加**可选只读参数** | S2-1 已交付 `RuleDescriptor` / `describe_rules` / `_RULE_POLICY_REFS`（`+63/−0`）；判定逻辑不动 |
| **`desktop/sessions.py`** | **不需要改** ✅ | 桌面经 `build_runner_components` 拿 spine（`desktop/app.py:506,553`；`application/service.py:215,253`）→ 治理装配**自动获得**；其 `_attach_persistence` 只涉及落盘订阅，与治理无关 |

---

## 4. 装配方案

### 4.1 `PolicyEngine.from_config()`（S2-1 已交付，本步只**消费**）

**输入**

| 参数 | 生产值 | 说明 |
|---|---|---|
| `cfg` | `Settings` | 只读：取 `security.guards.disabled` 作初始禁用面 |
| `session` | `log_` | 事件落点（`policy.updated`） |
| `bus` | `bus` | 预留（当前未消费） |
| `rules` | `tools_guard.describe_rules(**inj)` | 规则描述（登记序 = 求值序） |
| `chain_factory` | `tools_guard.from_config` | **执行对象构造入口**（Δ-4：治理层不 import 它） |
| `validator` | `tool_reg.validate_args` | 透传 + 计入指纹 |
| `credential_paths` | `None`（→ cfg 默认） | 同上 |
| `path_exists` / `link_resolver` | `None`（→ 模块默认） | 同上 |
| `approval_channel` | `True` | 同 S1：engine 装配面视同通道在位 |
| `policy_id` / `version` | `"builtin:v1"` / `"1.0.0"` | 缺省 |

**输出**：`PolicyEngine`（持有 `Policy`（含内容哈希指纹）+ `PolicyRegistry` + `chain`）

### 4.2 组装 `GovernanceContext`

```python
spine.governance = GovernanceContext(policy=policy)     # M2 形状:只挂 policy
```

—— `decisions`/`receipts`/`evidence`/`audit` 保持 `None` 占位（S3~S5 补齐）；**`authorize()` 不声明**（Δ-5）。

### 4.3 挂载链（两处，保证三壳全覆盖）

| 路径 | 挂载点 | 覆盖面 |
|---|---|---|
| **Agent 路径** | `core/agent.py::create_agent`（`:471` 邻域） | `agent_loop` 消费的 `ctx`（三壳的任务执行都经此） |
| **轻量壳路径** | `engine.py::attach_engine_to_ctx`（`:890` 邻域） | CLI（`cli.py:578`）· ACP（`acp.py:379`）的前置门面 |

**幂等/单实例**：`GovernanceContext` 在 `build_runner_components` **只构造一次**；两处挂载只是把**同一对象**挂到两个 ctx 门面上（与 `ctx.guard` 既有做法完全一致）。

---

## 5. 一致性验证方案

> S2 判据 ②："治理层对同一 `(tool,args,scope)` 的判定与 `tools_guard` 逐例一致"。
> **注意**：M2 的 `PolicyEngine` **没有 `evaluate()`**（决策属 S3）。故本判据在 S2-3 的正确落地是**"规则集同一性"验证**——即证明治理层**持有的规则**与链**实际执行的规则**是同一套。

### 5.1 前置：参数同源（**A 步的硬要求**）

```python
inj = dict(validator=tool_reg.validate_args, credential_paths=None,
           path_exists=None, link_resolver=None, approval_channel=True)
rules = tools_guard.describe_rules(**inj)          # 描述面
policy = PolicyEngine.from_config(..., rules=rules,
                                  chain_factory=guard_from_config, **inj)  # 执行面
guard = policy.chain
```
**两侧必须用同一份 `inj`**。（`describe_rules` 与 `guard_from_config` 内部的 `build_builtin_chain` 会把注入参数**烘焙进 guard 的闭包**——参数分歧 = 判定分歧。）

### 5.2 验证三层（T-A / T-B / T-C）

| 层 | 断言 | 强度 |
|---|---|---|
| **T-A 结构同一性** | `[r.rule_id for r in policy.rules] == [g.id for g in guard.chain]`；`[r.rule_id for r in policy.enabled_rules()] == guard.enabled_guard_ids()`；`r.forced == (r.rule_id in FORCED_GUARDS)` | 中（验序与开关面） |
| **T-B 行为同一性（逐例）** | 对语料中每个 `call`：`await guard.evaluate(call, scope)` 与**测试侧按 `policy.enabled_rules()` 复算的 waterfall** 结果**逐例相等**（含 `scope.can_use` 前置与 approval 降级两闸） | 强（验语义） |
| **T-C 注入面同一性** | 对**参数敏感**的规则做定向用例（如 `validator` 注入 vs 不注入 时 g1 的判定；`credential_paths` 变化时 g4 的判定） | 强（验参数同源） |

**T-B 的语料**（覆盖 7 条规则 + 链级兜底）：

| 用例 | 期望 |
|---|---|
| `fs.read_file` 缺 `path`（畸形参数） | g-schema → **reject**（TLB-803） |
| `fs.list_dir` + `path="../../outside"` | g-fs-path → **reject**（POL-FS-2） |
| `fs.read_file` 读 `credentials.yaml` | g-credential-read → **reject**（POL-CRED-1） |
| `fs.list_dir` + 绝对越界路径 | g-fs-path → **reject**（POL-FS-1） |
| `web.fetch` + 未列入域名 | g-net-outbound → **reject**（POL-NET-1） |
| `exec.subprocess`（strict 沙箱） | g-exec → **reject**（POL-EXEC-1） |
| `exec.subprocess` + `shell=True`（basic） | g-exec → **reject**（POL-EXEC-1） |
| `fs.write_file` 目标已存在 | g-overwrite → **approval**（POL-OVW-1） |
| `fs.delete_file`（critical） | g-danger → **reject**（不可审批） |
| 合规调用（`fs.read_file` + workspace 内路径） | **allow** |
| `scope.can_use` 为假的工具 | 链级 → **reject**（GRD-401） |

**T-B 实现要点**：`guard.evaluate` 是 **async** 且需真实 `Scope`（含 `workspace_root`）→ 复用 `test_engine.py` 的 `build_spine`/`_cfg`/`_sessions_dir` 既有模式（S1 已建）；`ToolCall` 从 `tools_guard` 取。

**T-C 的额外价值**：它是 §2.2-A "参数同源" 的**唯一自动化守卫**——若将来有人只给一侧传 `inj`，T-C 会红。

### 5.3 顺带关闭 S2-1 Exit Review 的 F-1

```python
assert set(tools_guard._BUILTIN_IDS) <= set(tools_guard._RULE_POLICY_REFS)
```
（`describe_rules` 现用 `.get(g.id, ())`，新内置 guard 漏登记会**静默给空 refs**。）

### 5.4 待裁定：X-5（如何保证"政策规则 == 链规则"）

| 方案 | 做法 | 优点 | 代价 |
|---|---|---|---|
| **X-5(a) 同源派生（建议）** | `tools_guard.describe_rules(chain=None, *, ...)`：传既有 chain 则**从同一实例**派生描述符 | 结构+**对象**同一性可断言（`r.check is g.check`）→ 零参数漂移风险 | 给 `tools_guard` 追加**一个可选参数**（只读；不改判定逻辑/执行权） |
| **X-5(b) 双建 + 参数同源** | engine 用同一 `inj` 分别调 `guard_from_config` 与 `describe_rules` | 不动 `tools_guard` | 两个 `_FuncGuard` 实例 → **只能靠 T-B/T-C 行为复算**验证；参数漂移**不会**被结构性断言捕捉 |
| **X-5(c) 仅结构断言** | 只做 T-A | 最省 | **验不到注入参数面**——最弱 |

**建议 X-5(a)**：它让"同一性"从"行为等价（需可信 oracle）"升级为"**同一对象**（构造性保证）"，且参数面由构造保证、无需靠测试兜。

---

## 6. 回滚方案（→ `a6d097c`）

### 6.1 前提：S2-3 是"零行为变更"步，回滚代价极低

| 事实 | 后果 |
|---|---|
| S2-3 不插入运行期调用 → **无行为变更** | 回滚后系统行为与今日**完全一致** |
| `ctx.governance` 当前**无任何读者**（实证零命中） | 回滚后**无悬挂引用**（无需同步改消费方） |
| 治理层为**旁挂**，`spine.guard` 仍是 `chain_factory` 产出的 `GuardChain` | 回滚后 `executor` 消费面不变 |
| 不触碰 `events/*` / `session.append` / 落盘格式 | **无数据影响**（不需要迁移/回填） |

### 6.2 三级回滚

| 级别 | 命令 | 效果 |
|---|---|---|
| **1. 整步丢弃**（推荐） | `git reset --hard a6d097c` | 回到 S2-2.1 checkpoint；⚠️ 破坏性、丢弃 S2-3 全部产出（含文档）；**执行前先确认无未提交工作**（`git status` 须 clean） |
| **2. 保历史撤销** | `git revert <S2-3 feat commit> [<S2-3 docs commit>]` | 生成反向提交；**逆序**（文档描述代码） |
| **3. 文件级最小回滚** | ① `engine.py`：`guard = guard_from_config(...)` 复原、删 `governance=` 入参、删 `ctx.governance=`；② `agent.py`：删 `ag.ctx.governance = ...` 一行；③ 删新增测试 | 最细粒度；因改动是**加法为主**，删除即复原 |

### 6.3 回滚验收判据

回滚后必须满足（与 S2-2.1 终态一致）：

```
git status --short         → 空
git rev-parse --short HEAD → a6d097c
pytest 全量                → 1468 / 1466 passed / 2 skipped / 0 failed
grep -c governance engine.py core/agent.py → 0
```

---

## 7. 待裁定与风险

### 7.1 待裁定

| # | 事项 | 建议 |
|---|---|---|
| **X-0** | 采纳**修正链**（治理层进装配链、不进运行链），还是按原图让运行期经 `PolicyEngine`？ | **修正链**——原图超出 M2 边界且违 G-4/INV-G5（§0） |
| **X-5** | 一致性如何保证（§5.4） | **X-5(a) 同源派生** |
| **X-6** | 是否在本步拆 `build_runner_components`（ADR-019 判"S2 内顺手"） | **拆**（本函数本步必改） |
| **X-7** | 新增测试落点：`test_engine.py` 还是 `test_governance_policy.py` | `test_engine.py`（需 `build_spine` 真实装配） |

### 7.2 风险

| # | 风险 | 等级 | 缓解 |
|---|---|---|---|
| **R1** | **注入参数不同源** → 治理层规则与链的判定分歧（静默） | **高** | §5.1 单一 `inj`；**T-C 定向用例**守卫 |
| **R2** | `PolicyEngine.from_config` 会**另建一条 `GuardChain`**（经 `chain_factory`），若 engine 同时保留自己那条 → **两条链** | 中 | 以 `guard = policy.chain` **取代**原 `guard` 变量（§2.2-A ④），确保只有一条 |
| **R3** | 装配入口切换触及三壳（`build_runner_components` 是共享漏斗） | 中高 | 按 CORE-02 走**全量回归**；S1 已建 `test_engine.py` 装配断言可直接扩展 |
| **R4** | `EngineSpine` 加字段破坏既有构造点 | 低 | `EngineSpine` 是 dataclass，新字段给默认值 `None`；5 处 `build_runner_components` 调用点无需改 |
| **R5** | `attach_engine_to_ctx` 与 `create_agent` 两处挂载**不一致**（漏挂一处 → 某壳无 `ctx.governance`） | 中 | 两处都用 `getattr(spine, "governance", None)`；T-D 断言两路径均挂载 |
| **R6** | 一致性测试的**测试侧 oracle** 与 `GuardChain.evaluate` 语义漂移（T-B 把链的降级两闸/`can_use` 前置写漏） | 中 | 若采纳 **X-5(a)**，T-B 可退化为**对象同一性**断言，**不再需要 oracle**（推荐） |

---

## 8. 边界声明

| 项 | 状态 |
|---|---|
| **未创建代码** | ✅ |
| **未修改任何文件** | ✅（本文件为唯一新增） |
| **未 commit** | ✅ |
| 未进入 S2-3 落码 | ✅（本步为 S2-3.0 设计评审） |
| 未创建 Decision / Receipt / Evidence / Audit | ✅ |
| 未实现 `authorize()` | ✅ |
| 未修改 ADR / `ARCHITECTURE_DECISION_RECORD.md` | ✅ |

**等待人工批准后再落码。**
